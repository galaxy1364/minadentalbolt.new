# CHANGELOG — مینادنت

قالب هر رکورد: `شناسه | تاریخ | شرح | وضعیت`
وضعیت‌ها: `done` (تست‌شده) · `pending` (شروع نشده) · `needs-review` (نیاز به بررسی)

---

## MOD-FEAT-022 | ۱۴۰۵/۰۶/۱۰ | چارت سه در دارد، نه یکی | **done ✓**

### مشکل
ممیزی MOD-TEST-001 نشان داد `DentalChart` دقیقاً یک خروجی دارد:
`onAddTreatment(toothNumber)`. نه دری به لابراتوار، نه به ایمپلنت.

پس خواسته‌ی مهدی — «اگر شخصی فقط کار لابراتوار داشت مستقیم از چارت
انتخاب شود» — **از نظر ساختاری ممکن نبود**. هر دو ماژول انتخابگر پالمر
مستقل خودشان را داشتند، یعنی همان دندانی که پزشک روی چارت لمس کرده بود،
دو صفحه بعد از صفر دوباره پرسیده می‌شد.

نقص دوم: `onAddTreatment` فقط **شماره‌ی دندان** را پاس می‌داد. سطحی که
کاربر همان لحظه در ویرایشگر دندان علامت زده بود دور ریخته می‌شد.

### راه‌حل
ویرایشگر دندان حالا سه دکمه دارد: **افزودن درمان · سفارش لابراتوار ·
مورد ایمپلنت** — هر سه با شماره‌ی دندان **و سطحی که همان‌جا علامت خورده**.

`firstSurface` اولین سطحی است که کاربر چیزی جز «سالم» رویش زده — یعنی
همان سطحی که به‌خاطرش آمده.

| گزینه | ارزیابی |
|---|---|
| کپی کردن انتخابگر پالمر در هر ماژول | همان وضع فعلی؛ سه جای مستقل برای یک انتخاب |
| ادغام لابراتوار و ایمپلنت در فرم درمان | سه گردش‌کار متفاوت را در یک فرم فشرده می‌کند |
| **تحویل دندان از چارت به ماژول مقصد (انتخاب‌شده)** | هر ماژول فرم خودش را نگه می‌دارد، ولی سؤال دو بار پرسیده نمی‌شود |

**چرا «درمان» جابه‌جا نمی‌شود:** درمان داخل همان ویزیتِ باز ثبت می‌شود؛
رفتن به صفحه‌ی دیگر یعنی رها کردن ویزیت. `buildChartHandoff('treatment')`
عمداً `null` برمی‌گرداند.

**چرا تحویل ناقص رد می‌شود:** فرمی که خودش با نصف جواب باز شود، بدتر از
فرمی است که کاربر عمداً بازش کرده — چون معلوم نیست چه چیزی از قبل پر شده
و چه چیزی مانده. `readChartHandoff` بدون دندان **یا** بدون بیمار، `null`
برمی‌گرداند.

`window.history.replaceState` بعد از باز شدن فرم پاک می‌شود تا هر بار
برگشتن به صفحه، فرم دوباره باز نشود.

### نتیجه‌ی تست واقعی
```
npm run verify → sw-sync ✓ · tsc --noEmit ✓ · vitest ۶۹۲/۶۹۲ ✓ · vite build ✓
تست: ۶۷۵ → ۶۹۲ (۱۷ تست جدید در src/lib/chartHandoff.test.ts)
```
پنج تست آخر قفل ساختاری‌اند: وجود هر سه callback، هر سه دکمه، انتقال
سطح، اتصال در صفحه‌ی ویزیت، و خواندن تحویل در هر دو ماژول مقصد.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** سه دکمه به‌جای یکی در ویرایشگر دندان، روی
  صفحه‌ی گوشی جای بیشتری می‌گیرد و ممکن است اسکرول لازم شود. jsdom
  چیدمان را نمی‌سنجد.
- **مسیر ایمپلنت کامل تست نشده.** فرم ایمپلنت از قبل الگوی `quickStart`
  داشت و فقط دندان به آن اضافه شد؛ مسیر لابراتوار از صفر ساخته شد.
- **سطح فقط به فرم می‌رسد، در سفارش لابراتوار ذخیره نمی‌شود** —
  `lab_orders` ستون سطح ندارد. برای درمان ذخیره می‌شود، برای لابراتوار نه.
- **زنجیره‌ی هوشمند** (عصب‌کشی ← پست ← روکش) همچنان ساخته نشده.

---

## MOD-TEST-001 | ۱۴۰۵/۰۶/۱۰ | تست تعاملی واقعی + ممیزی چارت‌محوری بقیه‌ی ماژول‌ها | **done ✓**

### سؤال مهدی
«شما به عنوان کاربر می‌تونی کامل رو سیستم کار بکنی… واقعاً باهاش کار بکنی
و ایراداتش رو در بیاری؟»

جواب کوتاه: **نه کامل.** ولی مرزش را این جلسه با آزمایش مشخص کردم، نه با
حدس.

### آنچه امتحان شد
| روش | نتیجه |
|---|---|
| نصب Playwright | CLI نصب می‌شود ✓ |
| دانلود مرورگر Chromium | **شکست** — CDN خارج از دامنه‌های مجاز |
| اتصال به سایت زنده از bash | **شکست** — `vercel.app` در فهرست مجاز نیست |
| `@testing-library/react` + jsdom | **موفق** ✓ |

پس مرورگر واقعی در دسترس نیست، ولی **رندر کامپوننت و کلیک شبیه‌سازی‌شده**
هست. اولین تست تعاملی پروژه در `src/lib/toothPicker.interaction.test.tsx`
نوشته شد: کامپوننت واقعاً رندر می‌شود، کلیک رخ می‌دهد، DOM بررسی می‌شود.

باگ خط وسط (MOD-FIX-006) که قبلاً فقط منطقش تست شده بود، حالا **روی DOM
رندرشده** هم قفل شد — در هر دو حالت دائمی و شیری.

### یافته‌ی همان اولین اجرا
تست انتظار داشت برچسب دندان‌ها فارسی باشد؛ **لاتین بود**.
`PalmerToothPicker.tsx:50` مقدار `t.palmer` را خام چاپ می‌کند بدون
`toPersianDigits`. در عکس IMG_0317 هم دیده می‌شود: «8 7 6 5 4 3 2 | 1»
لاتین، کنار مبالغ فارسی. تست فعلاً واقعیت را ثبت می‌کند؛ رفعش کار
جداگانه‌ای است چون تغییر ظاهری است و تأیید بصری می‌خواهد.

### ممیزی: آیا لابراتوار و ایمپلنت هم همان ایراد را دارند؟
**بله، و بدتر.**

`DentalChart` دقیقاً **یک** خروجی دارد: `onAddTreatment(toothNumber)`.
هیچ `onAddLabOrder` و هیچ `onAddImplantCase` وجود ندارد.

| ماژول | ورود از چارت | انتخاب دندان |
|---|---|---|
| ثبت درمان | ✓ (MOD-FEAT-021) | یک بار |
| **لابراتوار** | **❌ ندارد** | `PalmerToothPicker` جدا، از صفر |
| **ایمپلنت** | **❌ ندارد** | `PalmerToothPicker` جدا، از صفر |

یعنی خواسته‌ی «اگر شخصی فقط کار لابراتوار داشت مستقیم از چارت انتخاب
شود» امروز **از نظر ساختاری ممکن نیست** — چارت آن درها را ندارد.

ضمناً `onAddTreatment` فقط **شماره‌ی دندان** را پاس می‌دهد، نه سطح و نه
وضعیتی که کاربر همان لحظه در ویرایشگر دندان ثبت کرده. پس بخشی از
دوباره‌کاری حتی در مسیر درمان هم باقی است.

### نتیجه‌ی تست واقعی
```
npm run verify → sw-sync ✓ · tsc --noEmit ✓ · vitest ۶۷۵/۶۷۵ ✓ · vite build ✓
تست: ۶۷۰ → ۶۷۵ (۵ تست تعاملی جدید)
```

### ریسک باقی‌مانده — و مرز صادقانه‌ی این روش
jsdom چیزی را **نمایش نمی‌دهد**؛ فقط DOM می‌سازد. پس این روش می‌گیرد:
ساختار، برچسب، رفتار کلیک، شرطی‌ها. و **نمی‌گیرد**: چیدمان، همپوشانی
عناصر، رنگ و کنتراست، اسکرول، رفتار لمسی، و اینکه چیزی زیر نوار پایین
پنهان شده باشد.

یعنی باگ‌هایی مثل «برچسب روی ریشه افتاده» یا «hover گیرکرده روی iOS» با
این روش هم پیدا نمی‌شوند. تأیید بصری مهدی همچنان لازم است — فقط کمتر.

---

## MOD-FEAT-021 | ۱۴۰۵/۰۶/۱۰ | ترتیب چارت‌محور و حذف پرسش تکراری دندان | **done ✓**

### مشکل — گزارش مهدی
«اول جای چارت و رویه و روند درست تغییر بده. بعد انتخاب هر دندان و سطح،
دیگه بابت اون دندان دوباره پرسش نشود.»

دو نقص جدا با یک ریشه‌ی مشترک.

**ترتیب —** ویزارد اول **رویه** را می‌پرسید و بعد **دندان**. این عکسِ
کاری است که واقعاً اتفاق می‌افتد: پزشک به یک دندان نگاه می‌کند و بعد
تصمیم می‌گیرد چه کاری روی آن انجام دهد. پرسیدن «بیلدآپ» پیش از «کدام
دندان» یعنی پزشک باید جواب را در ذهنش نگه دارد تا به سؤالی جواب بدهد که
خودش به آن جواب وابسته است. بدتر: **قیمت** هم در همان گام دندان بود،
یعنی پیش از آنکه معلوم باشد رویه چیست.

**تکرار —** زدن یک دندان روی چارت، همان دندان را به فرم پاس می‌داد
(`Treatments.tsx:1161`) — ولی ویزارد باز هم روی گام ۱ باز می‌شد و گام ۲
یک انتخابگر کامل پالمر نشان می‌داد و دوباره همان دندانی را می‌پرسید که
پزشک تازه لمس کرده بود. **اطلاعات آنجا بود؛ جریان صرفاً نادیده‌اش
می‌گرفت.**

### راه‌حل
ترتیب گام‌ها: `دندان و سطح ← رویه و هزینه ← لابراتوار و مالی ← یادداشت`

بخش قیمت از گام دندان به گام رویه منتقل شد و اعتبارسنجی‌ها هم با
فیلدهایشان جابه‌جا شدند — «قیمت واحد الزامی است» حالا جایی است که قیمت
پرسیده می‌شود.

وقتی دندان از چارت آمده باشد:
- ویزارد **از گام رویه** باز می‌شود، نه از دندان
- گام دندان به‌جای انتخابگر، یک **تأیید** نشان می‌دهد: «دندان ۳۸ — دیستال»
  با دکمه‌ی «تغییر دندان»

| گزینه | ارزیابی |
|---|---|
| پنهان کردن کامل گام دندان وقتی معلوم است | ویزاردی که دندانی نامرئی حمل کند، همان جایی است که دندان اشتباه صورتحساب می‌شود |
| فقط پیش‌فرض گذاشتن در انتخابگر (وضع فعلی) | همان پرسش تکراری؛ پزشک نمی‌داند باید دوباره بزند یا نه |
| **تأیید قابل‌تغییر (انتخاب‌شده)** | دیده می‌شود، قابل اصلاح است، ولی پرسیده نمی‌شود |

**ویرایش عمداً استثناست:** همیشه از گام اول شروع می‌شود و انتخابگر کامل
دارد، چون کسی که آمده چیزی را عوض کند شاید نداند در کدام گام است.

### نتیجه‌ی تست واقعی
```
npm run verify → sw-sync ✓ · tsc --noEmit ✓ · vitest ۶۷۰/۶۷۰ ✓ · vite build ✓
تست: ۶۵۱ → ۶۷۰ (۱۹ تست جدید در src/lib/treatmentWizardFlow.test.ts)
```
پنج تست آخر **قفل ساختاری** روی خود `Treatments.tsx` هستند: ترتیب گام‌ها،
نبودِ گام قدیمی «دندان و هزینه»، رد شدن از گام دندان هنگام ورود از چارت،
و باقی ماندن راه تغییر دندان. بازآرایی بعدی نمی‌تواند بی‌صدا برشان
گرداند.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** جابه‌جایی بلوک‌های JSX تایپ‌چک و بیلد را رد
  کرد، ولی چیدمان واقعی روی گوشی دیده نشده. این بزرگ‌ترین تغییر ظاهری
  چند جلسه‌ی اخیر است و نیاز به چشم مهدی دارد.
- **سطح دندان هنوز از چارت نمی‌آید.** `onAddTreatment` فقط شماره‌ی دندان
  را پاس می‌دهد، نه سطح. منطق آماده است (`seededSummary` سطح را نشان
  می‌دهد اگر بیاید) ولی چارت هنوز آن را نمی‌فرستد. کار بعدی.
- **زنجیره‌ی هوشمند ساخته نشد** — عصب‌کشی ← پست ← روکش. این یک نگاشت
  دانشی است، نه ترتیب فرم، و کار جداگانه‌ای است.
- **لابراتوار و ایمپلنت هنوز چارت‌محور نیستند.** فقط ثبت درمان اصلاح شد.

---

## MOD-FEAT-020 | ۱۴۰۵/۰۶/۱۰ | پرداخت بابت کدام دندان و کدام پزشک | **done ✓**

### مشکل — گزارش مهدی
«در پرداخت بابت کدام دندان و کدام پزشک چقدر داده.»

جدول `payments` می‌گفت **چه کسی**، **چقدر** و **چه تاریخی** — ولی هرگز
**بابت چه**. نه `treatment_id`، نه `doctor_id`، نه شماره‌ی دندان.

در کلینیکی با دو پزشک و بیماری که شش دندانش زیر درمان است، این دو سؤال
بی‌جواب می‌ماند: این ۸ میلیون بابت کدام دندان بود، و کارِ چه کسی؟ سؤال
دوم مستقیماً **سهم درآمد پزشک** را تعیین می‌کند.

مثال واقعی از دیتابیس زنده: «مهدی امیری» دو بیلدآپ دارد — دندان ۱۱ به
۱۰,۵۰۰,۰۰۰ و دندان ۳۸ به ۱۲,۰۰۰,۰۰۰ — و پنج پرداخت که هیچ‌کدام نمی‌گفتند
بابت کدام‌اند.

### راه‌حل
| گزینه | ارزیابی |
|---|---|
| افزودن `tooth_number` و `doctor_id` روی خود پرداخت | داده تکراری می‌شود؛ اصلاح دندان در درمان، پرداخت را دروغ‌گو می‌کند — همان رانشی که MOD-FIX-008 بود |
| فقط یادداشت متنی | قابل جست‌وجو و جمع‌زدنی نیست؛ سهم پزشک از روی متن آزاد محاسبه نمی‌شود |
| **`treatment_id` به‌عنوان لنگر (انتخاب‌شده)** | درمان از قبل دندان و پزشک را دارد؛ یک منبع حقیقت |

`doctor_id` هم اضافه شد، ولی **فقط** برای پولی که به هیچ درمان مشخصی وصل
نیست (بیعانه‌ی کلی). وقتی `treatment_id` هست، پزشکِ درمان معتبر است — چون
سند واقعیِ اینکه کار را چه کسی کرده، همان درمان است.

**ترتیب درست این بار:** migration اول روی دیتابیس زنده اعمال و تأیید شد،
بعد کدی نوشته شد که آن ستون‌ها را می‌نویسد. در MOD-DATA-001..003 برعکس
بود و همان‌جا ثبتش کردم.

### چه چیزی نمایش داده می‌شود
- در فرم ثبت پرداخت: «بابت کدام درمان» با **شماره‌ی دندان و مانده‌ی همان
  درمان** در هر گزینه — دو چیزی که پشت میز پذیرش پرسیده می‌شود.
- در فهرست پرداخت‌ها: «بیلدآپ — دندان ۳۸ — دکتر سعید نوری»
- روی رسید چاپی: سطر «بابت» و سطر «پزشک»

پرداخت‌های قدیمی «بابت مشخص نشده» نشان می‌دهند. این اطلاعات است نه
نویز — دقیقاً می‌گوید کدام ردیف‌ها هنوز به تصمیم انسان نیاز دارند.

### نتیجه‌ی تست واقعی
```
npm run verify → sw-sync ✓ · tsc --noEmit ✓ · vitest ۶۵۱/۶۵۱ ✓ · vite build ✓
تست: ۶۳۴ → ۶۵۱ (۱۷ تست جدید در src/lib/paymentAttribution.test.ts)
بررسی زنده: ستون‌های treatment_id و doctor_id روی payments موجودند ✓
```

یک تست در اجرای اول خطا داد و **درست بود**: شماره‌ی دندان لاتین می‌آمد
کنار مبالغ فارسی. کد اصلاح شد، نه تست.

### ممیزی پرداخت‌ها (خواسته‌ی «پرداخت‌ها چک شوند»)
| بیمار | هزینه | پرداخت‌شده | مانده |
|---|---|---|---|
| آراد حسنوند | ۴,۵۰۰,۰۰۰ | ۴,۵۰۰,۰۰۰ | ۰ ✓ |
| ابا امیری | ۱,۰۰۰,۰۰۰ | ۱,۰۰۰,۰۰۰ | ۰ — با یک پرداخت «در انتظار» تکراری |
| مهدی امیری | ۳۰,۵۰۰,۰۰۰ | ۵۰,۵۰۰,۰۰۰ | **−۲۰,۰۰۰,۰۰۰** |

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.**
- **پرداخت تکراری هنوز جلویش گرفته نمی‌شود.** پرداخت دوم ابا امیری با
  فاصله‌ی ۲ دقیقه و ۳۷ ثانیه، همان مبلغ، همان روز، بدون هشدار ثبت شد.
  هشدار «تسویه‌حساب کامل» وجود دارد ولی حالت «همان مبلغ در چند دقیقه» را
  نمی‌گیرد. کار بعدی.
- **پرداخت‌های موجود بازتخصیص نشدند** — عمداً. اینکه آن ۸ میلیون بابت
  دندان ۱۱ بوده یا ۳۸، حدس من نیست.
- **سهم پزشک هنوز از این ستون محاسبه نمی‌شود.** داده حالا هست؛ گزارشش
  ساخته نشده.
- روند چارت‌محور (خواسته‌ی اصلی) هنوز دست نخورده است.

---

## MOD-FIX-011 | ۱۴۰۵/۰۶/۱۰ | صفحه‌ی چاپ، دامی بدون راه خروج بود | **done ✓**

### مشکل — گزارش مهدی
«پرینت وارد می‌شم، گزینه پرینت یا دانلود یا ارسال نداره که لینک بفرستم
برای بیمار، و اینکه گزینه برگشت نداره — باید برنامه را ببندم و دوباره
وارد شوم.»

عکس IMG_0312 دقیقاً همین را نشان می‌دهد: پرونده‌ی کامل بیمار، بدون هیچ
کنترلی، بدون نوار آدرس، بدون راه بازگشت.

### علت ریشه‌ای
دو چیز، هر دو مخصوص PWA نصب‌شده:

۱. `window.open('', '_blank')` در مرورگر یک تب جدید می‌دهد که نوار آدرس
   و حرکت بازگشت دارد. داخل PWA نصب‌شده هیچ‌کدام نیست — **سند تولیدشده
   کل صفحه است**. سندی که فقط محتوا دارد، بن‌بست است.

۲. `setTimeout(() => win.print(), 300)` روی iOS Safari اغلب **هیچ کاری
   نمی‌کند**. برای همین صفحه فقط یک متن ساده به نظر می‌رسید، نه پنجره‌ی
   چاپ. شکست بی‌صدا، دوباره.

و همین اشتباه **در چهار جا** تکرار شده بود: رسید پرداخت، نسخه،
رضایت‌نامه و پرونده‌ی کامل — هر کدام سند خودش را از صفر می‌ساخت.

### راه‌حل
| گزینه | ارزیابی |
|---|---|
| افزودن دکمه به هر چهار سند | چهار نسخه از یک نوار ابزار؛ پنجمی که ساخته شود باز فراموش می‌شود |
| نمایش سند داخل خود برنامه به‌جای پنجره‌ی جدید | بازنویسی هر چهار جریان و از دست دادن چاپ بومی مرورگر |
| **`buildPrintDocument()` — یک پوسته برای همه (انتخاب‌شده)** | یک راه خروج، یک بار درست، و قابل تست |

کنترل‌ها **داخل خود سند** ساخته می‌شوند، نه در برنامه — چون در آن لحظه
برنامه روی صفحه نیست:
- **بازگشت**: `window.close()`، اگر نشد `history.back()`، و اگر باز هم
  نشد یک راهنمای دیده‌شدنی. سکوت همان چیزی بود که دام را ساخت.
- **چاپ**: دکمه، نه تایمر. حالا شکست بی‌صدا ممکن نیست — یا پنجره باز
  می‌شود یا دکمه هنوز آنجاست.
- **ارسال برای بیمار**: `navigator.share`، و اگر نبود کپی در کلیپ‌بورد.
  فقط وقتی ساخته می‌شود که متنی برای اشتراک وجود داشته باشد — دکمه‌ای که
  کاری نمی‌کند از نبودنش بدتر است.

نوار ابزار با `@media print` روی کاغذ نمی‌آید.

**امنیت:** متن بیمار داخل اسکریپت با `JSON.stringify` و خنثی‌سازی `<`
قرار می‌گیرد، پس یادداشتی که شامل `</script>` باشد سند را نمی‌شکند.

### نتیجه‌ی تست واقعی
```
npm run verify → sw-sync ✓ · tsc --noEmit ✓ · vitest ۶۳۴/۶۳۴ ✓ · vite build ✓
تست: ۶۱۵ → ۶۳۴ (۱۹ تست جدید در src/lib/printDocument.test.ts)
```
سه تست آخر **قفل ساختاری** هستند: هیچ صفحه‌ای اجازه ندارد `<!DOCTYPE`
دستی بنویسد یا چاپ خودکار زمان‌دار بگذارد. بررسی شد: صفر مورد باقی‌مانده.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** رفتار `window.close()` داخل PWA نصب‌شده‌ی
  iOS از اینجا قابل اجرا نیست. اگر بستن کار نکرد، مسیر دوم و سوم هست،
  ولی کدام یکی عمل می‌کند را فقط مهدی می‌بیند.
- **«دانلود» ساخته نشد.** خواسته شده بود ولی PDF واقعی روی iOS از داخل
  مرورگر تولیدشدنی نیست بدون کتابخانه؛ «چاپ → ذخیره در فایل‌ها» همان کار
  را می‌کند. اگر لازم شد، کار جداگانه‌ای است.
- **متن اشتراک عمداً خلاصه است** — نام، شماره پرونده، تعداد درمان و
  مانده‌حساب. کل پرونده در یک پیام هم ناخواناست و هم پخش کردن سوابق
  پزشکی در پیام‌رسان‌ها کار درستی نیست.
- بقیه‌ی فهرست مهدی (تقویم، انتخاب مدت، روند چارت‌محور) دست نخورده.

---

## MOD-DATA-001..003 | ۱۴۰۵/۰۶/۱۰ | استانداردسازی داده‌ی زنده | **done ✓**

اولین جلسه‌ای که دسترسی مستقیم به دیتابیس زنده بود. به‌جای فرضیه،
اندازه‌گیری و سپس اصلاح — و هر اصلاح با کوئری واقعی تأیید شد.

### تأیید عملی MOD-FEAT-019
رویدادهای `encounter_created`، `treatment_created` و `appointment_created`
با تاریخ امروز در `patient_timeline` دیده شدند. یعنی قلاب‌های تایم‌لاین
روی سرور **واقعاً کار می‌کنند**. اولین تأیید عملی یکی از رفع‌های این
مجموعه، از داده‌ی واقعی نه از تست.

### MOD-DATA-001 — بازسازی تایم‌لاین (migration 029)
ریسک باقی‌مانده‌ی MOD-FEAT-019 این بود که رکوردهای قدیمی نامرئی می‌مانند.
۲ نوبت، ۵ ویزیت، ۴ درمان، ۱ سفارش لابراتوار و ۱ ایمپلنت تایم‌لاین
نداشتند.

`event_date` از تاریخ خودِ رکورد گرفته شد، نه `now()` — وگرنه کل تاریخچه
روی روز بازسازی جمع می‌شد و ترتیب واقعی اتفاق‌ها از بین می‌رفت.

```
تایم‌لاین: ۷ → ۲۰ سطر
«مهدی امیری»: ۱ → ۱۳ سطر، هر شش نوع رویداد
بیمار بدون تایم‌لاین: ۰
```
همان بیماری که در اسکرین‌شات ۱۴۰۵/۰۶/۰۹ تایم‌لاینش یک خط بود.

### MOD-DATA-002 — بازمحاسبه‌ی جمع ویزیت (migration 030)
یک ویزیت با اختلاف ۱۰,۰۰۰,۰۰۰ تومان پیدا شد — قربانی واقعی باگ
MOD-FIX-008، حالا اثبات‌شده روی داده‌ی زنده نه روی تست.

`paid_amount` **عمداً دست نخورد**. اضافه‌پرداختی که بعد از بازمحاسبه
ظاهر می‌شود، یک وضعیت مالی واقعی برای تصمیم انسان است، نه عددی که
بی‌سروصدا صافش کنیم.

### MOD-DATA-003 — واژگان نقش پرسنل (migration 031)
`staff.role` مقدار `admin` داشت که در فهرست `staffRoles` برنامه نیست.
آن فهرست برچسب فارسی و رنگ نشان را می‌سازد، پس مقدار خارج از آن خام و
بی‌استایل نمایش داده می‌شد — و بدتر: منوی نقش در فرم ویرایش چیزی برای
انتخاب نداشت، پس باز کردن و ذخیره‌ی آن شخص نقشش را بی‌صدا عوض می‌کرد.
به `manager` («مدیر») تبدیل شد.

### تأیید نهایی — کوئری واقعی بعد از اصلاح
```
جمع ویزیت ناسازگار ............... ۰
نقش پرسنل خارج از واژگان ......... ۰
نقش کاربر خارج از واژگان ......... ۰
رکورد بدون تایم‌لاین (۵ نوع) ...... ۰
بیمار بدون تایم‌لاین .............. ۰
سیاست خواندن باز (USING true) .... ۰
```

### آنچه عمداً انجام نشد
- **قیمت چهار رویه** (بیلدآپ، یک/دو/سه سطح) خالی ماند. عدد جعلی در
  دیتابیس واقعی از خالی بودن بدتر است — این تصمیم کسب‌وکار است.
- **شیفت پزشکان** صفر ماند. ساعت کاری را نمی‌شود حدس زد.
- **۲۰,۰۰۰,۰۰۰ تومان اضافه‌پرداخت** روی سه ویزیت «مهدی امیری» دست نخورد.
  دقیقاً همان رقمی که در درس‌های پروژه ثبت شده و منشأش «داده‌ی تست» گفته
  شده بود — حالا تأیید شد: همان است، همان بیمار.

### ریسک باقی‌مانده
- مهاجرت‌های ۰۲۹ تا ۰۳۱ **اول روی دیتابیس زنده اجرا و بعد در مخزن ثبت
  شدند**. ترتیب درست عکس این است. هر سه idempotent هستند، ولی این همان
  رانشی است که در MOD-FIX-010 درسش گرفته شد.
- دو مسدودکننده‌ی راه‌اندازی (شیفت و قیمت) همچنان بازند.

---

## MOD-FIX-010 | ۱۴۰۵/۰۶/۱۰ | علت واقعی «وارد نمی‌شود» — از لاگ زنده‌ی سرور | **done ✓**

### مشکل
ادامه‌ی MOD-FIX-009. آن رفع، شکست را **قابل دیدن** کرد؛ این یکی علت را
پیدا و رفع کرد. برای اولین بار به دیتابیس و لاگ زنده دسترسی بود، پس
به‌جای فرضیه، اندازه‌گیری شد.

**آنچه در دیتابیس زنده دیده شد:**
- `auth.users` فقط **یک** ردیف دارد: `mostafa.hasanvand@gmail.com`
- `staff` پنج ردیف دارد، سه‌تا با ایمیل — دوتایشان هیچ حساب ورودی ندارند
- Edge Function `invite-staff` **مستقر و فعال** بود (نسخه‌ی ۵). فرضیه‌ی
  «مستقر نشده» رد شد.
- نقش سازنده `owner` بود. فرضیه‌ی «نقش کافی نیست» رد شد.

**آنچه در لاگ سرور دیده شد:**
```
19:22:42  422  A user with this email address has already been registered  (email_exists)
19:27:37  422  Phone logins are disabled                    (phone_provider_disabled) ×۲
19:20-19:28  400  Invalid login credentials                 (invalid_credentials) ×۸
```

سه چیز جدا، نه یکی:

۱. **ایمیل تکراری.** رکورد پرسنلِ «مالک کلینیک» ایمیلش
   `mostafa.hasanvand@gmail.com` است — یعنی خودِ مهدی، که از قبل حساب
   داشت. تیک «ساخت حساب ورود» روی رکورد خودش، ۴۲۲ گرفت. تابع متن خام
   انگلیسی Supabase را برمی‌گرداند و برنامه (پیش از MOD-FIX-009) آن را
   پشت توست سبز پنهان می‌کرد.

۲. **ورود با موبایل اصلاً ممکن نیست.** ارائه‌دهنده‌ی Phone در Supabase
   خاموش است (نیاز به سرویس پیامک دارد که هنوز نیست). صفحه‌ی ورود یک تب
   موبایل دارد که **با هیچ رمزی** کار نمی‌کند، و پیام کلی می‌داد.

۳. **هشت بار «رمز اشتباه».** تلاش برای ورود با حسابی که هرگز ساخته نشده
   بود. Supabase برای کاربر ناموجود هم همین را برمی‌گرداند تا فهرست
   کاربران لو نرود — پس از بیرون، «حساب وجود ندارد» و «رمز غلط» یکسان
   دیده می‌شوند.

### راه‌حل
**سمت سرور —** `invite-staff` بازنویسی و به‌عنوان **نسخه‌ی ۶ مستقر شد**:
- هر شکست، پیام فارسی + کد ماشین‌خوان برمی‌گرداند
- `email_exists` / `phone_exists` / `phone_provider_disabled` ترجمه‌ی
  اقدام‌پذیر می‌گیرند
- پیش‌بررسی: اگر پرسنل از قبل حساب دارد، ۴۰۹ با راهنمای «فراموشی رمز»
- خطای ناشناخته **متن اصلی را نگه می‌دارد** — پیام انگلیسی عجیب بهتر از
  پیام فارسی مهربانی است که پنهان می‌کند چه چیزی شکست
- تفکیک ۴۰۳ «بدون پروفایل» از ۴۰۳ «نقش کافی نیست»

**سمت برنامه —** `mapAuthError` حالا `phone_provider_disabled` را
می‌شناسد و می‌گوید «ورود با موبایل هنوز فعال نیست — با ایمیل وارد شوید»
به‌جای فرستادن کاربر دنبال رمزی که اصلاً مشکل نبود.

**اصلاح داده —** ردیف `users` مهدی `staff_id = null` داشت و به رکورد
پرسنلی‌اش وصل نبود. با یک `update` (بدون حذف) وصل شد، پس پیش‌بررسی
جدید از این به بعد این حالت را می‌گیرد.

**همگام‌سازی مخزن —** `supabase/functions/invite-staff/index.ts` با
نسخه‌ی مستقر یکی شد. درس این جلسه: **بودن در مخزن یعنی مستقر بودن نیست.**

### نتیجه‌ی تست واقعی
```
npm run verify → sw-sync ✓ · tsc --noEmit ✓ · vitest ۶۱۵/۶۱۵ ✓ · vite build ✓
تست: ۶۰۸ → ۶۱۵ (۷ تست جدید در src/lib/authErrors.test.ts)
```
ورودی تست‌ها حدسی نیست — رشته‌های خطای واقعی از لاگ Supabase است.

بررسی زنده‌ی بعد از استقرار: `invite-staff` نسخه‌ی ۶، وضعیت ACTIVE.

### ریسک باقی‌مانده
- **تابع سرتاسر اجرا نشد.** برای فراخوانی‌اش JWT معتبر مالک لازم است که
  از اینجا در دسترس نبود. کد استقرار شد و منطق ترجمه تست شد، ولی مسیر
  کامل «ساخت حساب واقعی» را اجرا نکردم. باید مهدی امتحان کند.
- **ورود با موبایل همچنان کار نمی‌کند** — فقط دلیلش را می‌گوید. فعال
  کردنش نیاز به سرویس پیامک دارد.
- **دو پرسنل هنوز حساب ورود ندارند:** «مینا مازندارنی» و «اکرم عیدی».
  عمداً برایشان حساب نساختم — رمز کاربر را نباید کسی جز خودش انتخاب کند.
- «مالک کلینیک» در `staff.role` مقدار `admin` دارد که در واژگان `ROLES`
  برنامه نیست. `staff.role` و `users.role` دو واژگان جدا هستند و این
  بررسی نشد.

---

## MOD-FIX-009 | ۱۴۰۵/۰۶/۱۰ | «ثبت شد» ولی حساب ورود ساخته نشده بود | **done ✓**

### مشکل — گزارش مهدی
«داخل برنامه تعریف می‌کنم که این جیمیل با این رمز وارد بشه، ولی با همان
رمز وارد نمی‌شود.»

ثبت پرسنل با تیک «ساخت حساب ورود» **دو نوشتن جدا** است:
۱. رکورد پرسنل در دیتابیس محلی
۲. فراخوانی Edge Function `invite-staff` که حساب واقعی Supabase Auth را
   می‌سازد

توست سبز «پرسنل با موفقیت اضافه شد» **بین این دو** شلیک می‌شد — پیش از
آنکه مرحله‌ی دوم حتی امتحان شود. پس وقتی مرحله‌ی دو شکست می‌خورد:
- سبز دیده می‌شد ✅
- پرسنل در فهرست ظاهر می‌شد ✅ (چیزی که آدم چک می‌کند)
- حسابی که کل هدف فرم بود، وجود نداشت ❌

خطای قرمز **بعد از** سبز می‌آمد و روی موبایل به‌راحتی از دست می‌رفت.

دو شکست بی‌صدای دیگر هم پیدا شد:
- **حساب غیرفعال:** رمز درست پذیرفته می‌شد، `loadProfile` بلافاصله
  `signOut` می‌زد و صفحه‌ی ورود **بدون هیچ پیامی** برمی‌گشت. از «رمز
  اشتباه» قابل تشخیص نبود.
- **کاربر بدون رکورد `users`:** اگر `invite-staff` کاربر Auth را بسازد و
  درج ردیف پروفایل شکست بخورد، شخص وارد می‌شود ولی نقش ندارد.
  `canAccess()` درست او را به داشبورد محدود می‌کند (بسته شکست می‌خورد،
  خوب است) ولی هیچ توضیحی نمی‌بیند — برنامه فقط خراب به نظر می‌رسد.

### علت ریشه‌ای
هر سه یک ریشه دارند: **یک عملیات، چند نتیجه، و گزارش هر مرحله جدا از
مرحله‌ی بعد.** یک ذخیره باید یک نتیجه داشته باشد.

### راه‌حل
| گزینه | ارزیابی |
|---|---|
| سبز را به بعد از invite منتقل کن | همان اشکال با ترتیب دیگر؛ منطق تصمیم باز هم داخل کامپوننت و غیرقابل تست می‌ماند |
| رکورد پرسنل را در صورت شکست عقب بگردان | نقض «هیچ حذف دائمی»؛ ضمناً پرسنل واقعاً ذخیره شده و پنهان کردنش باعث ثبت دوباره می‌شود |
| **`staffSaveMessage()` — یک پیام از هر دو نتیجه (انتخاب‌شده)** | دو نیمه نمی‌توانند با هم اختلاف داشته باشند، و قانون بدون رندر قابل تست است |

شکست ساخت حساب حالا **خطا** گزارش می‌شود، حتی با اینکه رکورد پرسنل نوشته
شده — چون نیمه‌ای که شکست خورده همان نیمه‌ای است که کاربر می‌خواست. سبز
گفتن به آن، همان چیزی است که باعث می‌شود یک حساب خراب یک هفته سالم به نظر
برسد. پیام هر دو را می‌گوید: «پرسنل ثبت شد — اما حساب ورود ساخته نشد».

`AuthState` حالا `notice` دارد و صفحه‌ی ورود آن را نشان می‌دهد، پس خروج
بی‌صدا جای خودش را به دلیل واقعی می‌دهد.

### نتیجه‌ی تست واقعی
```
npm run verify → sw-sync ✓ · tsc --noEmit ✓ · vitest ۶۰۸/۶۰۸ ✓ · vite build ✓
تست: ۵۹۸ → ۶۰۸ (۱۰ تست جدید در src/lib/staffSaveOutcome.test.ts)
```

### ریسک باقی‌مانده
- **علت اصلی رفع نشده — فقط دیده می‌شود.** این تغییر باعث نمی‌شود حساب
  ساخته شود؛ باعث می‌شود بفهمی چرا نشد. سه دلیل محتمل:
  ۱) Edge Function `invite-staff` روی پروژه مستقر نشده (کد در مخزن هست،
  ولی بودن در مخزن یعنی مستقر بودن نیست)، ۲) حساب سازنده در جدول `users`
  نقش `owner` ندارد — تابع ۴۰۳ برمی‌گرداند، ۳) `SUPABASE_SERVICE_ROLE_KEY`
  در محیط تابع تنظیم نشده.
- **تأیید بصری انجام نشده.**
- **حساب‌های ناقص موجود اصلاح نشدند.** هر پرسنلی که تا امروز با این باگ
  ساخته شده، هنوز حساب ورود ندارد. ویرایش همان پرسنل با تیک «ساخت حساب
  ورود» دوباره تلاش می‌کند — حالا با پیام درست.
- ورود با موبایل بررسی شد و **سالم بود** — `normalizeIranPhone` از قبل
  `0912…` را به `+98912…` تبدیل می‌کند. آن فرضیه رد شد.

---

## MOD-FEAT-019 | ۱۴۰۵/۰۶/۱۰ | تایم‌لاین بیمار فقط یک خط داشت — برای همیشه | **done ✓**

### مشکل
`addTimelineEntry()` در کل برنامه **یک** فراخوان داشت: `createPatient`.

یعنی تب «تایم‌لاین» هر بیمار دقیقاً یک سطر نشان می‌داد — «ثبت بیمار
جدید» — و بعد از آن تا ابد خالی می‌ماند. نوبت، ویزیت، درمان، سفارش
لابراتوار و مورد ایمپلنت همگی ثبت می‌شدند بدون اینکه هیچ ردی روی همان
صفحه‌ای بگذارند که قرار است **تاریخچه‌ی بیمار** باشد.

این با اسکرین‌شات‌های ۱۴۰۵/۰۶/۰۹ هم تأیید شد: بیماری با یک ویزیت، دو
درمان، یک سفارش لابراتوار و یک مورد ایمپلنت — تایم‌لاینش فقط «ثبت بیمار
جدید» داشت.

مورد **ه-۳** فهرست تست دستی («تایم‌لاین بیمار همه‌ی این‌ها را نشان
می‌دهد؟») پاسخش **نه** بود.

### علت ریشه‌ای
دو مورد جدا:

۱. تابع نوشتن وجود داشت ولی هیچ‌کس صدایش نمی‌زد — همان الگوی «کد یتیم»
   که در این پروژه چهار بار باگ ساخته.
۲. `reference_id` همیشه `null` نوشته می‌شد، پس حتی سطرهای موجود هم به
   رکورد مبدأشان وصل نبودند.

و یک مورد سوم که موقع رفع پیدا شد: نقشه‌ی آیکون‌های تایم‌لاین در
`PatientDetail.tsx` کلیدهای `appointment`، `treatment`، `payment`،
`prescription`، `radiology` و `encounter` را داشت — **هیچ‌کدام هرگز
نوشته نمی‌شدند**. شش کلید مرده که فقط نفر بعدی را گمراه می‌کردند.

### راه‌حل
| گزینه | ارزیابی |
|---|---|
| در هر صفحه بعد از ثبت، `addTimelineEntry` صدا زده شود | صفحه یک **مسیر** است و مسیر فراموش می‌شود — دقیقاً همان‌طور که جمع ویزیت (MOD-FIX-008) و ارسال به لابراتوار (MOD-FIX-007) فراموش شدند |
| trigger روی دیتابیس | آفلاین کار نمی‌کند؛ این برنامه آفلاین‌اول است |
| **قلاب در لایه‌ی `api.ts` (انتخاب‌شده)** | هر مسیری از قبل از این توابع رد می‌شود، پس صفحه‌ی جدید تایم‌لاین را مجانی می‌گیرد |

پنج قلاب روی `createAppointment`، `createEncounter`، `createTreatment`،
`createLabOrder` و `createImplantCase`. `addTimelineEntry` حالا
`referenceId` می‌گیرد و هر سطر به رکورد مبدأش وصل است.

**نکته‌ی طراحی مهم:** `logToTimeline` خطا را می‌بلعد. یک سطر تاریخچه،
رکوردی **از** یک چیز است، نه خودِ آن چیز. از دست دادن یک خط تاریخچه بد
است؛ از دست دادن درمانی که آن خط توصیفش می‌کند غیرقابل قبول.

شش کلید مرده‌ی نقشه‌ی آیکون هم حذف و با نوع‌های واقعی جایگزین شد.

### نتیجه‌ی تست واقعی
```
npm run verify → sw-sync ✓ · tsc --noEmit ✓ · vitest ۵۹۸/۵۹۸ ✓ · vite build ✓
تست: ۵۹۱ → ۵۹۸ (۷ تست جدید در src/lib/timelineCoverage.test.ts)
```
تست‌ها قفل می‌کنند: هر شش رکورد اصلی قلاب دارند · تایم‌لاین بیش از یک
نویسنده دارد · نوشتن تایم‌لاین هرگز رکورد اصلی را نمی‌اندازد ·
`reference_id` واقعی نوشته می‌شود · **هر نوع رویدادِ نوشته‌شده در صفحه
آیکون دارد** · نقشه‌ی آیکون کلید مرده ندارد.

آخری مهم‌ترین است: تست، `api.ts` و `PatientDetail.tsx` را به هم قفل
می‌کند، پس افزودن رویداد جدید بدون آیکونش تست را می‌شکند.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** ترتیب، آیکون و خوانایی سطرهای جدید روی گوشی
  دیده نشده.
- **تاریخچه‌ی گذشته ساخته نمی‌شود.** فقط رکوردهای **جدید** روی تایم‌لاین
  می‌آیند. درمان‌ها و ویزیت‌های قبلی همچنان نامرئی‌اند. ساخت یک‌باره‌ی
  تایم‌لاین از داده‌ی موجود کار جداگانه‌ای است و نیاز به دسترسی Supabase دارد.
- **فقط ساخت، نه تغییر وضعیت.** لغو درمان، تحویل سفارش لابراتوار و
  پیشرفت مرحله‌ی ایمپلنت هنوز روی تایم‌لاین نمی‌آیند. عمداً — این رفع یک
  چیز را کامل می‌کند نه سه چیز را نصفه.
- **پرداخت هنوز قلاب ندارد.** `createPayment` منطق شاخه‌دار دارد
  (ویزیت/ایمپلنت/طرح قسطی) و افزودن قلاب بدون خواندن کامل آن منطق ریسک
  دارد. مورد بعدی است.

---

## MOD-FIX-008 | ۱۴۰۵/۰۶/۱۰ | جمع ویزیت بعد از اصلاح قیمت اشتباه می‌ماند | **done ✓**

### مشکل
`encounters.total_amount` یک کپی ذخیره‌شده از «این ویزیت چقدر است» است.
**چهار مسیر** درمان‌های یک ویزیت را عوض می‌کنند؛ فقط **دو تا** این عدد را
می‌نوشتند، آن هم با دو حساب متفاوت:

| مسیر | قبلاً |
|---|---|
| ثبت درمان جدید | بازمحاسبه از صفر |
| ثبت دسته‌ای | مقدار ذخیره‌شده + دلتا (حساب دوم) |
| **ویرایش درمان** | **هیچ** ❌ |
| **لغو درمان** | **هیچ** ❌ |

یعنی درمان ۱۰ میلیونی را به ۶ میلیون اصلاح می‌کردی، سربرگ ویزیت همچنان
۱۰ میلیون می‌ماند — برای همیشه. و چون گزارش «کل درآمد»
(`Treatments.tsx:380`) از جمع همین فیلد ساخته می‌شود، **درآمد کل کلینیک
هم ۴ میلیون بیشتر از واقعیت** نشان داده می‌شد. لغو درمان همان اثر را
داشت. نقض مستقیم «محاسبه‌ی مالی غیردقیق».

### آنچه اشتباه **نبود** — و مهم است
`calcPatientBalance()` مانده‌ی بیمار را **زنده** از `treatments` حساب
می‌کند، نه از این فیلد. پس بدهی بیماران هیچ‌وقت اشتباه نبوده. خطا فقط در
سه جای نمایشی/گزارشی بود: سربرگ ویزیت (`:1145`)، ستون مبلغ در جدول
ویزیت‌ها (`:975`) و گزارش درآمد (`:380`).

### علت ریشه‌ای
عدد در چهار جای مختلف، هر بار از نو، به‌صورت درون‌خطی حساب می‌شد. دو جا
یادشان رفت. روش «دلتا» هم ذاتاً شکننده است: فقط وقتی درست است که **همه‌ی**
نوشتن‌های قبلی درست بوده باشند — و دو تایشان اصلاً نمی‌نوشتند.

### راه‌حل
| گزینه | ارزیابی |
|---|---|
| همان حساب را در دو مسیر جامانده هم کپی کن | چهار نسخه از یک فرمول؛ نفر بعدی مسیر پنجم را اضافه می‌کند و باز جا می‌ماند |
| trigger روی دیتابیس | درست، ولی migration روی دیتابیس زنده لازم دارد و آفلاین کار نمی‌کند — این برنامه آفلاین‌اول است |
| **`calcEncounterTotal()` در `finance.ts` + یک نقطه‌ی نوشتن (انتخاب‌شده)** | یک فرمول، کنار `calcPatientBalance` که همان فیلتر را دارد |

`syncEncounterTotal()` قبل از نوشتن، درمان‌ها را **دوباره می‌خواند** و از
صفر جمع می‌زند. روش دلتا کاملاً حذف شد. هر چهار مسیر حالا از همین‌جا رد
می‌شوند.

فیلتر عمداً با `calcPatientBalance` یکی است: درمان لغو‌شده نه در بدهی
بیمار می‌آید نه در جمع ویزیت. دو عددی که یک پول را توصیف می‌کنند و با هم
نمی‌خوانند، از غلط بودن هرکدامشان بدتر است.

ضمناً مبلغ پیشنهادی هنگام رفتن به «مالی» هم دیگر عدد محلی نیست — همان
عدد تازه‌بازمحاسبه‌شده است.

### نتیجه‌ی تست واقعی
```
npm run verify → sw-sync ✓ · tsc --noEmit ✓ · vitest ۵۹۱/۵۹۱ ✓ · vite build ✓
تست: ۵۸۲ → ۵۹۱ (۹ تست جدید در src/lib/encounterTotal.test.ts)
```

### ریسک باقی‌مانده
- **ویزیت‌های موجود اصلاح نشدند.** هر ویزیتی که تا امروز درمانش ویرایش یا
  لغو شده، عدد غلطش هنوز در دیتابیس است. با یک بار ویرایش هر درمان آن
  ویزیت خودبه‌خود درست می‌شود، ولی **گزارش درآمد تا آن موقع اشتباه
  می‌ماند**. اصلاح یک‌باره‌ی داده‌های قدیمی کار جداگانه‌ای است.
- `syncEncounterTotal` یک `fetchTreatments()` اضافه می‌زند. روی IndexedDB
  محلی است و در عمل بی‌هزینه، ولی سنجیده نشده.
- تخفیف سطح ویزیت (`discount_amount`) در این جمع لحاظ نمی‌شود — همان رفتار
  قبلی حفظ شد تا این رفع فقط یک چیز را عوض کند.

---

## MOD-FIX-007 | ۱۴۰۵/۰۶/۱۰ | درمانی که می‌گفت رفته لابراتوار، ولی نرفته بود | **done ✓**

### مشکل
ثبت درمان جدید با تیک «ارسال به لابراتوار» → سفارش واقعی ساخته می‌شد ✅
**ویرایش** یک درمان و زدن همان تیک → **هیچ سفارشی ساخته نمی‌شد** ❌

و بدترین بخش: ردیف درمان چیپ «لابراتوار» را **نشان می‌داد**، چون آن چیپ
از `treatment.lab_id` می‌آید و ویرایش آن را ذخیره می‌کرد
(`Treatments.tsx:709`). یعنی صفحه می‌گفت کیس رفته آزمایشگاه، در حالی که
ماژول لابراتوار اصلاً خبر نداشت. تا وقتی روکش نرسد، هیچ‌کس نمی‌فهمید.

سه حالت دیگر هم پوشش نداشتند:
| کار کاربر | قبلاً | حالا |
|---|---|---|
| ویرایش + زدن تیک لابراتوار | هیچ | سفارش ساخته می‌شود |
| ویرایش + برداشتن تیک | سفارش یتیم می‌ماند، لابراتوار به کارش ادامه می‌داد | سفارش لغو می‌شود |
| عوض کردن آزمایشگاه | سفارش روی آزمایشگاه قدیمی می‌ماند | قبلی لغو، جدید ساخته می‌شود |

### علت ریشه‌ای
منطق ساخت سفارش **داخل شاخه‌ی `else`** (ثبت جدید) نوشته شده بود:

```tsx
if (editingTreat) { await updateTreatment(...) }      // ← هیچ اشاره‌ای به لابراتوار
else { ...; if (has_lab && lab_id) await createLabOrder(...) }
```

پس ویرایش هرگز به آن نمی‌رسید. علت دوم: `lab_orders` هیچ ستون
`treatment_id` ندارد، پس پیوند بین درمان و سفارش **استنباطی** است و آن
استنباط به‌صورت درون‌خطی فقط در یک نقطه (مسیر لغو درمان) نوشته شده بود —
جای دیگری که لازمش داشت، نداشتش.

### راه‌حل
| گزینه | ارزیابی |
|---|---|
| افزودن ستون `lab_orders.treatment_id` | **درست‌ترین**، ولی نیاز به migration اعمال‌شده روی دیتابیس زنده دارد. ارسال کدی که ستون ناموجود می‌نویسد دقیقاً همان چیزی است که یک بار سینک را شکست — عمداً انجام نشد |
| کپی کردن بلوک `createLabOrder` در شاخه‌ی ویرایش | باگ امروز را می‌بندد و «دو مسیر برای یک کار» می‌سازد |
| **بیرون کشیدن تصمیم به `src/lib/labHandoff.ts` (انتخاب‌شده)** | یک قانون، مشترک بین ثبت و ویرایش و لغو؛ و قابل تست بدون رندر |

`decideLabHandoff()` چهار حالت `none / create / cancel / replace` را
تصمیم می‌گیرد و `findLinkedLabOrder()` سفارش زنده‌ی یک درمان را پیدا
می‌کند.

**یک باگ دوم هم همین‌جا پیدا و رفع شد:** استنباط قدیمی فقط
`encounter_id` و `lab_id` را چک می‌کرد. دو درمان در یک ویزیت به یک
آزمایشگاه از هم قابل تشخیص نبودند — لغو یکی، سفارش آن یکی را لغو می‌کرد.
حالا `tooth_number` هم جزء شرط است.

دو مورد یتیم هم پاک شد: متغیر `newTreat` که ثبت می‌شد و هیچ‌جا استفاده
نمی‌شد، و نبودِ توست تأیید هنگام ثبت درمان بدون لابراتوار (تنها نشانه‌ی
موفقیت، بسته شدن پنجره بود).

### نتیجه‌ی تست واقعی
```
npm run verify → sw-sync ✓ · tsc --noEmit ✓ · vitest ۵۸۲/۵۸۲ ✓ · vite build ✓
تست: ۵۶۶ → ۵۸۲ (۱۶ تست جدید در src/lib/labHandoff.test.ts)
```

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** منطق ثابت شده؛ اینکه توست درست ظاهر شود و
  سفارش در ماژول لابراتوار دیده شود، نیاز به چشم مهدی دارد.
- **پیوند هنوز استنباطی است.** اگر کسی در ماژول لابراتوار دستی سفارشی با
  همان ویزیت/آزمایشگاه/دندان بسازد، سیستم آن را «سفارش همین درمان»
  می‌شناسد. رفع واقعی: `migration 029` برای افزودن `lab_orders.treatment_id`
  — **نوشته نشده و اعمال نشده**؛ باید قبل از هر کدی که آن ستون را
  می‌نویسد اعمال شود.
- سفارش‌های لابراتوارِ **موجود** که با ویرایش ساخته نشده بودند، اصلاح
  نشدند. اگر درمانی چیپ «لابراتوار» دارد ولی در ماژول لابراتوار نیست،
  باید دستی ثبت شود.

---

## MOD-UI-011 | ۱۴۰۵/۰۶/۱۰ | دکمه‌هایی که «حذف» می‌گفتند ولی حذف نمی‌کردند | **done ✓**

### مشکل
در ۱۳ جای برنامه دکمه‌ای با سطل زباله‌ی قرمز، عنوان «حذف» و در دو مورد
حتی برچسب **«حذف قطعی»** وجود داشت — در حالی که هیچ‌کدام چیزی را حذف
نمی‌کردند. همه `deactivate*` یا `status: 'cancelled'` می‌زدند.

بدترین نمونه‌ها:
| جا | چه می‌گفت | چه می‌کرد |
|---|---|---|
| انبار | «حذف اقلام» + «حذف قطعی» | `deactivateInventoryItem` |
| لیست انتظار | «حذف از لیست انتظار» + «حذف قطعی» | `cancelWaitingEntry` |
| ایمپلنت (کامپوننت) | توست «کامپوننت حذف شد» | `deactivateImplantComponent` |
| مالی شخصی | توست «حذف شد» | `cancelPersonalFinanceItem` |
| پنجره‌ی تأیید | «برای اجرای قطعی این عملیات…» | هیچ عملیات قطعی‌ای در کار نبود |

ضرر دوطرفه است: یا کاربر باور می‌کند داده رفته و دیگر در بایگانی دنبالش
نمی‌گردد، یا از ترس «قطعی» بودن اصلاً دکمه را نمی‌زند و کار روی زمین
می‌ماند. هر دو حالت روی داده‌ی واقعی کلینیک اثر دارد.

### علت ریشه‌ای
`ConfirmAction` یک نوع `'delete'` داشت — با آیکون سطل، رنگ قرمز و متن
«اجرای قطعی». چون آن نوع **وجود داشت**، طبیعی بود که هر کار مخربی به آن
وصل شود، حتی وقتی رفتار واقعی بعداً به soft-delete تغییر کرد
(migration 023 و 026). برچسب‌ها با رفتار مهاجرت نکردند.

### راه‌حل
| گزینه | ارزیابی |
|---|---|
| فقط متن ۱۳ دکمه اصلاح شود | همین امروز درست می‌شود و دفعه‌ی بعد دوباره خراب — نوع `'delete'` سر جایش می‌ماند |
| فقط تست اضافه شود | تست بدون رفع، خطای قرمز دائمی است نه راه‌حل |
| **حذف کامل نوع `'delete'` از `ConfirmAction` (انتخاب‌شده)** | چون `src/lib/api.ts` **صفر** فراخوانی حذف دارد، این نوع چیزی را توصیف می‌کرد که برنامه اصلاً توانش را ندارد. برداشتنش دروغ را «غیرقابل بیان» می‌کند، نه صرفاً «اصلاح‌شده» |

۱۲ فایل تغییر کرد. آیکون‌ها هم با کار واقعی هم‌راستا شدند
(`Trash2` → `Ban` برای لغو، `Archive` برای آرشیو) و سه `import` یتیمِ
`Trash2` که بعد از تغییر بلااستفاده مانده بودند حذف شدند.

### نتیجه‌ی تست واقعی
```
npm run verify → sw-sync ✓ · tsc --noEmit ✓ · vitest ۵۶۶/۵۶۶ ✓ · vite build ✓
تست: ۵۶۰ → ۵۶۶ (۶ تست جدید در src/lib/actionLabels.test.ts)
```
تست‌ها قفل می‌کنند: نوع `'delete'` در هیچ صفحه‌ای؛ عبارت «حذف قطعی»
در هیچ دکمه‌ای؛ توست موفقیتی که خبر از حذف بدهد؛ و هیچ عنوان پنجره‌ای که
«حذف» را به‌عنوان کارِ همان دکمه اعلام کند.

نکته‌ی جالب: تست در همان اجرای اول یک مورد گرفت که **درست بود** —
«این شرکت بیمه قابل حذف نیست». جمله‌ی منفی استثنا شد، چون دقیقاً همان
صداقتی است که این تست دنبالش است.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** فقط متن و آیکون عوض شده و منطق دست نخورده،
  ولی جا افتادن آیکون‌های جدید در دکمه‌های کوچک باید دیده شود.
- `Settings.tsx:1375` هنوز `delete: 'حذف'` دارد — این برچسب **گزارش
  ممیزی** است (عملیات دیتابیس در `audit_log`)، نه دکمه. عمداً دست نخورد.
- برچسب‌ها الان راست می‌گویند، ولی **مسیر بازگردانی** برای همه‌ی این
  ۱۳ مورد در «بایگانی» بررسی نشده. مورد «د» فهرست تست دستی همچنان باز است.

---

## MOD-FIX-006 | ۱۴۰۵/۰۶/۱۰ | خط وسط قوس پالمر: یکی دوتا شده بود، یکی اصلاً نبود | **done ✓**

### مشکل
در انتخابگر دندان (فرم «درمان جدید» و هر جای دیگری که `PalmerToothPicker`
استفاده می‌شود) خط جداکننده‌ی وسط دهان **دو بار** کشیده می‌شد:

```
… ۳ ۲ | ۱ | ۱ ۲ ۳ …        ← آنچه روی گوشی دیده شد (v1.167.0)
… ۳ ۲ ۱ | ۱ ۲ ۳ …          ← آنچه باید باشد
```

ثنایای میانی سمت راست در قفسه‌ی خودش تنها می‌افتاد و شبیه دندان جدا یا
جای خالی به نظر می‌رسید. **باگ دوم در همان خط کد:** در حالت
«دندان شیری» **هیچ خط وسطی کشیده نمی‌شد**، یعنی مرز راست/چپ دهان در
چارت اطفال اصلاً معلوم نبود.

### علت ریشه‌ای
شرط جای‌گذاری خط روی **برچسب چاپ‌شده‌ی دندان** تصمیم می‌گرفت:

```tsx
{i > 0 && t.palmer === '1' && <divider />}
```

در نماد پالمر هر ربع از ۱ شروع می‌شود، پس `palmer === '1'` برای **هر دو**
ثنایای میانی (FDI 11 و 21) درست است → دو خط. و در دندان شیری برچسب
`'A'` است نه `'1'` → صفر خط. یک شرط، دو حالت خرابی متضاد.

عامل بقای باگ: داده‌ی ردیف‌ها و این شرط **داخل فایل کامپوننت** بودند و
بدون رندر React قابل import نبودند، پس هیچ تستی نمی‌توانست اجرایشان کند.
کامنت بالای همان کد ادعا کرده بود «دیگر نمی‌تواند خط را جابه‌جا بگذارد».

### راه‌حل
| گزینه | ارزیابی |
|---|---|
| شرط را به `palmer === '1' && side === 'چپ'` تغییر بده | کوتاه‌ترین، ولی هنوز به برچسب وابسته است و دندان شیری را دوباره از قلم می‌اندازد |
| شماره‌ی خانه را ثابت بنویس (۸ برای دائمی، ۵ برای شیری) | هر تغییر آینده در ردیف‌ها بی‌صدا خرابش می‌کند |
| **مرز `راست → چپ` (انتخاب‌شده)** | خط وسط، طبق تعریف، همان مرز دو سمت است — در هر دندانی و هر برچسبی |

داده‌ی چهار ردیف قوس و تابع `isMidlineStart()` از کامپوننت به
`src/lib/palmerArch.ts` منتقل شد تا **قابل تست بدون React** باشد؛ همان
چیزی که نبودش اجازه داده بود باگ زنده بماند.

ضمناً `isMidline ? 'mr-1'` که به هر دو ثنایای میانی حاشیه می‌داد حذف شد —
مفهوم تکراری با فاصله‌ی خود جداکننده بود و فاصله‌ی دو طرف خط را نامتقارن
می‌کرد.

### نتیجه‌ی تست واقعی
```
npm run verify → sw-sync ✓ · tsc --noEmit ✓ · vitest ۵۶۰/۵۶۰ ✓ · vite build ✓
تست: ۵۴۷ → ۵۶۰ (۱۳ تست جدید در src/lib/palmerArch.test.ts)
```
تست‌ها قفل می‌کنند: در هر چهار ردیف **دقیقاً یک** خط وسط؛ خانه‌ی ۸ برای
دائمی و ۵ برای شیری؛ هیچ ردیفی با خط شروع نمی‌شود؛ خط همیشه روی اولین
دندان سمت چپ می‌افتد.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** درستی منطق ثابت شده، ولی اینکه خط روی گوشی
  در جای درست *دیده* شود هنوز به چشم مهدی نیاز دارد. درس v1.157 همین بود.
- `DentalChart.tsx` ردیف‌های دندان را **جداگانه** می‌سازد و از
  `palmerArch` استفاده نمی‌کند. الان باگی ندارد چون خط وسط را جور دیگری
  می‌کشد، ولی دو منبع حقیقت برای «کدام دندان در کدام سمت است» باقی مانده.
  ادغامشان یک کار مستقل است، نه بخشی از این رفع.

---

## MOD-SEC-001 | ۱۴۰۵/۰۶/۰۳ | رفع بنیادی RLS | **done ✓**

### مشکل
۳۹ از ۴۳ سیاست امنیتی روی `USING (true)` بودند. RLS فقط ظاهراً روشن بود.
هرکس کلید `anon` را داشت (در هر PWA سمت کلاینت قابل استخراج) می‌توانست
کل پرونده‌ی بیماران را بخواند یا حذف کند.

### علت ریشه‌ای
هیچ ستونی جدول `staff` را به `auth.users` وصل نمی‌کرد، پس نوشتن هیچ
سیاست معناداری ممکن نبود — به همین دلیل همه روی `true` رها شده بودند.

### راه‌حل (۴ migration)
| Migration | کار |
|---|---|
| `20260825000001` | افزودن `staff.user_id` + توابع `current_clinic_id()` و `current_staff_role()` با `SECURITY DEFINER` و `search_path` قفل‌شده |
| `20260825000002` | آزمایش روی `patients` (فقط یک جدول، برای کاهش ریسک) |
| `20260825000003` | تعمیم خودکار به همه‌ی جداول دارای `clinic_id` |
| `20260825000004` | `notification_logs`، `sms_logs` (بدون `clinic_id`) و `online_booking_requests` |

### نتایج تست واقعی (روی دیتابیس زنده)
| سناریو | قبل | بعد | نتیجه |
|---|---|---|---|
| کاربر ناشناس ← خواندن بیماران | ۲ رکورد | **۰** | ✅ |
| کاربر ناشناس ← خواندن پرداخت/درمان/ایمپلنت/پیامک | همه | **۰** | ✅ |
| کاربر ناشناس ← درج بیمار | موفق | **رد شد** | ✅ |
| کاربر لاگین‌کرده ← خواندن ۸ جدول اصلی | همه | **همه (بدون تغییر)** | ✅ |
| نوبت‌دهی عمومی (بدون لاگین) ← درج | موفق | **موفق** | ✅ |

**آمار نهایی:** ۳۷ جدول با جداسازی کلینیکی، ۲ جدول محدود به کاربر
لاگین‌کرده، ۱ جدول با درج عمومی عمدی. صفر سیاست باز باقی مانده.

### ریسک باقی‌مانده
- `notification_logs` و `sms_logs` ستون `clinic_id` ندارند، پس جداسازی
  کلینیکی روی آن‌ها ممکن نیست؛ فعلاً به کاربر لاگین‌کرده محدود شده‌اند.
  در معماری چندکلینیکی آینده باید `clinic_id` اضافه شود.
- تفکیک دسترسی **بر اساس نقش** (مثلاً دستیار نتواند حقوق پرسنل را ببیند)
  هنوز فقط سمت کلاینت (`permissions.ts`) است، نه سطح دیتابیس.
  تابع `current_staff_role()` برای این کار آماده شده ولی هنوز استفاده نشده.

---

## MOD-TEST-001/002/003 | ۱۴۰۵/۰۶/۰۳ | زیرساخت تست خودکار | **done ✓**

### مشکل (AUD-003)
صفر تست خودکار در ۲۸٬۳۰۰ خط کد — علت مستقیم رگرسیون‌های مکرر
(فروریختن چیدمان کارت‌ها، دکمه‌های تکراری، سرریز هدر).

### تحلیل انتخاب ابزار
| گزینه | سرعت | نگهداری | سازگاری | نتیجه |
|---|---|---|---|---|
| **Vitest** | بالا (موتور Vite) | همان `vite.config` | بومی Vite 5 + TS 5 | ✅ انتخاب شد |
| Jest | کند (ts-jest) | پیکربندی موازی | اصطکاک ESM | رد |
| `node:test` | بالا | کامپایل دستی TS | alias ها را نمی‌فهمد | رد |

**دلیل انتخاب:** از همان پیکربندی بیلد واقعی استفاده می‌کند، پس هیچ
واگرایی بین محیط تست و تولید ایجاد نمی‌شود.

### پوشش
| شناسه | فایل | تعداد | تمرکز |
|---|---|---|---|
| MOD-TEST-001 | `finance.test.ts` | ۱۳ | درمان لغوشده، پرداخت در انتظار، ایمپلنت، تفکیک بیماران، null |
| MOD-TEST-002 | `persianDate.test.ts` | ۱۴ | تبدیل رفت‌وبرگشت، کبیسه، ۳۰ اسفند، ارقام فارسی، باگ نیمه‌شب |
| MOD-TEST-003 | `permissions.test.ts` | ۹ | مرز نقش‌ها، fail-closed، دور زدن با لینک مستقیم |

### نتیجه‌ی اجرای واقعی
```
✓ finance.test.ts      (13 tests)
✓ persianDate.test.ts  (14 tests)
✓ permissions.test.ts  (9 tests)
Test Files  3 passed (3)  |  Tests  36 passed (36)
```
تأیید شد که کد تست وارد بیلد تولیدی نمی‌شود.

### دستور جدید
`npm run verify` = تایپ‌چک ← تست ← بیلد (دروازه‌ی واحد قبل از هر commit)

### ریسک باقی‌مانده
- `sync.ts` (۲۲۲ خط) هنوز تست ندارد؛ نیاز به mock کردن Dexie و Supabase دارد.
- هیچ تست UI/کامپوننتی وجود ندارد — رگرسیون‌های چیدمانی همچنان
  فقط با چشم قابل تشخیص‌اند.

---

## MOD-UI-001 | ۱۴۰۵/۰۶/۰۳ | سیستم متریال iOS 27 | **done ✓**

### جستجو (مرحله ۱ استاندارد)
واقعیت iOS 27 با جستجوی وب تأیید شد، نه حدس:
- **iOS 26** (شهریور ۱۴۰۴): زبان طراحی «Liquid Glass» — شفاف و عمق‌دار
- **iOS 27** (WWDC خرداد ۱۴۰۵، انتشار شهریور): اپل عمداً **عقب‌نشینی کرد** —
  یک «نوار تنظیم شفافیت» اضافه کرد چون شفافیت زیاد خوانایی را خراب می‌کرد،
  به‌علاوه آیکون‌های لایه‌دارتر و واضح‌تر.

**نتیجه‌گیری:** پیاده‌سازی صادقانه‌ی iOS 27 یعنی شیشه‌ی زیبا **به‌همراه
کنترل واقعی کاربر** — نه شیشه‌ی بیشتر. برای نرم‌افزار پزشکی این ایمنی
است، نه سلیقه: مبلغ یا شماره‌ی دندانِ ناخوانا یعنی خطای واقعی.

### تحلیل (مرحله ۲)
| گزینه | نتیجه |
|---|---|
| **توکن CSS + کنترل زنده** | ✅ انتخاب شد — یک منبع حقیقت، کل اپ بدون دست زدن به ۷۳ فایل |
| فقط Tailwind config | رد — کنترل زنده بدون بیلد مجدد ممکن نیست |
| کتابخانه‌ی UI | رد — مهاجرت ۷۳ فایل، درگیری با ساختار فعلی |

### پیاده‌سازی (مرحله ۳)
- `src/lib/materials.ts` — ۴ سطح (مات / ملایم / استاندارد / شیشه‌ای کامل)
- مقادیر ثابت پراکنده (blur: 40/20/60/50px) حذف و به سه متغیر ریشه تبدیل شد:
  `--glass-blur` · `--glass-opacity` · `--glass-saturate`
- همه‌ی کلاس‌ها (`glass`, `glass-dark`, `glass-teal`, `glass-ultra`,
  `glass-specular`, `tab-bar`) از همین توکن‌ها مشتق می‌شوند
- نوار پایین عمداً در هر سطح کدرتر است (روی محتوای متغیر قرار می‌گیرد)
- هایلایت لبه با نزدیک شدن به حالت مات محو می‌شود (وگرنه شبیه خط اضافی می‌شود)
- **دسترس‌پذیری:** `prefers-reduced-transparency` هم در CSS و هم در JS
  رعایت شده و بر انتخاب کاربر **اولویت دارد** (مطابق راهنمای اپل)
- تب جدید «ظاهر و شفافیت» در تنظیمات با اعمال آنی

### تست واقعی (مرحله ۵)
```
✓ materials.test.ts (13 tests)
Test Files  4 passed (4)  |  Tests  49 passed (49)
```
تست کلیدی: **تضمین ریاضی ترتیب سطوح** — اگر کسی مقادیر را دستکاری کند
و «مات» شفاف‌تر از «شیشه‌ای» شود، تست می‌شکند.
همچنین: مقدار ذخیره‌شده‌ی خراب، نبود `matchMedia`، و سطح نامعتبر
هیچ‌کدام نباید رابط کاربری را بشکنند — هر سه تست شد.

### ریسک باقی‌مانده
- تأیید بصری روی گوشی واقعی لازم است؛ من فقط کد و بیلد را می‌بینم.
- تایپوگرافی و فاصله‌گذاری (بخش دیگری از زبان طراحی) هنوز توکن‌محور
  نشده — فعلاً فقط لایه‌ی متریال.
- آیکون‌های لایه‌دارتر iOS 27 اعمال نشده (نیاز به کار طراحی گرافیکی).

---

## MOD-UI-002 | ۱۴۰۵/۰۶/۰۳ | تایپوگرافی سیال و مقیاس معنایی | **done ✓**

### جستجو (مرحله ۱)
اجماع طراحی ۲۰۲۶، جستجوشده نه حدس:
- اندازه‌های ثابت px **منسوخ**‌اند → `clamp()` سیال
- **نسبت** تُکِن می‌شود نه اندازه (تغییر یک عدد = هماهنگی کل هرم)
- جمله‌ی `rem` در clamp **اجباری** است، وگرنه زوم مرورگر می‌شکند
- line-height نسبتی: ۱.۴–۱.۶ متن، ۱.۱–۱.۲۵ تیتر
- طول خط ۶۰–۷۵ کاراکتر (رایج‌ترین شکست خوانایی وب)

### کشف کلیدی حین بررسی کد
Tailwind از `rem` استفاده می‌کند و `input/textarea/select` روی `16px`
**ثابت px** قفل است (ضدزوم iOS). یعنی سیال کردن **اندازه‌ی ریشه**
کل ۷۳ فایل را هم‌زمان و هماهنگ مقیاس می‌دهد، بدون دست زدن به هیچ‌کدام،
و ورودی‌ها چون px هستند درست دست‌نخورده می‌مانند.

### تحلیل (مرحله ۲)
| گزینه | نتیجه |
|---|---|
| **ریشه‌ی سیال + توکن نسبتی** | ✅ صفر تغییر در ۷۳ فایل، زوم سالم |
| بازنویسی همه‌ی `text-*` | رد — پرریسک |
| media query ثابت | رد — «یادگار گذشته» طبق منابع ۲۰۲۶ |

### پیاده‌سازی (مرحله ۳)
- `src/lib/typography.ts` — نسبت «سوم بزرگ» (۱.۲۵)، ۷ توکن معنایی
- ریشه: `clamp(0.9375rem, 0.8958rem + 0.2083vw, 1.0625rem)` — ۱۵→۱۷px
- بازه عمداً محافظه‌کارانه: چون در همه‌ی padding و gap ضرب می‌شود
- **مخصوص فارسی:** line-height متن ۱.۶۵ (توصیه‌ی لاتین ۱.۴ است) —
  خط فارسی به‌خاطر زیرنویس و کشیدگی (ی، ج، ع) فضای بیشتری می‌خواهد
- شبکه‌ی فاصله‌گذاری ۴ واحدی + کلاس `.prose-measure` با `68ch`

### تست واقعی (مرحله ۵)
```
✓ typography.test.ts (14 tests)
Test Files  6 passed (6)  |  Tests  72 passed (72)
```
تست‌های کلیدی: **ترتیب اکید نزولی توکن‌ها** (اگر caption از body
بزرگ‌تر شود می‌شکند)، **الزام جمله‌ی rem** در clamp (محافظ زوم)،
**آستانه‌ی خوانایی** ریزترین متن، و **حداقل ۱.۶ برای فارسی**.

مقدار `clamp` در CSS با اجرای واقعی فرمول ماژول راستی‌آزمایی شد و
اختلاف جزئی اصلاح شد — دو منبع حقیقت برای یک عدد، منشأ واگرایی خاموش است.

### ریسک باقی‌مانده
- تأیید بصری روی گوشی لازم است (مقیاس ریشه روی فاصله‌ها هم اثر دارد).
- کلاس‌های `.text-display` تا `.text-micro` ساخته شده ولی هنوز در
  کامپوننت‌ها جایگزین `text-*` تیلویند نشده — پذیرش تدریجی.

---

## MOD-UI-003 | ۱۴۰۵/۰۶/۰۳ | سیستم حرکت و رفع باگ پرش | **done ✓**

### جستجو (مرحله ۱)
اصول حرکت ۲۰۲۶، جستجوشده:
- توکن معنایی به‌جای عدد خام؛ ۴ پله کافی است (instant/fast/base/slow)
- **خروج سریع‌تر و بدون کشش از ورود** — خروج کند مثل کندی سیستم حس می‌شود
- فقط `transform` و `opacity` روی GPU؛ بقیه باعث بازمحاسبه‌ی چیدمان
- بازخورد فشردن **زیر ۱۰۰ms** وگرنه لمس بی‌جواب حس می‌شود
- الگوی **ضریب‌مدت**: صفر کردن یک متغیر = رعایت خودکار کاهش حرکت
- تأخیر پلکانی باید **افت** کند وگرنه فهرست بلند کند حس می‌شود

### بررسی کد موجود — یافته‌ی مثبت و یک باگ واقعی
✅ **مثبت:** بلوک سراسری `prefers-reduced-motion` روی `*` از قبل وجود
داشت. پایه‌ی دسترس‌پذیری سالم بود — تغییرش ندادم.

🔴 **باگ واقعی:** نشانگر تب پایین `width` را انیمیت می‌کرد (خط ۸۰۸).
`width` خاصیت چیدمانی است، یعنی هر فریم کل نوار تب دوباره چیده
می‌شد — علت کلاسیک پرش، دقیقاً روی عنصری که کاربر بیشترین لمس را
با آن دارد.

### پیاده‌سازی (مرحله ۳)
- `src/lib/motion.ts` — ۴ پله، ۴ منحنی، ریاضیات مسافت و پلکان
- توکن‌های CSS با **الگوی ضریب‌مدت**: همه‌ی مدت‌ها در
  `--duration-scalar` ضرب می‌شوند، که در حالت کاهش حرکت صفر می‌شود.
  یعنی رعایت دسترس‌پذیری **ساختاری** است، نه چیزی که هر انیمیشن
  جدید باید یادش باشد انتخاب کند.
- مدت خروج ۰.۷ برابر ورود، با منحنی بدون کشش
- **رفع باگ:** نشانگر تب از `width` به `transform: scaleX()` تبدیل شد —
  از نظر بصری یکسان، ولی روی GPU ترکیب می‌شود نه بازچیدمان هر فریم

### تست واقعی (مرحله ۵)
```
✓ motion.test.ts (17 tests)
Test Files  7 passed (7)  |  Tests  89 passed (89)
```
تست‌های کلیدی: **آستانه‌ی ۱۰۰ms** برای فشردن، **عدم کشش در منحنی
خروج** (کشش یعنی عنصرِ در حال رفتن برمی‌گردد)، **زیرخطی بودن رشد
مسافت** (مسافت ۴ برابر نباید مدت ۴ برابر بدهد)، **افت تأخیر پلکانی**
(آیتم پنجاهم نباید ۲ ثانیه دیر بیاید)، و **تشخیص خواص ناامن GPU**.

تأیید شد که هیچ انیمیشن روی خاصیت چیدمانی در کل فایل باقی نمانده.

### ریسک باقی‌مانده
- انیمیشن‌های موجود (کارت، دکمه) هنوز مقادیر ثابت خودشان را دارند و
  به توکن‌ها مهاجرت نکرده‌اند — پذیرش تدریجی، بدون ریسک رگرسیون.
- روانی واقعی حرکت فقط روی دستگاه واقعی قابل قضاوت است.

---

## MOD-UI-003 | ۱۴۰۵/۰۶/۰۳ | سیستم حرکت — اتصال کد یتیم | **done ✓**

### جستجو (مرحله ۱)
اجماع ۲۰۲۶، جستجوشده:
- `prefers-reduced-motion` در صدر فهرست «حتماً باید رفع شود»
- توکن معنایی به‌جای عدد خام
- **خروج سریع‌تر از ورود** — خروج کند حس تأخیر می‌دهد نه ظرافت
- فقط `transform`/`opacity` — نه width/height/top/left (بازچینش هر فریم)
- مدت‌زمان با مسافت رشد کند، ولی **زیرخطی**

### دو کد یتیم پیدا شد (نقض مستقیم استاندارد خودمان)
۱. **توکن‌های CSS مدت‌زمان** تعریف شده بودند ولی فقط **۱ قانون** از
   ۳۶ مورد مصرفشان می‌کرد. یعنی `--duration-scalar: 0` که قرار بود
   با یک خط کل حرکت را برای کاهش‌حرکت خاموش کند، عملاً روی ۹۷٪
   انیمیشن‌ها **بی‌اثر** بود.
۲. **ماژول `motion.ts`** کامل و تست‌شده بود ولی **هیچ فایلی از آن
   استفاده نمی‌کرد**.

### راه‌حل
- همه‌ی ۲۵+ مدت‌زمان ثابت به توکن تبدیل شد: **از ۱ مصرف به ۳۶**،
  صفر مقدار ثابت باقی ماند. حالا `prefers-reduced-motion` واقعاً
  ساختاری است، نه اسمی.
- `staggerDelay()` در Dashboard جایگزین فرمول دستی `index * 60` شد.
  آن فرمول خطی بود: فهرست ۳۰ تایی ۱.۸ ثانیه طول می‌کشید تا کامل
  ظاهر شود. نسخه‌ی زیرخطی این مشکل را ندارد و تست هم دارد.

### تست واقعی (مرحله ۵)
```
✓ motion.test.ts (17 tests)
Test Files  7 passed (7)  |  Tests  89 passed (89)
```
تست‌های کلیدی: خروج همیشه سریع‌تر از ورود، رشد زیرخطی مدت با مسافت،
ویژگی‌های چیدمان به‌عنوان ناامن رد می‌شوند، منحنی خروج بدون کشش.

حین اتصال، یک خطای واقعی گرفته شد (کامنت JSX داخل ویژگی‌های تگ)
که تایپ‌چک آن را قبل از commit گرفت.

### ریسک باقی‌مانده
- تأیید بصری روی گوشی لازم است؛ نگاشت مدت‌زمان‌ها (مثلاً ۰.۲۸s →
  `base`) قضاوت من بوده و ممکن است جایی کمی سریع/کند حس شود.
- `durationForTravel` و `exitDuration` هنوز در کد زنده مصرف نشده‌اند
  — تست‌شده‌اند ولی هنوز یتیم‌اند.

---

## MOD-UI-004 | ۱۴۰۵/۰۶/۰۳ | کنتراست WCAG 2.2 و فوکوس | **done ✓**

### جستجو (مرحله ۱)
- **WCAG 2.2 AA معیار قانونی است** (ADA آمریکا، EAA اروپا از خرداد ۱۴۰۵)
  آستانه: ۴.۵:۱ متن معمولی، ۳:۱ متن بزرگ و اجزای رابط
- **APCA** دقیق‌تر است ولی هنوز الزام قانونی نیست؛ منابع صراحتاً
  می‌گویند فعلاً جایگزین نشود → عمداً WCAG 2.2 پیاده شد نه APCA
- **معیار ۲.۴.۱۱ (ظاهر فوکوس)** تازه در WCAG 2.2 — «بسیاری از
  استایل‌های پیش‌فرض مرورگر را رد می‌کند»

### 🚨 دو شکست واقعی کشف شد (نه تئوری)
تست خودکار روی **پالت واقعی همین برنامه** اجرا شد:

| ترکیب | نسبت واقعی | لازم | نتیجه |
|---|---|---|---|
| متن سفید روی `primary-600` | **۳.۷۴:۱** | ۴.۵:۱ | ❌ **مهم‌ترین دکمه‌های برنامه** |
| متن سفید روی `primary-500` | **۲.۴۹:۱** | ۳:۱ | ❌ |

**۲. هیچ استایل فوکوسی وجود نداشت.** نه ناکافی — اصلاً نبود.
Tailwind preflight پیش‌فرض مرورگر را حذف می‌کند و جایگزینی اضافه
نشده بود. یعنی کاربر کیبورد اصلاً نمی‌دید کجاست — مانع کامل.

### راه‌حل
- دکمه‌ی اصلی و شماره‌های ویزارد: `primary-600` → **`primary-700`**
  (۵.۴۷:۱ ✅). هاور به ۸۰۰ رفت تا تغییر وضعیت محسوس بماند.
- حلقه‌ی فوکوس با `:focus-visible` اضافه شد (نه `:focus` — کاربر ماوس
  با کلیک حلقه نمی‌گیرد، کاربر کیبورد همیشه می‌گیرد)
- حلقه‌ی سفید بیرونی تا روی سطح تیره/سبزآبی هم دیده شود
- `forced-colors` برای حالت کنتراست بالای ویندوز

### تست واقعی (مرحله ۵)
```
✓ contrast.test.ts (21 tests)
Test Files  8 passed (8)  |  Tests  111 passed (111)
```
شکست‌های کشف‌شده صریحاً به‌عنوان تست ثبت شدند (`primary-600` برای
متن سفید کافی نیست) تا کسی دوباره به‌اشتباه از آن‌ها استفاده نکند.
از این پس هر تغییر رنگی که خوانایی را بشکند، **بیلد را می‌شکند**.

### ریسک باقی‌مانده
- ممیزی روی توکن‌های پالت انجام شد، نه روی هر ترکیب واقعی در ۷۳ فایل.
  ترکیب‌های موردی (مثلاً متن رنگی روی پس‌زمینه‌ی شیشه‌ای) پوشش داده
  نشده‌اند — نیاز به axe-core برای پوشش کامل.
- تأیید بصری تغییر رنگ دکمه‌ها روی گوشی لازم است.

---

## MOD-DATA-001 | ۱۴۰۵/۰۶/۰۳ | تعطیلات قمری محاسبه‌شده (AUD-011) | **done ✓**

### مشکل
۵۱ ورودی تعطیلات برای ۱۴۰۳/۱۴۰۴/۱۴۰۶/۱۴۰۷ همان ماه/روز ۱۴۰۵ را
تکرار می‌کردند. تعطیلات قمری هر سال شمسی ~۱۱ روز عقب می‌روند، پس
**ریاضی‌وار تضمین‌شده غلط** بودند. این داده مستقیم در نوبت‌دهی
دیده می‌شود: تاریخ غلط = نوبت در روز تعطیل یا بستن مطب در روز کاری.

### تحلیل (مرحله ۲)
| گزینه | نگهداری | نتیجه |
|---|---|---|
| هاردکد دستی هر سال | ❌ همین باگ سال بعد برمی‌گردد | رد |
| **محاسبه از تقویم قمری با `Intl`** | ✅ همه‌ی سال‌ها خودکار، بدون وابستگی | ✅ انتخاب |
| کتابخانه‌ی جدید | وابستگی اضافه | رد |

### پیاده‌سازی
- `src/lib/lunarHolidays.ts` — ۱۷ تعطیلی با تاریخ ثابت قمری
  (عاشورا همیشه ۱۰ محرم است) از طریق `Intl` با تقویم ام‌القری
- ۵۱ ورودی غلط **حذف شد**؛ ۱۱ ورودی تأییدشده‌ی ۱۴۰۵ حفظ شد
- تاریخ‌های محاسبه‌شده با پسوند **«(تقریبی)»** نمایش داده می‌شوند

**تصمیم مهندسی:** حذف داده‌ی غلط، نه نگه‌داشتنش. تعطیلی «قطعی‌نما
ولی غلط» بدتر از «تقریبی صادقانه» است، چون پرسنل به آن اعتماد می‌کنند.

**محدودیت صادقانه:** ایران تقویم قمری را با **رؤیت هلال** تعیین
می‌کند نه محاسبه؛ ام‌القری معمولاً ۰ تا ۱ روز فرق دارد. به همین
دلیل «تقریبی» برچسب خورده.

### 🚨 باگ دوم که تست یکپارچگی کشف کرد
`getHoliday` از `replace('/', '-')` استفاده می‌کرد که فقط **اولین**
اسلش را عوض می‌کند. پس «1406/05/01» می‌شد «1406-05/01» و هیچ کلیدی
مطابقت نمی‌کرد — یعنی تابع برای **هر تاریخ با فرمت اسلش بی‌صدا
null برمی‌گرداند**، حتی برای نوروز.

این باگ **از قبل وجود داشت** و تست‌های واحد نگرفته بودندش، چون با
فرمت خط‌تیره صدا زده شده بودند. فقط اسکن یک سال کامل آن را آشکار کرد.

**نتیجه: از ۰ تعطیلی به ۲۶ تعطیلی در سال ۱۴۰۶.**

### تست واقعی
```
✓ lunarHolidays.test.ts (15 tests)
Test Files  9 passed (9)  |  Tests  126 passed (126)
```
تست کلیدی: اثبات جابه‌جایی سالانه (عاشورا در دو سال متوالی تاریخ
متفاوت دارد) — دقیقاً همان چیزی که باگ اصلی نقض می‌کرد.

### ریسک باقی‌مانده
- تاریخ‌های محاسبه‌شده ±۱ روز با اعلام رسمی ایران فرق دارند.
- تعطیلات ملی غیرقمری (مثل ۲۲ بهمن) در جدول ثابت هستند و تأیید شدند،
  ولی تعطیلات مناسبتی موردی (اعلام دولت) قابل محاسبه نیستند.

---

## MOD-FEAT-001 | ۱۴۰۵/۰۶/۰۳ | نمای پیشرفت طرح درمان (AUD-010) | **done ✓**

### درخواست اصلی
«بیمار یک بار می‌آید، ۴۰ تا کار دارد. همه را یک‌جا ثبت کنیم و
جلسه‌به‌جلسه تیک بزنیم و بدانیم چه کاری انجام شده، چه کاری مانده.»

### کشف مهم حین بررسی کد (مرحله ۱)
زیرساخت داده **از قبل موجود بود**: درمان‌ها وضعیت
برنامه‌ریزی‌شده/در حال انجام/تکمیل‌شده دارند. کمبود واقعی **نمایش**
بود — پرونده‌ی بیمار فقط یک فهرست صاف زمانی نشان می‌داد. نمی‌شد در
یک نگاه فهمید چند درصد تمام شده، چقدر هزینه مانده، یا روی کدام
دندان چه چیزی باقی است.

پس مسئله ساختن انبار داده‌ی جدید نبود، آشکار کردن چیزی بود که
از قبل ذخیره می‌شد.

### پیاده‌سازی
- `src/lib/treatmentPlan.ts` — منطق جدا از UI برای قابل تست بودن
- نوار پیشرفت + سه عدد: انجام‌شده / باقی‌مانده / کل طرح
- **گروه‌بندی بر اساس دندان** — واحد کار دندانپزشک، نه ردیف فهرست
- دندان‌های ناتمام **اول** مرتب می‌شوند؛ گروه «عمومی» آخر
- **تیک یک‌ضربه‌ای:** برنامه‌ریزی‌شده ← در حال انجام ← تکمیل‌شده
  بدون باز کردن فرم، برای ثبت سریع حین جلسه
- `role="progressbar"` با `aria-valuenow` برای صفحه‌خوان

### تصمیم‌های طراحی و دلیلشان
- **درصد بر اساس تعداد نه ارزش ریالی:** «۸ از ۱۰ کار» برای بیمار
  معنادارتر از «۷۳٪ ارزش» است، و یک ایمپلنت گران نباید ده ترمیم
  کوچک را در نمودار محو کند.
- **تکمیل‌شده با ضربه‌ی دوباره تغییر نمی‌کند:** برگرداندن باید
  تصمیم آگاهانه باشد نه نتیجه‌ی لمس تصادفی روی گوشی.

### تست واقعی
```
✓ treatmentPlan.test.ts (19 tests)
Test Files  10 passed (10)  |  Tests  145 passed (145)
```
تست کلیدی: **درمان لغوشده نباید شمرده شود** — چون طبق سیاست پروژه
حذف دائمی نداریم، اگر شمرده شود بیمار برای همیشه «کار ناتمام»
خواهد داشت و درصد هرگز به ۱۰۰ نمی‌رسد.

حین پیاده‌سازی، تایپ‌چک سه خطای واقعی گرفت (بقایای کد قدیمی پس از
جایگزینی، براکت تکراری، نام اشتباه تابع بارگذاری) — همه قبل از
commit رفع شدند.

### ریسک باقی‌مانده
- ثبت ۴۰ کار همچنان یعنی ۴۰ بار باز کردن ویزارد درمان. نمای پیشرفت
  ساخته شد ولی **ورود دسته‌ای** هنوز نه — این بخش دوم درخواست است
  و کار جداگانه‌ای می‌خواهد.
- `treatments.phase_id` وجود ندارد، پس درمان‌ها به فازهای طرح درمان
  وصل نمی‌شوند؛ گروه‌بندی فعلاً بر اساس دندان است نه فاز.
- تأیید بصری روی گوشی لازم است.

---

## MOD-FEAT-002 | ۱۴۰۵/۰۶/۰۳ | ورود دسته‌ای طرح درمان | **done ✓**

### مشکل (نیمه‌ی دوم AUD-010)
نمای پیشرفت (MOD-FEAT-001) ردیابی ۴۰ کار را ممکن کرد، ولی **ثبت**
اولیه همچنان یعنی ۴۰ بار باز کردن ویزارد ۴ مرحله‌ای — یعنی همان
مرحله‌ای که کاربر گفت «بیمار یک بار می‌آید» عملاً غیرعملی بود.

### تحلیل (مرحله ۲)
| گزینه | سرعت ۴۰ کار | خطاپذیری | نتیجه |
|---|---|---|---|
| ویزارد فعلی × ۴۰ | ~۱۰ دقیقه | زیاد (خستگی) | رد |
| **سبد: دندان ← رویه ← افزودن** | ~۲ دقیقه | کم | ✅ انتخاب |
| وارد کردن از فایل | سریع ولی نامناسب بالین | زیاد | رد |

**دلیل انتخاب:** دندانپزشک همان‌طور فکر می‌کند — دندان، بعد کار.

### پیاده‌سازی
- `src/lib/bulkTreatment.ts` — منطق جدا از UI برای تست‌پذیری
- انتخاب دندان از چارت پالمر + رویه با قیمت پیش‌فرض
- جمع کل **زنده** + شمارش دندان‌های درگیر
- ویرایش تعداد/قیمت/تخفیف هر قلم داخل سبد
- ذخیره‌ی **ترتیبی** نه موازی: هر ثبت یک عملیات سینک صف می‌کند و
  حفظ ترتیب یعنی صف آفلاین به همان ترتیب ورود پخش می‌شود
- تکراری **هشدار می‌دهد نه مسدود**: دو ترمیم روی یک دندان در دو
  سطح واقعی است، ولی دوبار زدن اتفاقی هم رایج

### 🚨 یافته از دیتابیس زنده
کوئری واقعی نشان داد **هیچ‌کدام از ۴ رویه‌ی موجود `default_price`
ندارند**. بدون اطلاع‌رسانی، سبد قیمت‌ها را صفر پر می‌کرد،
اعتبارسنجی جلوی ثبت را می‌گرفت، و کاربر بدون فهمیدن دلیل گیر می‌کرد.
هشدار صریح در بالای مودال اضافه شد.

### تست واقعی
```
✓ bulkTreatment.test.ts (24 tests)
Test Files  11 passed (11)  |  Tests  169 passed (169)
```
تست‌های کلیدی مالی: **تخفیف بزرگ‌تر از مبلغ عدد منفی نمی‌سازد**
(وگرنه جمع کل سبد کم می‌شد و کلینیک ضرر می‌کرد)، تخفیف از جمع کل
کم می‌شود نه از قیمت واحد، و سبد ۴۰ تایی درست جمع می‌زند.

### ریسک باقی‌مانده
- تأیید بصری روی گوشی لازم است — سبد طولانی روی صفحه‌ی کوچک
  ممکن است نیاز به بهبود پیمایش داشته باشد.
- اقلام با وضعیت «برنامه‌ریزی‌شده» ثبت می‌شوند؛ اگر کاری همان جلسه
  انجام شده باشد باید جداگانه تیک بخورد.

---

## MOD-SEC-002 | ۱۴۰۵/۰۶/۰۸ | قفل جدول بایگانی قدیمی (AUD-002) | **done ✓**

### مشکل
جدول `minadent_entity_records` روی RLS روشن بود ولی **صفر سیاست**
داشت — یعنی برای همه مسدود، بدون اینکه معلوم باشد عمدی است یا فراموش‌شده.

### بررسی واقعی
- **۴۰ ردیف داده** دارد (پس حذف بی‌فکرانه اشتباه بود)
- محتوا: Treatment ۹، Transaction ۸، Staff ۸، Patient ۷، Appointment ۵، Doctor ۳
- آخرین نوشتن: ۳۰ مرداد ۱۴۰۵
- **هیچ فایلی در `src/` به آن ارجاع نمی‌دهد** (بررسی با grep)

**تشخیص:** بازمانده‌ی معماری اولیه — همه‌چیز در یک جدول JSON عمومی.
بعداً به جداول اختصاصی مهاجرت شد و این رها ماند.

### تصمیم
**حذف نشد** (سیاست پروژه حذف دائمی را ممنوع کرده، و این داده ممکن
است برای بازیابی سابقه لازم شود). در عوض:
- `COMMENT` صریح روی جدول: بایگانی، فقط‌خواندنی، کد جدید استفاده نکند
- سیاست `SELECT` برای کاربر لاگین‌کرده
- **هیچ سیاست INSERT/UPDATE/DELETE ساخته نشد** — عمدی
- `REVOKE` مجوزهای نوشتن در سطح Postgres

**دو لایه‌ی مستقل:** سیاست RLS تعیین می‌کند «کدام ردیف‌ها»، ولی GRANT
تعیین می‌کند «آیا اصلاً دسترسی هست». حین تست کشف شد که فقط سیاست
کافی نیست — خطای `permission denied` گرفتیم و مجوز پایه هم لازم بود.

### تست واقعی
| سناریو | نتیجه |
|---|---|
| کاربر لاگین‌کرده ← خواندن | **۴۰ ردیف** ✅ |
| کاربر لاگین‌کرده ← درج | **permission denied** ✅ |

### ریسک باقی‌مانده
اگر روزی این داده‌ی قدیمی واقعاً بی‌فایده تشخیص داده شد، حذفش باید
تصمیم آگاهانه‌ی مهدی باشد نه پاک‌سازی خودکار.

---

## MOD-UI-010 | ۱۴۰۵/۰۶/۰۹ | آیکون‌های رنگی نوار پایین + بازگشت‌پذیری غیرفعال‌سازی | **done ✓**

### ۱. نوار پایین
آیکون‌ها ۲۰ پیکسل بودند — **زیر اندازه‌ای که یک آیکون می‌تواند شکل
قابل‌تشخیص داشته باشد**، پس گلیف‌های سفارشی روی گوشی لکه دیده می‌شدند.
به ۲۵ (غیرفعال) و ۲۸ (فعال) رفتند.

و **هر ماژول رنگ خودش را دارد**، نه فقط فعال. خاکستری بودن بقیه، نوار
را به یک نوار یکنواخت تبدیل می‌کرد؛ رنگ همان چیزی است که ماژول را بدون
خواندن پنج برچسب ۱۰ پیکسلی پیدا می‌کنی.

غیرفعال‌ها با `color-mix` ۶۲٪ رنگ ماژول با خاکستری ترکیب می‌شوند — هویت
رنگی می‌ماند ولی فعال همچنان واضح‌تر است.

### ۲. 🔴 غیرفعال‌سازی یک‌طرفه بود
`MOD-SEC-005` حذف را روی ۱۲ جدول پیکربندی بست — ولی **هیچ راهی برای
فعال‌کردن دوباره از داخل برنامه نگذاشت**.

یعنی اگر کسی اشتباهی یک رویه یا پزشک را غیرفعال می‌کرد، فقط از خود
دیتابیس قابل بازگرداندن بود. **این چیزی نیست که بشود منتشر کرد.**

صفحه‌ی `Archive` از قبل پنج نوع را برمی‌گرداند (بیمار، پرسنل، بیمه،
ایمپلنت، آزمایشگاه)، پس **مسیر دوم ساخته نشد** — یک تب «تنظیمات» به
همان صفحه اضافه شد که پزشک، یونیت، رویه، کالا، پکیج و قالب پیامکِ
غیرفعال‌شده را با دکمه‌ی فعال‌سازی نشان می‌دهد.

### ۳. `START-HERE.md` بازنویسی شد
خواسته‌ی مستقیم مهدی: گفتگوی بعدی باید بداند چه شده و چه مانده.

اضافه شد: جدول ۱۶ نسخه‌ی این جلسه، **فهرست تست دستی در شش بخش**، وضعیت
تأییدشده‌ی دیتابیس، ۱۱۱ کمبود باقی‌مانده، شش بدهی فنی، و سه کاری که
فقط مهدی می‌تواند انجام دهد.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.171.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 28 passed | Tests 547 passed (547)
✓ vite build — موفق
```

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده** — ارتفاع نوار از `h-16` به `h-[4.5rem]`
  رفت؛ اگر روی گوشی کوچک جا نشود باید برگردد.
- تب «تنظیمات» بایگانی **شش نوع** را پوشش می‌دهد. `doctor_schedules`،
  `inventory_categories` و `custom_roles` هنوز رابط بازگردانی ندارند.
- «تمام قابلیت‌های عکس‌ها» اضافه **نشد** — ۱۱۱ مورد باقی است و در
  `START-HERE.md` فهرست شده.

---

## MOD-SEC-005 | ۱۴۰۵/۰۶/۰۹ | غیرفعال‌سازی و ویرایش به‌جای حذف — در کل برنامه | **done ✓**

### خواسته
> «گزینه غیرفعال و ادیت داشته باشند.»

### چرا این فقط نظم نیست
پزشک، رویه، یونیت و آزمایشگاهِ حذف‌شده هنوز توسط **درمان‌ها، نوبت‌ها و
سفارش‌های لابراتواری که قبلاً اتفاق افتاده‌اند** ارجاع می‌شوند.

حذف آن ردیف، تاریخچه‌ای را که به آن اشاره می‌کند حذف **نمی‌کند** — فقط
آن را **ناخوانا** می‌کند. یعنی فاکتور سال گذشته دیگر نمی‌تواند بگوید چه
کاری انجام شد و چه کسی انجامش داد.

### migration 026 — دوازده جدول پیکربندی
پزشکان، پرسنل، رویه‌ها، یونیت‌ها، آزمایشگاه‌ها، بیمه‌ها، کالا و
دسته‌بندی انبار، قالب‌های پیامک، پکیج‌های درمانی، شیفت‌ها، نقش‌ها.

`custom_roles` و `inventory_categories` اصلاً ستون `is_active` نداشتند.
بستن حذف بدون افزودن ستون، **هیچ راهی برای کنار گذاشتنشان باقی
نمی‌گذاشت** — پس اول ستون اضافه شد.

### 🔴 سه چیز که ممیزی گرفت و چشم من ندیده بود

**۱. `expenses` هیچ ستون وضعیتی نداشت.**
migration 023 حذفش را بسته بود ولی نه `status` داشت نه `is_active` — پس
هزینه‌ی اشتباه **اصلاً قابل اصلاح نبود**. migration 027 ستون را افزود.

**۲ و ۳. دو مورد «واگرایی» — بدترین حالت ممکن.**
`deleteToothRecord` و `deleteImplantComponent` فقط ردیف **محلی** را پاک
می‌کردند، در حالی که 023 سرور را از قبل بسته بود.

نتیجه: رکورد روی این دستگاه ناپدید می‌شد، **روی سرور می‌ماند**، و سینک
بعدی برش می‌گرداند — پس کاربر فکر می‌کرد برنامه حرفش را نادیده گرفته.

این را **تست رگرسیون گرفت، نه من.** موقع نوشتن `MOD-SEC-003` فقط صف
سینک را پاک کرده بودم و حذف محلی را جا انداخته بودم.

- دندان به **سالم** برمی‌گردد (دندان همیشه وجود دارد؛ آنچه لغو می‌شود
  یافته‌ی ثبت‌شده روی آن است)
- قطعه‌ی ایمپلنت غیرفعال می‌شود (migration 028 ستون را افزود)

### متن دکمه‌ها هم اصلاح شد
«حذف» روی کنترلی که غیرفعال می‌کند **دروغ** است — دروغی که کاربر فقط
وقتی می‌فهمد که ردیف برگردد. همه‌ی عنوان‌ها، دکمه‌های تأیید و پیام‌های
موفقیت به «غیرفعال کردن» / «غیرفعال شد» تغییر کردند.

### تست رگرسیون گسترش یافت
`recordSafety.test.ts` حالا **۳۶ جدول** را می‌پاید (۲۴ رکورد + ۱۲
پیکربندی) و رد می‌شود اگر کسی حذفی برگرداند — چه به صف سینک، چه فقط
محلی.

### وضعیت نهایی دیتابیس
```
۴۲ جدول، همه با RLS
مسیر حذف باقی‌مانده: ۳
```
هر سه **عمدی**:
- `online_booking_requests` — فرم عمومی، باید ثبت بپذیرد
- `role_permissions` — با نقش مدیریت می‌شود، نه جدا
- `users` — **به احراز هویت وصل است.** دست‌کاری سیاست‌هایش می‌تواند ورود
  را بشکند، و آن از یک مسیر حذف بلااستفاده بدتر است. عمداً دست‌نخورده.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.170.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 28 passed | Tests 547 passed (547)
✓ vite build — موفق
```
۵۴۳ ← **۵۴۷ تست**.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** ردیف‌های غیرفعال باید در فهرست‌ها یا پنهان
  شوند یا کم‌رنگ دیده شوند — بعضی صفحات ممکن است هنوز همه را نشان دهند.
- **راهی برای فعال‌کردن دوباره از داخل برنامه وجود ندارد** برای همه‌ی
  این جدول‌ها. بیمار از بایگانی برمی‌گردد، ولی مثلاً رویه‌ی غیرفعال‌شده
  فعلاً فقط از دیتابیس قابل بازگرداندن است. **این مهم‌ترین کار بعدی
  است.**
- اضافه‌پرداخت ۲۰ میلیونی طبق تأیید مهدی **داده‌ی تستی** بود؛ دست نخورد.

---

## MOD-SEC-004 + MOD-FIX-005 | ۱۴۰۵/۰۶/۰۹ | ممیزی ۳۶۰ درجه روی دیتابیس زنده | **done ✓**

### روش
تست واقعی روی **دیتابیس زنده**، نه ادعا. ۴۲ جدول، سیاست‌ها، کلیدهای
خارجی، و تطابق کد با schema بررسی شد.

### ✅ چیزهایی که سالم بودند
| بررسی | نتیجه |
|---|---|
| رکورد یتیم (۱۰ رابطه) | **صفر** |
| `TABLE_NAMES` در برابر جدول‌های واقعی | **صفر اختلاف** — سینق سالم |
| RLS روی جدول‌های رکورد | ۲۴ جدول، **صفر مسیر حذف** |
| `current_clinic_id()` | موجود |
| سطوح دسترسی | ۵ پرسنل، ۴ نقش، ۱۲۰ ردیف مجوز |

### 🔴 سه ایراد پیدا شد

**۱. `sms_logs` و `notification_logs` با `USING(true)`**
هر کاربر لاگین‌کرده‌ی **هر کلینیکی** می‌توانست بخواندشان — و این ردیف‌ها
**شماره موبایل بیماران و متن پیامک‌های ارسالی** دارند.

هیچ‌کدام ستون `clinic_id` نداشتند، پس نمی‌شد مثل بقیه محدودشان کرد.
به‌جای ساختن ستون و پر کردن کورکورانه‌اش:
- `notification_logs` از طریق `patients.clinic_id` محدود شد
- `sms_logs` هیچ پیوندی ندارد جز یک شماره‌ی خام، پس **دسترسی کلاینت
  کاملاً برداشته شد**. تابع `send-sms` با نقش سرویس می‌نویسد و RLS را
  دور می‌زند، پس ارسال پیامک نمی‌شکند. هیچ‌جای `src/` آن را نمی‌خواند.

**۲. جدول قدیمی `minadent_entity_records` برای همه خواندنی بود**
۴۰ ردیف JSON از سیستم قبلی: بیمار، پرسنل، پزشک، نوبت، درمان، تراکنش —
با **نام، موبایل و ایمیل واقعی**. تنها سیاستش `SELECT USING(true)` بود.

حذف نشد چون سند مهاجرت است و این پروژه رکورد پاک نمی‌کند. دسترسی
کلاینت برداشته شد.

**نتیجه: صفر سیاست خواندن باز.** تنها نوشتن باز باقی‌مانده
`online_booking_requests` است که **عمدی** است — فرم نوبت آنلاین عمومی.

**۳. 🔴 پرداخت بیشتر از بدهی، بی‌صدا ثبت می‌شد**
در داده‌ی زنده:
```
مهدی امیری — بدهی: ۲۲٬۵۰۰٬۰۰۰   پرداخت ثبت‌شده: ۴۲٬۵۰۰٬۰۰۰
```
**۲۰ میلیون اضافه، بدون هیچ هشداری.**

علتش: هشدار قدیمی فقط وقتی می‌آمد که مانده **از قبل صفر** بود. پس
پرداختی که حساب را از ۱۲.۵ میلیون بدهی به ۶.۵ میلیون بستانکار می‌برد،
در سکوت رد می‌شد.

`checkOverpayment()` حالا مبلغ اضافه را دقیق می‌گوید. **هشدار می‌دهد،
مسدود نمی‌کند** — پیش‌پرداخت واقعی است (بیعانه پیش از دوره‌ی درمان،
یکی که پرونده‌ی چند نفر را تسویه می‌کند). اشکال در انجام‌شدنش نبود، در
**دیده‌نشدنش** بود.

### 🟡 کمبودهایی که پیدا شد ولی داده‌ی توست، نه باگ
| یافته | اثر |
|---|---|
| `doctor_schedules` = **۰ ردیف** | **نوار «ساعت‌های خالی» هرگز ظاهر نمی‌شود** و درصد اشغال محاسبه نمی‌شود — هر دو به شیفت وابسته‌اند |
| هر ۴ رویه **بدون قیمت پایه** | هر ثبت درمان قیمت دستی می‌خواهد |
| `payment_plans`, `installments`, `cheques`, `treatment_phases`, `personal_finance_items` = **۰** | این بخش‌ها هرگز با داده‌ی واقعی اجرا نشده‌اند |

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.169.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 28 passed | Tests 543 passed (543)
✓ vite build — موفق
```
۵۳۸ ← **۵۴۳ تست**.

### ریسک باقی‌مانده
- **۲۰ میلیون اضافه‌پرداخت موجود اصلاح نشد.** تصمیم مهدی است که آیا
  داده‌ی تستی بوده یا باید تعدیل شود — من عدد مالی کسی را خودسرانه
  تغییر نمی‌دهم.
- جدول‌های پیکربندی (پزشک، پرسنل، رویه، یونیت، آزمایشگاه، بیمه، انبار،
  قالب پیامک، نقش) **هنوز حذف دائمی دارند**. تصمیم جداگانه می‌خواهد.
- ۵ جدول در دیتابیس هستند ولی سینک نمی‌شوند: `users`, `sms_logs`,
  `notification_logs`, `online_booking_requests`,
  `minadent_entity_records`. برای هیچ‌کدام آفلاین لازم نیست، ولی
  ثبت می‌شود.
- «۳۶۰ درجه کامل» **نیست**. این ممیزی داده و دسترسی بود؛ رفتار رابط
  کاربری همچنان فقط با چشم قابل تأیید است.

---

## MOD-FEAT-018 | ۱۴۰۵/۰۶/۰۸ | کارهای معطل‌مانده + «پیگیری شد» | **done ✓**

### چرا این، و نه ظاهر
> «روند و ثبت و پیگیری و مدیریت اول بشود که استفاده واقعی کنم، بعداً
> ارتقای حرفه‌ای‌تر.»

پس این نسخه سراغ **پیگیری** رفت، نه زیبایی.

### دو شکافی که پیگیری را غیرقابل‌استفاده کرده بود

**۱. هیچ کار بالینی در آن نبود.**
`smartReminders` هفت دسته داشت، ولی همه **بیمار-محور**: تولد، بدهکار،
غایب، قسط، عدم‌حضور. هیچ‌کدام نمی‌گفت:
- روکشی **سه هفته** از موعد لابراتوار گذشته
- ایمپلنتی که **از بهار** در همان مرحله مانده
- مرحله‌ی درمانی که **یک ماه** از برآوردش گذشته

اینها کارهایی‌اند که کلینیک روی‌شان پول و اعتماد از دست می‌دهد، و هیچ
جای برنامه دیده نمی‌شدند.

**۲. 🔴 راهی برای گفتن «انجامش دادم» نبود.**
می‌شد پیامک فرستاد، ولی همان مورد فردا صبح **دقیقاً همان‌طور** برمی‌گشت.
لیستی که پاک نمی‌شود، ظرف یک هفته خوانده نمی‌شود — و آن از نبودن لیست
بدتر است.

### تصمیم: تعویق، نه حذف
«پیگیری شد» مورد را **سه روز** پنهان می‌کند، نه برای همیشه.

حذف دائمی برای کار بالینی غلط است: روکش بعد از اینکه تیک «به لابراتوار
زنگ زدم» را زدی **هنوز نرسیده**، و کل نکته این است که اگر چیزی عوض نشد
برگردد. تعویقی که منقضی می‌شود، لیست را صادق نگه می‌دارد.

### ذخیره‌سازی — عمداً جدول جدید نساختم
تعویق‌ها در `localStorage` می‌مانند. افزودن جدول به `TABLE_NAMES` پیش
از migration همان چیزی است که یک بار **کل حلقه‌ی سینک را از کار
انداخت**. و «زنگ زدم» یک وضعیت رابط کاربری روی همین دستگاه است، نه
رکورد بالینی.

### 🔴 باگی که پیش از رسیدن به کاربر گرفته شد
اول ستون را `due_date` نوشتم. ستون واقعی `deadline` است.

اگر می‌ماند، پیگیری لابراتوار **بی‌صدا خالی** می‌ماند — بدترین نوع
خرابی برای یک لیست کار، چون شبیه «کاری نیست» به نظر می‌رسد. با خواندن
schema پیش از وصل کردن پیدا شد.

### صبر متفاوت برای هر مرحله
| مرحله | مهلت |
|---|---|
| برنامه‌ریزی | ۶۰ روز |
| جراحی انجام شد | ۳۰ روز |
| **در حال بهبود** | **۲۴۰ روز** |
| قالب‌گیری | ۲۱ روز |
| تحویل روکش | ۱۴ روز |

بهبود واقعاً ماه‌ها طول می‌کشد. علامت‌زدن هر ایمپلنت در حال بهبود،
مواردی را که واقعاً فراموش شده‌اند **دفن** می‌کرد.

### کارت جداگانه، عمداً
زنگ زدن به لابراتوار کار دیگری است از زنگ زدن به بیمار. قاطی کردنشان
باعث می‌شد **هیچ‌کدام** تا آخر انجام نشوند.

### پس‌زمینه پررنگ‌تر شد
طبق درخواست: حوضچه‌ها از ۲۲/۱۸/۱۶٪ به ۳۴/۳۰/۲۶٪، سطوح از ۶/۵/۷٪ به
۱۱/۹/۱۲٪، پایه‌ی بدنه از ۶۰٪ به ۹۵٪ رنگ ماژول.

سطوح **زیر ۱۶٪** ماندند. حوضچه‌ها پشت کارت‌ها هستند نه زیر متن، پس
می‌توانند پررنگ‌تر باشند.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.168.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 28 passed | Tests 538 passed (538)
✓ vite build — موفق
```
۵۱۶ ← **۵۳۸ تست**.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده** (مهدی گفت فعلاً نمی‌تواند عکس بفرستد).
- تعویق **روی همین دستگاه** است. اگر منشی روی گوشی خودش پیگیری کند،
  روی گوشی مهدی هنوز دیده می‌شود. اشتراکی کردنش جدول و migration
  می‌خواهد.
- ایمپلنت به `updated_at` تکیه می‌کند، که هر ویرایشی آن را جلو می‌برد —
  اصلاح یک غلط املایی، ساعت را صفر می‌کند. ستون `stage_changed_at`
  دقیق‌تر بود ولی migration می‌خواهد.
- «یکپارچگی همه‌جا» و «سطح اینترپرایز» انجام **نشده**. این یک ماژول
  است، نه بازطراحی.

---

## MOD-UI-009 | ۱۴۰۵/۰۶/۰۸ | انتخاب سطح دندان با یک ضربه (COMP-103) | **done ✓**

### مشکل
ثبت اینکه کدام **سطح** درگیر است، پنج ضربه می‌خواست: باز کردن دندان،
صبر برای پنجره، پیدا کردن ردیف سطح، ضربه، بستن.

معاینه با این سرعت پیش نمی‌رود. چارت باید به سرعت معاینه خوانده و
علامت زده شود.

### راه‌حل
همان الگویی که در عکس‌های خودت بود: یک هدف پنج‌قسمتی مستقیم **زیر**
دندان. یک ضربه از روی قوس.

هندسه در `src/lib/surfaceGlyph.ts` جدا شد تا هم قابل تست باشد و هم
چارت و پنجره‌ی جزئیات **یک** هدف بکشند، نه دو تا که با هم فاصله
می‌گیرند.

### 🔴 نکته‌ای که اشتباه کردنش رکورد را غلط می‌کند
**مزیال به سمت خط وسط است** — پس روی ربع‌های راستِ بیمار سمت راستِ
صفحه، و روی ربع‌های چپ سمت چپ.

اگر برعکس کشیده شود، ترمیم روی **سطح اشتباه دندان** ثبت می‌شود. برای
همین جهت از شماره‌ی FDI مشتق می‌شود نه فرض، و تست دارد که حرف `M` بعد
از آینه‌شدن با سطح مزیال بماند.

مرکز به‌جای گُوِه، **دیسک** است: سطح جونده بیشترین چیزی است که علامت
می‌خورد و راحت‌ترین هدف برای انگشت شست.

و روی دندان قدامی حرف مرکز `I` است نه `O` — دندان جلو با **لبه** گاز
می‌گیرد نه با سطح. چارت باید به زبان کسی حرف بزند که می‌خواندش.

### دو تصمیم که عمدی‌اند
**۱. فقط زیر دندان انتخاب‌شده.** شانزده گلیف در یک قوس روی عرض گوشی
خوانا نیست، و خودِ دندان‌ها را سخت‌خوان می‌کند — دقیقاً همان عیبی که
`MOD-UI-006` تازه رفع کرد.

**۲. از همان مسیر ذخیره می‌نویسد.** `onUpdateTooth` مثل هر ویرایش
دیگری، نه یک مسیر دوم. «دو مسیر برای یک مقصد» عیبی است که این پروژه
بارها از آن ضربه خورده.

علامت‌زدن اولین سطح، خود دندان را هم از «سالم» بیرون می‌آورد — سطح
نمی‌تواند روی دندان سالم علامت داشته باشد.

### هم‌راستایی رنگ
`conditionMeta` حالا هم کلاس تِیلویند دارد هم مقدار hex. یک جدول، پس
گلیف و راهنمای رنگ **هرگز** نمی‌توانند سر معنی یک رنگ اختلاف پیدا کنند.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.167.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 27 passed | Tests 516 passed (516)
✓ vite build — موفق
```
۵۰۴ ← **۵۱۶ تست**.

### وضعیت ممیزی
از ۱۹۵ قابلیت ثبت‌شده: **۱۱۱ هنوز ❌ و ۶۰ مورد 🟡**. این یکی
`COMP-103` را بست.

### ریسک باقی‌مانده
- **تأیید بصری لازم است.** گلیف ۳۶ پیکسل است؛ پنج هدف در آن اندازه
  ممکن است برای انگشت کوچک باشد. اگر ضربه‌ها خطا می‌خورند باید بزرگ‌تر
  شود یا فقط با فشار طولانی باز شود.
- سطح، وضعیت **خود دندان** را می‌گیرد؛ سطحی با وضعیت متفاوت از دندان
  فقط از پنجره‌ی جزئیات قابل ثبت است.
- `COMP-104` (چند رنگ حاشیه هم‌زمان با معانی مختلف) و `COMP-105`
  (حالت فقط‌خواندنی) هنوز ساخته نشده‌اند.

---

## MOD-UI-008 | ۱۴۰۵/۰۶/۰۸ | پس‌زمینه‌ی زنده‌ی هر ماژول — سفید خالی ممنوع | **done ✓**

### خواسته
> «هر بخش رنگ خودش، متحرک و متغیر از سفید تا همان رنگ، مثل پس‌زمینه‌ی
> جمنای، برای همه‌ی بخش‌ها و کارت‌ها و چیپ‌ها. سفید خالی ممنوع.»

### تصمیم اول: پالت جدید اختراع نشد
پروژه از قبل `src/theme/modules.ts` را دارد که به ازای هر مسیر
`--module-color` را روی `<html>` می‌گذارد — نوبت‌دهی کهربایی، درمان
سرخابی، مالی سبز، بیماران بنفش.

آن رنگ‌ها تا امروز فقط در آیکون‌ها و چند حاشیه دیده می‌شدند. این تغییر
**رنگی نساخت، رنگی را که برنامه از قبل داشت قابل دیدن کرد.**

### پس‌زمینه
سه حوضچه‌ی شعاعی بسیار ملایم روی پایه‌ی سفید، هرکدام با سرعت متفاوت
(۲۶ و ۳۴ ثانیه) جابجا می‌شوند. حرکت هست ولی هیچ لبه‌ای از روی متن رد
نمی‌شود.

تغییر رنگ هنگام جابجایی بین ماژول‌ها **محو** است نه پرش — هویت ماژول
باید حس «عوض شدن اتاق» بدهد، نه رنگ‌شدن دوباره‌ی صفحه.

### سطوح
| سطح | قبل | حالا |
|---|---|---|
| کارت | `#ffffff` تخت | گرادیان ۶٪ رنگ ماژول با شیب نور |
| کارت آمار | سفید ۹۶٪ | گرادیان ۱۰٪ + حاشیه‌ی هم‌رنگ |
| چیپ | `#fff` | ۷٪ رنگ ماژول + حاشیه‌ی ۱۸٪ |
| هر `bg-white` دیگر | سفید | ۵٪ رنگ ماژول |

آخری عمدی است: ده‌ها کامپوننت مستقیم از `bg-white` تِیلویند استفاده
می‌کنند. به‌جای ویرایش تک‌تکشان، **خود کلاس** ته‌رنگ گرفت. هیچ سطحی
سفید خالی نمی‌ماند بدون اینکه ۲۴ فایل دست بخورد.

### 🔴 دو مرزی که رد نشد

**۱. کنتراست.** همه‌ی ته‌رنگ‌ها زیر ۱۴٪ ماندند. `MOD-UI-004` کنتراست
WCAG 2.2 را قفل کرده، و پس‌زمینه‌ای که با کلمات رقابت کند دقیقاً همان
چیزی را خراب می‌کند که قرار بود بهتر کند.

**۲. حرکت.** `prefers-reduced-motion` رعایت شد — **رنگ می‌ماند، حرکت
متوقف می‌شود**. پس‌زمینه‌ی دائماً متحرک همان چیزی است که علائم
وستیبولار را تحریک می‌کند، و این یکی اختیاری نیست.

`fill="white"` داخل SVG دست‌نخورده ماند: آنجا سفید کار واقعی می‌کند —
برق روی تاج دندان.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.166.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 26 passed | Tests 504 passed (504)
✓ vite build — موفق
```

### ریسک باقی‌مانده
- **تأیید بصری لازم است، و این مورد بیش از همه.** یک تغییر ظاهری
  سراسری است که هیچ تستی نمی‌تواند قضاوتش کند — همان درسی که
  `MOD-UI-006` داد.
- `color-mix()` و `:not()` پیچیده نیاز به مرورگر مدرن دارند. اگر
  پشتیبانی نشود، قانون کنار گذاشته می‌شود و سطح **سفید** می‌ماند —
  یعنی شکست بی‌خطر، نه ظاهر خراب.
- شدت ته‌رنگ‌ها با چشم انتخاب شد، نه با اندازه‌گیری کنتراست. اگر جایی
  کم‌رنگ یا پررنگ است باید عدد واقعی گرفته شود.
- حالت تاریک از قبل سطوح خودش را دارد و دست نخورد.

---

## MOD-UI-007 | ۱۴۰۵/۰۶/۰۸ | چهار انتخاب که چیزی را که می‌دانستند نمی‌گفتند | **done ✓**

### مشکل مشترک
چهار کنترل یک عیب داشتند: **همه‌چیز را یکسان و معتبر نشان می‌دادند** و
عاقبتش را یا بعداً نشان می‌دادند یا اصلاً نه. تمام این اطلاعات در لحظه‌ی
انتخاب قابل دانستن بود.

### ۱. انتخاب پزشک
پزشکی که آن روز هفته **کار نمی‌کند** عین بقیه در لیست بود. حالا کنارش
نوشته می‌شود «این روز کار نمی‌کند»، و برای بقیه ساعت کاری‌شان.

**علامت می‌زند، حذف نمی‌کند.** کلینیک واقعاً خارج از ساعت اعلام‌شده
نوبت می‌دهد — اورژانس، لطف، شیفتی که کسی در تنظیمات به‌روز نکرده. و
پزشکی که هیچ برنامه‌ای ثبت نشده «در دسترس» حساب می‌شود، وگرنه خالی
بودن تنظیمات کل لیست را خاکستری می‌کرد.

### ۲. انتخاب یونیت
**این همان شکافی بود که در MOD-UI-005 باز ماند.** نوار ساعت‌های خالی
فقط پزشک انتخابی را چک می‌کرد، پس دو پزشک می‌توانستند هم‌زمان یک ساعت
را خالی ببینند در حالی که **یک صندلی** بیشتر نبود.

حالا یونیت اشغال با ساعت تداخلش نشان داده می‌شود.

### ۳. انتخاب رویه
ریسکی که در `START-HERE` ثبت شده بود: رویه‌های بدون `default_price`
فیلد قیمت را خالی می‌گذاشتند بدون هیچ توضیحی.

حالا می‌گوید **چرا** خالی است و کجا باید اضافه شود. ولی **همچنان خالی
می‌ماند** — صفرِ از پیش پرشده مثل یک قیمت واقعی به نظر می‌رسد، و این
همان‌طوری است که یک درمان رایگان ثبت می‌شود. آزاردهنده بودن اینجا
نتیجه‌ی درست است.

### ۴. انتخاب بیمار
بدهی و هشدار بالینی هر دو از قبل روی رکورد بودند و **هیچ‌کدام به لیست
نمی‌رسیدند**. حالا در همان ردیف: چیپ کهربایی بدهی، چیپ قرمز حساسیت و
دارو.

اولی به کلینیک پول ضرر می‌زند؛ دومی همان دلیلی است که کارت‌های هشدار
`MOD-FEAT-011` وجود دارند — و کسی که نوبت می‌دهد باید **قبل** ببیندش،
نه بعد.

برای این کار صفحه‌ی نوبت‌دهی حالا درمان و پرداخت را هم می‌خواند.
خواندن از IndexedDB محلی است، پس هزینه‌اش کم است؛ جایگزینش این بود که
برای بیماری نوبت بگذاری بدون هیچ نشانه‌ای از اینکه به کلینیک بدهکار است.

### ۵. دو فرمی که یکی به نظر می‌رسیدند
«شروع درمان مستقیم» و «ویزیت جدید» یک Modal با دو حالت‌اند — پس دو
مسیر نیستند. ولی روی صفحه **یک فرم سفید با یک کلمه تفاوت** بودند، در
حالی که به دو جای واقعاً متفاوت می‌روند.

طبق توضیح مهدی: ویزیت = **مرور کامل دهان و جمع‌آوری کارهایی که باید
انجام شود**؛ شروع مستقیم = **ثبت یک درمان**.

حالا هر حالت نوار رنگی، آیکون و یک خط دارد که می‌گوید با فشردن دکمه چه
اتفاقی می‌افتد.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.165.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 26 passed | Tests 504 passed (504)
✓ vite build — موفق
```
۴۸۵ ← **۵۰۴ تست**.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.**
- **رنگی کردن پس‌زمینه‌ی کل صفحه‌ها هنوز انجام نشده.** مهدی گفت
  صفحه‌ها نباید سفید خالی باشند. اینجا فقط دو حالت Modal رنگ گرفتند؛
  تم کلی یک کار جداگانه است و نیاز به تصمیم درباره‌ی پالت دارد.
- مرخصی و تعطیلی پزشک هنوز لحاظ نمی‌شود، فقط برنامه‌ی هفتگی.
- تداخل یونیت در برچسب دیده می‌شود ولی **مسدود نمی‌کند** — بررسی سخت
  همچنان در `checkConflict` هنگام ثبت است.

---

## MOD-UI-006 | ۱۴۰۵/۰۶/۰۸ | 🔴 چارت دندانی تقریباً نامرئی بود | **done ✓**

### گزارش مهدی
اسکرین‌شات از خود اپ (v1.162.0). دندان‌ها **کاسه‌های محو** بودند —
بدون ریشه، بدون آناتومی، بدون هیچ سطحی.

**این اولین باری بود که خود برنامه را دیدم.** و چیزی که دیدم با آنچه
کد به نظر می‌رسید انجام می‌دهد یکی نبود.

### علت ریشه‌ای — سه چیز روی هم
**۱. کنتراست تقریباً صفر.** تاج سفید `#ffffff` با خط دور `#cbd5e1`
روی کارت سفید. نسبت کنتراست حدود **۱.۳:۱** — پایین‌تر از هر چیزی که
انسان بتواند در ۴۸ پیکسل روی گوشی، از فاصله‌ی دست، در نور مطب تشخیص
دهد.

**۲. ریشه‌ها با شفافیت ۰.۵ کشیده می‌شدند.** با همان رنگ کم‌رنگ، عملاً
ناپدید. برای همین دندان‌ها «کاسه» به نظر می‌رسیدند: تاج بود، ریشه نبود.

**۳. دندان‌های قدامی هیچ سطحی نداشتند.** ثنایا و نیش **هیچ** سطحی
رندر نمی‌کردند — یعنی ثنایای با پوسیدگی مزیال دقیقاً شبیه ثنایای سالم
بود. و قدامی‌ها بیشترِ چیزی هستند که روی عرض گوشی دیده می‌شود.
مولر و پرمولر هم فقط سطح **جونده** را نشان می‌دادند، پس ترمیم باکال
آنجا هم نامرئی بود.

### درسی که اینجا گرفتم
`MOD-FEAT-013` شکل دندان را بر اساس وضعیت درست کرد و ۱۷ تست هم داشت.
همه‌ی تست‌ها پاس بودند. ولی **هیچ تستی نمی‌تواند بگوید یک عنصر روی
صفحه دیده نمی‌شود.**

منطق درست بود و نتیجه غیرقابل استفاده. این دقیقاً همان چیزی است که در
«آنچه هرگز ادعا نمی‌شود» نوشته شده — و تا وقتی مهدی عکس نفرستاد، من
نمی‌دانستم.

### راه‌حل
- خط دور یک پله تیره‌تر (`#64748b` برای سالم، و همه‌ی رنگ‌های وضعیت
  یک درجه اشباع‌تر)
- تاج روی ته‌رنگ عاجی می‌نشیند نه سفید خالص — دندان کم‌رنگ است، ولی
  باید **جسم** به نظر برسد
- ریشه از ۰.۵ به ۰.۸۵ شفافیت
- ضخامت خط از ۱.۵ به ۱.۹
- **هر پنج سطح روی هر سه نوع دندان** رندر می‌شود: جونده/برنده،
  مزیال، دیستال، باکال، لینگوال — با همان چیدمانی که انتخابگر سطح
  برچسب می‌زند

سطحی که چیزی رویش ثبت نشده **کاملاً نامرئی** می‌ماند (`opacity 0`).
رنگ‌کردن کم‌رنگِ همه‌ی سطوح، دندان سالم را به یک وصله‌کاری تبدیل
می‌کرد.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.164.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 25 passed | Tests 485 passed (485)
✓ vite build — موفق
```

### ریسک باقی‌مانده
- **دوباره باید دیده شود.** من هنوز نمی‌توانم صفحه را ببینم؛ این اصلاح
  بر اساس اعداد کنتراست و اسکرین‌شات مهدی است، نه مشاهده‌ی مستقیم.
- نسبت کنتراست جدید محاسبه نشده، تخمین زده شده. اگر هنوز کم‌رنگ است
  باید عدد واقعی اندازه‌گیری شود.
- تعداد ریشه هنوز آناتومیک نیست (مولر بالا سه ریشه دارد، دو تا کشیده
  می‌شود).
- گلیف انتخاب سطح **زیر** هر دندان (`COMP-103`) هنوز ساخته نشده؛ سطوح
  فعلاً فقط روی خود دندان دیده می‌شوند.

---

## MOD-UI-005 | ۱۴۰۵/۰۶/۰۸ | 🔴 «ساعت‌های خالی» خالی نبودند — و ریل نداشت | **done ✓**

### گزارش مهدی
> «در ثبت نوبت‌دهی، انتخاب زمان با اسکرول پایین صفحه چپ و راست هم
> می‌شود، ریل نداره.»

**اولین گزارش از دستگاه واقعی در این جلسه.** و وقتی رفتم سراغش، مشکل
ریل کوچک‌ترین بخشش بود.

### 🔴 مشکل اصلی: عنوان دروغ می‌گفت
نوار ۲۶ ساعت **hard-code شده** بود، زیر عنوان «ساعت‌های خالی».

- برای **هر پزشکی** یک لیست
- برای **هر روزی** یک لیست
- برنامه‌ی کاری پزشک را نادیده می‌گرفت
- نوبت‌های **از قبل رزروشده** را نادیده می‌گرفت

یعنی پرکاربردترین کنترل برنامه چیزی می‌گفت که درست نبود، و راه فهمیدنش
این بود که **دو مرحله بعد** هشدار تداخل بگیری — بعد از اینکه انتخاب
کرده بودی.

### 🔴 باگ دوم: ساعت پایان «۲۴:۰۰» می‌ساخت
```js
end_time: `${String(parseInt(t.split(':')[0])+1).padStart(2,'0')}:00`
```
`۲۳:۳۰` را که می‌زدی، `۲۴:۰۰` تولید می‌شد — که ساعت واقعی نیست. و
چون رشته‌ی `"24:00"` از هر چیزی بزرگ‌تر است، تست «پایان بعد از شروع»
را هم **بی‌سروصدا رد می‌کرد**.

### مشکل ریل
`flex-wrap` با ۲۶ دکمه → چند ردیف ناهموار که صفحه در **هر دو محور**
از میانشان رد می‌شد. چیزی نبود که انگشت دنبالش کند.

حالا: یک ردیف، یک محور، `scroll-snap` روی هر چیپ، و **نوار قابل دیدن**
— ریلی بدون لبه مثل شبکه‌ای است که تصادفی بریده شده.

### راه‌حل هوشمند
`src/lib/timeSlots.ts` ساعت‌ها را از **برنامه‌ی واقعی کاری** همان پزشک
در همان روز هفته می‌سازد و هرکدام را با وضعیت واقعی علامت می‌زند:

- **رزروشده** → غیرفعال، خط‌خورده
- **گذشته** → غیرفعال، ولی فقط اگر امروز باشد (خاکستری کردن صبحِ هفته‌ی
  بعد بی‌معناست)
- ساعتی که **تمام نوبت در آن جا نشود** اصلاً پیشنهاد نمی‌شود — ۱۷:۴۵
  برای نوبت نیم‌ساعته در شیفتی که ۱۸:۰۰ تمام می‌شود، دعوت به رزروی است
  که از ساعت تعطیلی می‌گذرد
- نوبت نباید وارد **زمان استراحت** شود
- شمارنده: «ساعت‌های خالی (۷ از ۱۶)»
- اگر ساعت انتخابی قابل رزرو نباشد، **نزدیک‌ترین ساعت خالی پیشنهاد
  می‌شود** — نه اینکه دو مرحله بعد بفهمی
- اگر شیفتی ثبت نشده باشد، **می‌گوید چرا** و ورود دستی را پیشنهاد
  می‌کند، به‌جای نمایش لیست جعلی

و طول نوبت با جابجایی ساعت شروع **حفظ** می‌شود.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.163.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 25 passed | Tests 485 passed (485)
✓ vite build — موفق
```
۴۶۰ ← **۴۸۵ تست**.

نکته‌ی روند: `firstBookableSlot` اول مصرف‌کننده نداشت — به پیشنهاد
نزدیک‌ترین ساعت وصل شد، نه اینکه به‌عنوان ریسک ثبت شود.

### ریسک باقی‌مانده
- **تأیید بصری لازم است** — به‌ویژه اینکه snap روی گوشی مهدی درست
  عمل کند و نوار قابل دیدن باشد.
- «رزروشده» فقط نوبت‌های همان پزشک را حساب می‌کند. اگر کلینیک با یونیت
  محدود کار کند، دو پزشک می‌توانند هم‌زمان رزرو شوند و اینجا خالی
  نشان داده می‌شود. بررسی تداخل یونیت جداگانه در `checkConflict` هست
  ولی در نوار منعکس نمی‌شود.
- مرخصی و تعطیلی پزشک لحاظ نمی‌شود، فقط برنامه‌ی هفتگی.

---

## MOD-FEAT-017 | ۱۴۰۵/۰۶/۰۸ | ایمپلنت — سهم جراح، ترتیب مراحل و اعتبارسنجی | **done ✓**

### مشکل
ایمپلنت گران‌ترین چیزی است که کلینیک می‌فروشد و طولانی‌ترین. ولی:

**۱. محاسبه‌ی سهم جراح داخل `Implants.tsx` بود و تست نداشت** — و
**گرد نمی‌شد**. `net / 2` برای مبلغ فرد، **نیم تومان** تولید می‌کرد که
مستقیم در یک فیلد ارزی و در هر جمعی که از آن تغذیه می‌شد می‌نشست.

**۲. ترتیب مراحل هیچ قانونی نداشت.** پرونده می‌توانست از
«برنامه‌ریزی شده» مستقیم به «تکمیل شده» بپرد — یعنی پرونده‌ای که ادعا
می‌کند روکش تحویل شده در حالی که هیچ جراحی‌ای پشتش ثبت نشده. همان
پرونده‌ای که سال‌ها بعد در دعوای گارانتی به آن استناد می‌شود.

**۳. تاریخ‌ها بررسی نمی‌شدند.** تحویل روکش می‌توانست قبل از جراحی ثبت
شود. این تاریخ‌ها بازه‌ی گارانتی و فاصله‌ی بهبود را تعیین می‌کنند.

**۴. مبلغ منفی پذیرفته می‌شد.**

### راه‌حل
`src/lib/implants.ts` — منطق خالص و تست‌شده. نسخه‌ی تکراری داخل صفحه
**حذف شد**، چون دو مسیر برای یک مقصد ممنوع است.

### تصمیم‌ها

**گرد کردن به تومان کامل.** پول کلینیک کسری پرداخت نمی‌شود.

**فیکسچر همیشه از کسر خارج است** — بدون قید و شرط، حتی اگر تیک
«کسر در سهم جراح» خورده باشد. اینجا گذاشتنش یعنی یک تیک اشتباه
نمی‌تواند بی‌سروصدا حقوق جراح را عوض کند.

**سهم هرگز منفی نمی‌شود.** اگر اقلام گران‌تر از قیمت فروش پرونده باشد،
کلینیک جذبش می‌کند. سهم منفی یعنی از جراح بابت افتخار عمل کردن پول
بگیریم.

**پریدن ممنوع، عقب رفتن آزاد.** پرسنل اشتباه لمس می‌کنند؛ مجبور کردنشان
به تحمل یک مرحله‌ی غلط، همان‌طوری است که یک سابقه از اعتبار می‌افتد.
«ناموفق» از هر مرحله‌ای ممکن است و فقط به «برنامه‌ریزی شده» برمی‌گردد.

**پنجره‌ی تسویه حالا کل تقسیم را نشان می‌دهد** — سهم پروتز، سهم کلینیک،
و **مانده‌ی بیمار**. پرداخت سهم از پرونده‌ای که بیمار هنوز پولش را نداده
یک تصمیم است و باید با دیدن مانده گرفته شود، نه بعد از آن.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.162.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 24 passed | Tests 460 passed (460)
✓ vite build — موفق
```
۴۳۰ ← **۴۶۰ تست** (۳۰ تست جدید).

بررسی کد یتیم: `caseFinancials` اول هیچ مصرف‌کننده‌ای نداشت — به
پنجره‌ی تسویه وصل شد، نه اینکه به‌عنوان «ریسک باقی‌مانده» ثبت شود.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.**
- پرونده‌های **موجود** با مرحله‌ی پریده اصلاح نمی‌شوند؛ قانون فقط از
  این به بعد اعمال می‌شود.
- سهم جراح از `total_cost` حساب می‌شود، نه از پول واقعاً دریافت‌شده.
  اگر بیمار فقط نصف داده باشد، سهم کامل محاسبه می‌شود. این تصمیم
  کلینیک است نه باگ، ولی حالا مانده کنارش دیده می‌شود.
- `COMP-162/163` (درصد پزشک از هزینه‌ی لابراتوار و مواد و مالیات) هنوز
  ساخته نشده.

---

## MOD-FEAT-016 | ۱۴۰۵/۰۶/۰۸ | مراحل درمان — پیشرفت، تأخیر و اعتبارسنجی مسدودکننده | **done ✓**

### مشکل
تب مراحل درمان می‌توانست فاز بسازد و ویرایش کند، ولی **هیچ‌چیز درباره‌ی
آن‌ها نمی‌دانست**. نمی‌گفت طرح نصفه است، فازی از برآوردش گذشته، یا
برآوردها دیگر با آنچه واقعاً ثبت شده نمی‌خواند. این سه دقیقاً همان
سؤالی است که بیمار می‌پرسد.

این آخرین حلقه‌ی دست‌نخورده‌ی زنجیره‌ی «نوبت → مراحل → اقساط → چک» بود.

### 🔴 اعتبارسنجی که فقط اجازه می‌داد
سه چیز از قبل **پذیرفته می‌شد**:
- **شماره فاز تکراری**
- **تاریخ پایان قبل از تاریخ شروع**
- **هزینه یا مدت منفی**

فازی که قبل از شروعش تمام می‌شود، هر محاسبه‌ی مدت و هر شمارش تأخیر را
در ادامه بی‌معنا می‌کند. حالا `validatePhase()` **مسدود می‌کند**، نه
هشدار — طبق ممنوعیت «فیلد اجباری که فقط هشدار می‌دهد».

### باگ: فاز لغوشده بدهی حساب می‌شد
شمارش قدیمی `phases.length` بود. یعنی طرحی که تنها فاز باقی‌مانده‌اش
لغو شده بود **هرگز به ۱۰۰٪ نمی‌رسید** و هزینه‌ی فاز لغوشده هم در
برآورد کل می‌ماند. حالا `livePhases()` آن‌ها را کنار می‌گذارد.

### باگ: `nextPhaseNumber` می‌توانست تکراری بدهد
منطق قدیمی max+1 بود که درست است، ولی تست نشده بود. حالا تست دارد که
`count + 1` نباشد: طرحی با فازهای ۱ و ۵، عدد ۳ نمی‌گیرد.

### تصمیم‌های طراحی
- **هزینه‌ی پیش‌بینی‌شده** از هزینه‌ی واقعی می‌آید، و برای فازهایی که
  هنوز هزینه نخورده‌اند از برآورد. بدون این، طرح نیمه‌هزینه‌شده تقریباً
  رایگان خوانده می‌شود — دقیقاً وقتی که داری قیمت می‌دهی.
- **فاز بدون تاریخ شروع «شروع‌نشده» است، هرچه وضعیتش بگوید.** ردیف
  «در حال انجام» بدون تاریخ شروع یک اشتباه ورود داده است؛ «به‌موقع»
  نامیدنش آن را پنهان می‌کند.
- **تاریخ پایان صریح بر برآورد مدت می‌چربد** — کسی عمداً تایپش کرده.
- **مقایسه‌ی هزینه گزارش می‌دهد، اصلاح نمی‌کند.** طرح نیمه‌اجراشده
  به‌درستی زیر برآورد می‌ماند، پس این اطلاعات است نه خطا.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.161.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 23 passed | Tests 430 passed (430)
✓ vite build — موفق
```
۴۰۲ ← **۴۳۰ تست** (۲۸ تست جدید).

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** نشان «روز تأخیر» روی کارت فاز.
- محاسبه‌ی مدت **میلادی** است، مثل اقساط. همان بدهی فنی.
- ترتیب فازها اجبار نمی‌شود: فاز ۳ می‌تواند قبل از فاز ۲ تکمیل شود.
  این عمدی است — بالینی واقعاً پیش می‌آید — ولی یعنی «۵۰٪ تکمیل» صرفاً
  شمارش است، نه پیشرفت زمانی.
- فازها به درمان‌های مشخص وصل نیستند؛ مقایسه‌ی هزینه در سطح کل پرونده
  است، نه فاز به فاز.

---

## MOD-FEAT-015 | ۱۴۰۵/۰۶/۰۸ | اقساط و چک — محاسبه‌ی تست‌شده و رفع باگ تاریخ | **done ✓**

### مشکل
ریاضیاتی که تعیین می‌کند بیمار هر ماه چقدر بدهکار است، **داخل یک
`onClick` در `Billing.tsx`** بود. یعنی قابل تست نبود. برای پولِ کلینیک
این کافی نیست.

### 🔴 باگ واقعی: سررسید قسط یک ماه می‌پرید
کد قدیمی:
```js
const dueDate = new Date(startDate)
dueDate.setMonth(dueDate.getMonth() + i)
```
`setMonth` سرریز می‌کند. طرحی که **۳۱ ژانویه** شروع می‌شد، قسط دومش
روی **۳ مارس** می‌افتاد — چون فوریه ۳۱ ندارد و جاوااسکریپت جلو می‌رود.

یعنی سررسیدی که بیمار امضا کرده، بی‌سروصدا جابجا می‌شد. این یک اختلاف
واقعی سر پیشخوان است.

اصلاح: تاریخ‌ها دستی ساخته می‌شوند و روز به آخرین روز آن ماه **گیر**
می‌کند، نه اینکه جلو برود. تست: `۲۰۲۶-۰۱-۳۱` →
`۰۱-۳۱`, `۰۲-۲۸`, `۰۳-۳۱`. سال کبیسه هم تست شد.

### ناوردای پول: جمع اقساط باید **دقیقاً** برابر مبلغ طرح باشد
۱٬۰۰۰٬۰۰۰ تقسیم بر ۳ می‌شود ۳۳۳٬۳۳۳ — سه تا از آن یک تومان کم می‌آورد.
باقی‌مانده روی **قسط آخر** می‌نشیند، نه اول: بیمار قسط اول را همان
لحظه می‌بیند، و عددی که با آنچه گفته‌ای نخواند دعوا شروع می‌کند.

تست روی ۶ مبلغ × ۲۴ تعداد قسط اجرا می‌شود و جمع را دقیق چک می‌کند.

### مغایرت‌سنجی طرح
اگر کسی بعد از ساخت طرح یک قسط را ویرایش کند، سطرها و سربرگ طرح با هم
نمی‌خوانند. `reconcilePlan()` **اختلاف را گزارش می‌دهد و خودش هیچ‌کدام
را اصلاح نمی‌کند** — کدام عدد درست است تصمیم کلینیک است، و تابعی که
بی‌صدا یکی را انتخاب کند یک اختلاف مالی را پنهان می‌کند. روی کارت طرح
یک خط کهربایی نشان داده می‌شود.

### دو اصلاح دیگر
| مشکل | اصلاح |
|---|---|
| پنجره‌ی تأیید «مبلغ هر قسط» را گرد می‌کرد — عددی که قسط آخر با آن نمی‌خواند | حالا اگر آخری فرق داشته باشد، هر دو نشان داده می‌شود |
| نوار پیشرفت، اقساط **لغوشده** را هم بدهی حساب می‌کرد | `planProgress()` آن‌ها را کنار می‌گذارد |

روی کارت طرح، «قسط سررسید گذشته» و «قسط بعدی» هم اضافه شد.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.160.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 22 passed | Tests 402 passed (402)
✓ vite build — موفق
```
۳۸۱ ← **۴۰۲ تست**.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.**
- **تاریخ‌ها میلادی محاسبه می‌شوند.** «یک ماه بعد» در تقویم میلادی با
  تقویم شمسی یکی نیست. کلینیک تاریخ شمسی می‌بیند ولی گام ماهانه
  میلادی است — برای اقساط ماهانه عملاً همان می‌شود، ولی **این یک
  بدهی فنی واقعی است** و باید با `jalaali-js` درست شود.
- طرح‌های **موجود** با سررسید اشتباه اصلاح نمی‌شوند؛ فقط طرح‌های جدید
  درست ساخته می‌شوند. اصلاح گذشته یک migration داده‌ای می‌خواهد.
- چک ضمانت هنوز با مبلغ طرح مغایرت‌سنجی نمی‌شود.

---

## MOD-SEC-003 | ۱۴۰۵/۰۶/۰۸ | 🔴 پرونده‌ی بیمار واقعاً حذف می‌شد | **done ✓**

### مشکل
اولین ممنوعیت مطلق پروژه «حذف دائمی رکورد» است. ولی:

**۱. در دیتابیس:** هر جدول یک سیاست `FOR ALL` داشت — و `FOR ALL` شامل
`DELETE` هم هست. یعنی هر کاربر لاگین‌کرده‌ی کلینیک می‌توانست رکورد را
از سرور پاک کند.

**۲. در کد:** `deletePatient()` واقعاً این کار را می‌کرد — **پانزده
جدول** را cascade می‌کرد: نوبت، ویزیت، درمان، پرداخت، نسخه،
رادیولوژی، رکورد دندان، تایم‌لاین، لیست انتظار، سفارش لابراتوار،
ایمپلنت، طرح پرداخت، **چک**، رضایت‌نامه، مراحل درمان — بعد خود بیمار
را. و برای هرکدام یک `delete` در صف سینک می‌گذاشت تا سرور هم پاک شود.

### چرا این بدترین حالت بود
زنجیره‌ی مالی با پرونده می‌رفت. **چک و اقساط بیمار پاک می‌شد** — یعنی
کلینیک سند طلبش را از دست می‌داد. دقیقاً همان چیزی که خواستی مطمئن
باشد.

### راه‌حل — تضمین در لایه‌ای که قابل دور زدن نیست

قانونی که دیتابیس اجرا نکند، یک باگ با شکستن فاصله دارد.

**migration 023** سیاست `FOR ALL` را روی **۲۴ جدول رکورد** با سه سیاست
صریح `SELECT` / `INSERT` / `UPDATE` جایگزین کرد. سیاست `DELETE` عمداً
ساخته نشد — **همان نبودن، خودِ تضمین است**: با فعال بودن RLS، فرمانی
که سیاست منطبق ندارد رد می‌شود.

بایگانی دست‌نخورده ماند، چون بایگانی یک `UPDATE` است.

### تأیید روی دیتابیس زنده
```
protected_tables       = 24
rls_on                 = 24
select/insert/update   = 25 / 25 / 25
DELETE_STILL_POSSIBLE  = 0
```

### سپس کد اپ
اگر فقط سرور را می‌بستم، بدترین نتیجه می‌شد: کپی محلی پاک می‌شد،
کاربر فکر می‌کرد انجام شد، و خطا در صف سینک دفن می‌شد.

پس در `api.ts`:
- `deletePatient` → **`archivePatient`** (فقط `is_active = false`)
- `deleteAppointment` → **`cancelAppointment`**
- ۱۳ تابع حذف دیگر روی جدول‌های محافظت‌شده → `cancel...`
- `implant_components` و `tooth_records` ستون وضعیت ندارند، پس فقط از
  صف سینک برداشته شدند — فیلد جعلی ساخته نشد

### تست رگرسیون
`src/lib/recordSafety.test.ts` متن `api.ts` را می‌خواند و رد می‌شود اگر
کسی دوباره تابع حذفی برای این جدول‌ها بسازد یا `delete` به صف بفرستد.
تست **در همان اجرای اول ۱۹ مورد باقی‌مانده را گرفت** که دستی ندیده بودم.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ tsc --noEmit — بدون خطا
✓ Test Files 21 passed | Tests 381 passed (381)
✓ vite build — موفق
```
۳۷۵ ← **۳۸۱ تست**.

### ریسک باقی‌مانده
- **تأیید بصری لازم است:** دکمه‌های «حذف» در صفحات حالا لغو/بایگانی
  می‌کنند. متن دکمه‌ها هنوز جاهایی «حذف» می‌گوید — باید بازبینی شود.
- جدول‌های پیکربندی (رویه‌ها، یونیت، قالب پیامک، دسته‌بندی انبار،
  نقش‌ها، آزمایشگاه‌ها، پزشکان، پرسنل) **پوشش داده نشدند**. حذف یک
  قالب بی‌استفاده همان کارِ پاک‌کردن پرونده نیست و تصمیم جداگانه
  می‌خواهد.
- ثابت شد با **ساختار سیاست**، نه با شبیه‌سازی نشست یک کاربر واقعی —
  بدون JWT، `current_clinic_id()` تهی است و تست بی‌نتیجه می‌ماند.
  نبودِ سیاست DELETE یک واقعیت ساختاری Postgres است، نه حدس.

---

## MOD-FIX-004 | ۱۴۰۵/۰۶/۰۸ | 🔴 پرداخت ایمپلنت دوبار شمرده می‌شد | **done ✓**

### مشکل
`calcPatientBalance()` — که Dashboard، Billing، Patients، پرونده‌ی بیمار
و هشدار بدهکار همه از آن می‌خوانند — هر پرداخت ایمپلنت را **دو بار**
حساب می‌کرد.

### علت ریشه‌ای
```ts
const paid = payments.filter(completed).reduce(...)          // شامل پرداخت ایمپلنت
const implantPaid = implantCases.reduce(sum paid_amount)     // همان پول، دوباره
return { paid: paid + implantPaid }
```
`createPayment()` مبلغ را روی `implant_cases.paid_amount` هم اضافه
می‌کند (`src/lib/api.ts:359`) — یعنی `paid_amount` **آینه‌ی** دفتر
پرداخت است، نه عددی مستقل. جمع کردن هر دو یعنی شمارش مضاعف.

### اثر واقعی
بیمار ۵ میلیون بابت ایمپلنت می‌داد → «۱۰ میلیون پرداخت کرده» خوانده
می‌شد → **مانده‌اش ۵ میلیون کمتر از واقعیت** درمی‌آمد.

یعنی کلینیک فکر می‌کرد کمتر طلبکار است. و چون هشدار «بیمار بدهکار»
(MOD-FEAT-011) از همین تابع می‌خواند، ممکن بود اصلاً بالا نیاید.

### چطور پیدا شد
تصادفی نبود. هنگام ساخت `MOD-FEAT-014` یک تست ناوردا (invariant) نوشتم
که مجموع ردیف‌های هر پزشک باید **دقیقاً** برابر خروجی
`calcPatientBalance` باشد. تست رد شد. بعد با کوئری روی دیتابیس زنده
بررسی شد که `paid_amount` واقعاً آینه است، نه عدد مستقل.

### وضعیت داده‌ی زنده
```
payments_linked_to_implant = 0
implant_paid_sum           = 0
```
هنوز هیچ پرداخت ایمپلنتی ثبت نشده، پس **داده‌ی موجود خراب نشده**. ولی
اولین پرداخت ایمپلنت واقعی، خرابش می‌کرد.

### راه‌حل
پرداخت‌های متصل به ایمپلنت از جمع `payments` کنار گذاشته شدند، چون از
مسیر `paid_amount` شمرده می‌شوند. سه تست رگرسیون اضافه شد.

### ریسک باقی‌مانده
اگر کلینیکی `paid_amount` را دستی ویرایش کند (که فرم ایمپلنت اجازه
می‌دهد) دوباره از دفتر پرداخت جدا می‌شود. تک‌منبع کردنش کار جداگانه‌ای
است.

---

## MOD-FEAT-014 | ۱۴۰۵/۰۶/۰۸ | خلاصه حساب پزشکان معالج (COMP-127) | **done ✓**

### مشکل
پرونده یک مانده نشان می‌داد. این جواب می‌دهد «آیا این آدم به ما بدهکار
است» ولی نه «کدام پزشک طلبکار است» — که همان چیزی است که کلینیک
چندپزشکه آخر ماه سرش تسویه می‌کند.

### راه‌حل
`src/lib/doctorLedger.ts` — تفکیک هزینه/پرداختی/مانده به‌ازای پزشک،
داخل یک پرونده.

**migration لازم نداشت.**

### 🔴 قانون حاکم بر این فایل
**پول نباید ساخته، حذف، یا پخش شود.**

پرداختی که به هیچ ویزیتی وصل نیست، به پزشک قابل انتساب نیست. پخش کردنش
بین پزشک‌ها به نسبت هزینه، جدولی می‌سازد که **تراز است و غلط** — که از
جدولی که آشکارا نمی‌داند بدتر است.

پس ردیف «نامشخص» جداگانه نشان داده می‌شود، با یک خط توضیح زیر جدول.

### ناوردایی که همه چیز را نگه می‌دارد
مجموع ردیف‌های پزشکان + ردیف نامشخص باید **دقیقاً** برابر خروجی
`calcPatientBalance` باشد. هفت حالت تست شد: دو پزشک، پرداخت بی‌انتساب،
درمان بدون پزشک، درمان لغوشده، ایمپلنت، **بیش‌پرداخت** (مانده منفی)،
و پرونده‌ی خالی.

همین ناوردا بود که `MOD-FIX-004` را پیدا کرد.

### مورد ظریف: ایمپلنت دوبار شمرده نشود
پرونده‌ی ایمپلنت هم `paid_amount` دارد و هم ممکن است ردیف پرداخت متصل
داشته باشد. شمردن هر دو، ستون «پرداختی» پزشک را دو برابر می‌کرد در
حالی که جمع کل درست می‌ماند — سخت‌ترین نوع باگ برای دیدن. تست دارد.

### جدول فقط با بیش از یک طرف نشان داده می‌شود
روی پرونده‌ی تک‌پزشکه، جدول فقط خلاصه‌ی زیرش را تکرار می‌کرد.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.158.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 20 passed | Tests 375 passed (375)
✓ vite build — موفق
```
۳۵۷ ← **۳۷۵ تست** (۱۵ تست ledger + ۳ تست رگرسیون مالی).

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** جدول روی موبایل افقی اسکرول می‌شود.
- انتساب پرداخت به ویزیت وابسته است. کلینیکی که پرداخت‌ها را بدون
  ویزیت ثبت می‌کند، همه‌چیز را در ردیف «نامشخص» می‌بیند — درست، ولی
  بی‌فایده. `COMP-128` (پرداخت تجمیعی) احتمالاً همین را حل می‌کند.
- سهم پزشک (درصد قرارداد) لحاظ نمی‌شود؛ این جدول گردش مالی بیمار را
  تفکیک می‌کند، نه حق‌الزحمه را. `COMP-162/163` هنوز ساخته نشده.

---

## MOD-FEAT-013 | ۱۴۰۵/۰۶/۰۸ | وضعیت دندان با شکل، نه فقط رنگ (COMP-19/20/22/106) | **done ✓**

### مشکل
چارت هر دندانِ موجود را با **همان طرح آناتومیک** می‌کشید و فقط رنگ
پرکردنش را عوض می‌کرد. رنگ نمی‌تواند بگوید «این ایمپلنت است»: ایمپلنت
یک دندان با ته‌رنگ دیگر نیست، یک پیچ رزوه‌دار در استخوان است.

دندانپزشکی که یک قوس را اسکن می‌کند، **شکل را قبل از رنگ می‌خواند** —
و خواندن قوس در یک نگاه، کل کارِ چارت است.

### راه‌حل
`src/lib/toothVisual.ts` — تصمیم «چطور کشیده شود» جدا از «چه رنگی
باشد»، به‌صورت توابع خالص و تست‌شده.

**سه شکل بنیادی:**
- `implant` → پیچ با abutment، گردن، بدنه‌ی مخروطی و **رزوه**
- `absent` → سوکت خالی
- `tooth` → طرح آناتومیک موجود

**دو نشانه‌ی روی دندان:**
- **ریشه‌ی پرشده** برای عصب‌کشی — ضخامت و کدری ریشه بالا می‌رود
- **تاج توپر** برای روکش/ونیر/بریج — تاج طبیعی دیگر آن چیزی نیست که
  می‌بینی

### تصمیم‌های بالینی که ارزش کار در آن‌هاست

**۱. `post` و `pin` هم ریشه‌ی پرشده می‌گیرند.**
پست و پین لنگرهایی هستند که **داخل کانال** سیمان می‌شوند — یعنی کانال
قبلش درمان شده. کشیدن ریشه‌ی توخالی که یک پست را نگه داشته، چیزی را
نشان می‌دهد که وجود ندارد.

**۲. دندان شیری هرگز پرمولر نیست.**
منطق قدیمی `number % 10 >= 4` بود، پس `54` را پرمولر حساب می‌کرد. اما
پرمولر شیری وجود ندارد — ۵۴ مولر اول شیری است. روی چارت کودک، تعداد
ریشه‌ی اشتباه می‌کشید.

**۳. روکش و ونیر و بریج یکی حساب می‌شوند.**
در جنس و در صورتحساب فرق دارند، ولی روی چارت هر سه یک معنی دارند.

### حذف مسیر دوم
`isUpper`، `isMolar`، `isPremolar` و `isCanine` به‌صورت درون‌خطی با
`number % 10` در خود کامپوننت حساب می‌شدند — یعنی **منبع دوم حقیقت**،
که ممنوعیت «دو مسیر برای یک مقصد» است. حالا همه از `toothVisual`
می‌آیند. باگ دندان شیری از همین دوگانگی آمده بود.

### دو چیزی که در حین کار درست کردم
| مشکل | اصلاح |
|---|---|
| تست «جامعیت» جامع نبود — فهرست شرایط را از حافظه نوشتم و `post` و `pin` جا افتاد | فهرست به `Record<ToothCondition, true>` تبدیل شد تا **کامپایلر** تضمین کند، نه حافظه‌ی من |
| `rootCount` و `isPrimaryTooth` نوشته و تست شدند ولی هیچ مصرف‌کننده‌ای نداشتند | **حذف شدند.** کد یتیم ممنوعیت مطلق است؛ وقتی هندسه‌ی ریشه واقعاً آناتومیک شد برمی‌گردند |

### دسترس‌پذیری
هر SVG حالا `role="img"` و `aria-label` دارد که وضعیت را می‌گوید —
«دندان ۱۶ — ایمپلنت». قبلاً صفحه‌خوان فقط یک عدد لخت می‌گرفت.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.157.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 19 passed | Tests 357 passed (357)
✓ vite build — موفق
```
۳۴۰ ← **۳۵۷ تست**.

بررسی کد یتیم: هر ۶ تابع باقی‌مانده در کامپوننت‌ها صدا زده می‌شوند.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده — و این مورد بیش از بقیه به آن نیاز دارد.**
  شکل پیچ در اندازه‌ی ۴۸ پیکسل روی گوشی باید دیده شود. اگر رزوه‌ها
  بهم بریزند یا تاج توپر خیلی تیره باشد، باید تنظیم شود.
- تعداد ریشه هنوز آناتومیک نیست: مولر بالا سه ریشه دارد ولی چارت دو تا
  می‌کشد. `rootCount` نوشته و تست شده بود ولی چون هندسه هنوز از آن
  استفاده نمی‌کرد حذف شد — کار جداگانه‌ای است.
- `COMP-103` (گلیف انتخاب سطح زیر هر دندان) ساخته نشد.
- `COMP-104` (چند رنگ حاشیه هم‌زمان با معانی مختلف) ساخته نشد.

---

## MOD-FEAT-012 | ۱۴۰۵/۰۶/۰۸ | درصد اشغال، میانگین انتظار و بیماران در مطب (COMP-133/134/135) | **done ✓**

### مشکل
صفحه‌ی نوبت‌دهی **ردیف می‌شمرد**: امروز / در انتظار / تکمیل‌شده.
شمردن ردیف حجم را می‌گوید، نه اینکه روز پر است یا نه. هشت نوبت برای
چهار یونیت یک روز خلوت است و برای یک پزشک نیم‌شیفت یک روز پرشده.

### راه‌حل
`src/lib/dayMetrics.ts` — کاملاً خالص، بدون `Date.now()` و بدون DOM.
«اکنون» به‌عنوان ورودی پاس داده می‌شود تا همان روز در تست در هر لحظه‌ای
قابل ارزیابی باشد.

**migration لازم نداشت.**

### تصمیم‌هایی که ارزش کار در آن‌هاست

**۱. ظرفیت با دقیقه سنجیده می‌شود، نه با تعداد اسلات.**
نوبت‌ها هم‌اندازه نیستند. سه ایمپلنت می‌تواند روزی را پر کند که شمارش
اسلات آن را «یک‌سوم پر» می‌نامد. تست این را پین می‌کند.

**۲. درصد اجازه دارد از ۱۰۰ رد شود.**
Overbooking واقعاً اتفاق می‌افتد. محدود کردن به ۱۰۰ دقیقاً همان روزی
را پنهان می‌کند که مدیر باید ببیند. بالای ۱۰۰ قرمز می‌شود.

**۳. زمان استراحت از ظرفیت کم می‌شود.**
اگر ناهار جزو ظرفیت حساب شود، هر کلینیکی همیشه کم‌کار به نظر می‌رسد —
و عددی که همیشه پایین است، عددی است که کسی به آن عمل نمی‌کند.

**۴. میانگین انتظار وقتی کسی منتظر نیست `null` است، نه صفر.**
نوبت ساعت ۱۵:۰۰ وقتی ساعت ۱۴:۳۰ است انتظار نیست، آینده است. کارت «—»
نشان می‌دهد، نه یک صفرِ آرامش‌بخشِ بی‌معنا.

**۵. «تأیید شده» یعنی بیمار زنگ زده، نه اینکه در ساختمان است.**
پس در شمارش «در مطب» حساب نمی‌شود.

**۶. زمان نامعتبر `null` برمی‌گرداند، نه `NaN`.**
یک `NaN` بی‌سروصدا در هر میانگینی که لمسش کند پخش می‌شود.

**۷. نوبت با زمان خراب صفر دقیقه حساب نمی‌شود** — پیش‌فرض می‌گیرد.
وگرنه یک ردیف معیوب روز را خلوت‌تر از واقعیت نشان می‌دهد.

### حالت خالی گویا
اگر برنامه‌ی کاری پزشکان برای امروز ثبت نشده باشد، کارت به‌جای «۰٪»
می‌نویسد «برنامه‌ی کاری پزشکان برای امروز ثبت نشده» — عدد نیست چون
شیفتی تعریف نشده، نه چون روز خالی است. همان ممنوعیت «پیام کلی».

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.156.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 18 passed | Tests 340 passed (340)
✓ vite build — موفق
```
۳۱۴ ← **۳۴۰ تست** (۲۶ تست جدید).

بررسی کد یتیم: هر ۹ تابع `dayMetrics` یا از `summariseDay` صدا زده
می‌شوند یا مستقیماً از صفحه — هیچ‌کدام معلق نیست.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** چهار کارت جدید روی موبایل دو ستون می‌شوند؛
  چیدمانشان را باید مهدی ببیند.
- ظرفیت از `doctor_schedules` می‌آید. اگر کلینیک شیفت‌ها را وارد نکرده
  باشد، کارت اشغال عدد نمی‌دهد — عمدی، ولی یعنی این قابلیت به پر کردن
  تنظیمات وابسته است.
- مرخصی و تعطیلی پزشک لحاظ نمی‌شود؛ فقط برنامه‌ی هفتگی. `COMP-136`
  (نمایش ناموجودی پزشک) هنوز ساخته نشده.
- اعداد در لحظه‌ی رندر حساب می‌شوند و خودکار تازه نمی‌شوند. `COMP-174`
  نشان داد رقیب بازه‌ی بروزرسانی خودکار دارد.

---

## MOD-FEAT-011 | ۱۴۰۵/۰۶/۰۸ | هشدار شناور بالینی و بدهکار (COMP-33/34) | **done ✓**

### مشکل
حساسیت دارویی، بیماری زمینه‌ای و داروی مصرفی بیمار در تب «سوابق پزشکی»
دفن شده بودند — یعنی فقط اگر کسی عمداً می‌رفت آن تب را باز می‌کرد.
دندانپزشکی که نداند بیمار وارفارین می‌خورد یا به پنی‌سیلین حساس است،
مشکل واقعی دارد. بدهی هم همین‌طور: در تب پرداخت‌ها بود، نه جلوی چشم.

### علت ریشه‌ای
داده‌ها از قبل وجود داشتند (`allergies`، `medical_conditions`،
`medical_history`، `medications`) ولی هیچ‌جا **مزاحم** نمی‌شدند.
اطلاعاتی که برای دیدنش باید بگردی، در لحظه‌ی درمان دیده نمی‌شود.

### راه‌حل
`src/lib/patientAlerts.ts` — منطق خالص و تست‌پذیر — به‌علاوه‌ی
`PatientAlerts.tsx` که فقط ظاهر را تصمیم می‌گیرد.

**migration لازم نداشت.** هر چهار فیلد از قبل روی `patients` بودند.

### 🔴 تصمیم اصلی: فیلدهای «ندارد» نباید کارت قرمز بسازند
کلینیک‌ها فیلدهای اجباری‌نما را با «ندارد» پر می‌کنند، نه خالی
می‌گذارند. یک کارت قرمز که می‌گوید «حساسیت: ندارد» **بدتر از نبودن
هشدار است**: پرسنل یاد می‌گیرند کارت قرمز معمولاً نویز است و خواندنش
را رها می‌کنند — و آن یک باری که نوشته «پنی‌سیلین» هم با بقیه بسته
می‌شود.

پس یک واژگان از مقادیر «هیچ‌چیز برای گزارش» ساخته شد، با نرمال‌سازی
حروف عربی/فارسی قبل از جستجو. فیلد نیمه‌ویرایش‌شده («دیابت، ندارد»)
هم فقط «دیابت» را نشان می‌دهد.

### 🔴 ترتیب شدت با تست قفل شد
```
۰ حساسیت    — می‌تواند ظرف چند دقیقه بکشد
۱ بیماری     — تغییر می‌دهد چه کاری روی صندلی امن است
۲ دارو       — عمدتاً ریسک خونریزی؛ مهم ولی کندتر
۳ بدهی       — پول. هرگز بالای یک واقعیت بالینی نه
```
اگر کارت بدهکار بالای کارت ضدانعقاد بیفتد، یک خطای ایمنی بیمار است.
پس ترتیب با تست پین شد، نه به ترتیب رندر JSX سپرده شد.

رنگ هم همین را می‌گوید: بالینی قرمز، مالی کهربایی — بدون خواندن متن
معلوم است «این می‌تواند به بیمار آسیب بزند» یا «این به کلینیک هزینه
دارد».

### تصمیم‌های دیگر
- **`medical_conditions` و `medical_history` با هم خوانده می‌شوند.**
  کلینیک‌ها سر اینکه کدام فیلد را پر کنند توافق ندارند؛ خواندن فقط
  فیلد مرتب یعنی از دست دادن هشدار روی نیمی از پرونده‌ها. تکراری‌ها
  حذف می‌شوند.
- **بستن هشدار موقتی است.** به ازای بیمار و به ازای هر بار باز کردن
  پرونده — هرگز ذخیره نمی‌شود. حساسیتی که ماه پیش تأیید شد باید دفعه‌ی
  بعد دوباره مزاحم شود، وگرنه اصلاً چرا کارت؟
- **مانده‌ی منفی هشدار نیست** — یعنی کلینیک به بیمار بدهکار است.
- سقف اعتبار اگر رد شده باشد، خط دوم اضافه می‌کند.

### نتیجه‌ی تست واقعی
تست اول **رد شد** و یک خلأ واقعی را نشان داد: «نداري» با املای عربی
نرمال می‌شد ولی در واژگان نبود، پس یک کارت قرمز اشتباه می‌ساخت.
واژگان اصلاح شد (`نداری`، `مشکلی ندارد`، `بدون سابقه` و چند مورد دیگر).

```
npm run verify
✓ sw.js cache bumped to v1.155.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 17 passed | Tests 314 passed (314)
✓ vite build — موفق
```
۲۹۰ ← **۳۱۴ تست** (۲۴ تست جدید).

### تأیید بصری از رقیب
`COMP-33`/`COMP-34`/`COMP-191`: کارت قرمز شناور، دکمه‌ی × برای بستن،
چند هشدار روی هم پشته — همان الگویی که پیاده شد.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** موقعیت کارت‌ها روی موبایل (بالا-چپ، عرض
  محدود) را باید مهدی ببیند. اگر با هدر تداخل داشت، `top-20` باید
  تنظیم شود.
- واژگان «ندارد» فهرست ثابت است. اگر کلینیکی عبارت دیگری بنویسد، یک
  کارت اشتباه ساخته می‌شود. فهرست باید با دیدن داده‌ی واقعی رشد کند.
- چیپ‌های سربرگ (`COMP-78`) عمداً قابل بستن **نیستند**. کارت شناور
  می‌تواند بسته شود؛ چیپ تا پایان ویزیت سر جایش می‌ماند. اگر روی
  پرونده‌های شلوغ سربرگ را بهم بریزد، باید محدود شود.

**یادداشت روند:** اولین بار `alertChips()` نوشته و تست شد ولی هیچ‌جا
صدا زده نمی‌شد. آن را در «ریسک باقی‌مانده» نوشتم که «عمدی است» — که
غلط بود: **کد یتیم ممنوعیت مطلق است و مستندکردنش مجازش نمی‌کند.**
قبل از کامیت به سربرگ وصل شد.

---

## MOD-FEAT-009 | ۱۴۰۵/۰۶/۰۸ | مکان قفسه، زنگ پیگیری و صف تحویل لابراتوار (بازیابی) | **done ✓**

### مشکل
کار لابراتوار **دیجیتالی** ردیابی می‌شد ولی **فیزیکی** نه. وقتی کار از
لابراتوار برمی‌گشت، هیچ‌کس نمی‌دانست روی کدام قفسه است — پرسنل جعبه‌ها
را دستی باز می‌کردند. و زنگ پیگیری وجود نداشت: نبودِ کار در روز موعد
معلوم می‌شد، نه چند روز قبلش که هنوز می‌شد کاری کرد.

### علت ریشه‌ای
سومین ماژول از پنج ماژول گم‌شده در revert اضطراری v1.147.0
(کامیت `c47a4c1`).

### راه‌حل
هفت ستون جدید روی `lab_orders` + `src/lib/labShelf.ts`.

**تصمیم‌های طراحی که عمدی‌اند:**
- **آدرس قفسه سه‌تکه است، نه یک فیلد آزاد.** قفسه / شماره / فضا. یک
  فیلد متنی آزاد به دست‌خط‌های ناهماهنگ تبدیل می‌شود.
- **همه یا هیچ.** «قفسه A» بدون شماره یعنی باز کردن همه‌ی جعبه‌های A —
  بدتر از هیچ. هم در کد و هم با `check constraint` در دیتابیس.
- **`alarm_date` جدا از `deadline`.** زنگ پیگیری چند روز جلوتر است.
- **«تحویل شده» تا وقتی «اتمام کار» تیک نخورده غیرفعال است.** کار
  تمام‌نشده نمی‌تواند به بیمار برسد.
- **صف «آماده تحویل»** با نمایش آدرس قفسه، و علامت‌گذاری موردهایی که
  آدرس ندارند — همان‌ها که در جعبه گم می‌شوند.

### ⚠️ نقصی که در کد بازیابی‌شده پیدا شد
دکمه‌ی سریع «ثبت تحویل» در صف، فقط `{ delivered: true }` می‌فرستاد.

ولی `readyForDelivery()` سفارش‌هایی را هم برمی‌گرداند که
`work_done = false` دارند اما `status` آن‌ها `ready` یا `received` است —
یعنی **هر ردیفی که قبل از MOD-FEAT-009 ساخته شده**، چون مقدار پیش‌فرض
ستون `false` است.

دیتابیس `check (delivered = false or work_done = true)` دارد و این را
رد می‌کند.

**چرا این بدتر از یک خطای ساده است:** برنامه آفلاین‌اول است. نوشتن در
Dexie موفق می‌شود، به پرسنل گفته می‌شود «تحویل ثبت شد»، و خطا فقط
بعداً در صف سینک ظاهر می‌شود. یعنی کار تحویل‌شده در ظاهر ثبت شده ولی
روی سرور نیست.

اصلاح: تابع `deliveryPatch()` که `work_done` را هم اجباراً true می‌کند.
منطق در کدِ تست‌شده گذاشته شد، نه داخل یک `onClick` در JSX.

### نتیجه‌ی تست واقعی

**migration 022 روی Supabase زنده اعمال شد:**
```
new_cols 7 · new_constraints 2 · ready_idx 1 · rls_enabled true
```

**و محدودیت‌ها واقعاً تست شدند** (درج عمدی داده‌ی بد داخل بلاک
`do $$` با rollback):
```
partial_shelf_blocked          = true
delivery_before_work_blocked   = true
→ BOTH_CONSTRAINTS_ENFORCED_ROLLED_BACK
```

```
npm run verify
✓ sw.js cache bumped to v1.154.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 16 passed | Tests 290 passed (290)
✓ vite build — موفق
```
۲۵۸ ← **۲۹۰ تست** (۳۰ تست بازیابی‌شده + ۲ تست جدید برای `deliveryPatch`).

نکته: `lab_orders` از قبل در `TABLE_NAMES` و Dexie بود و ستون‌های جدید
ایندکس نمی‌خواهند، پس **نسخه‌ی Dexie بالا نرفت** — بررسی شد که هیچ
`.where()` روی ستون‌های جدید زده نمی‌شود.

### تأیید بصری از رقیب
`COMP-152` و `COMP-153`: همان سه فیلد قفسه و همان «تاریخ آلارم» جدا از
تاریخ تحویل در فرم لابراتوار minadent.ir دیده شد.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** فرم لابراتوار و کارت «آماده تحویل» را باید
  مهدی ببیند.
- **Dexie محدودیت ندارد.** اگر ترکیب نامعتبری آفلاین ساخته شود، فقط
  هنگام push رد می‌شود. `deliveryPatch()` مسیر اصلی را می‌بندد، ولی فرم
  دستی هنوز به اعتبارسنجی سمت کلاینت تکیه دارد. یک آینه‌ی کامل
  محدودیت‌ها در لایه‌ی سینک کار جداگانه‌ای است.
- `COMP-160` («وضعیت برگشت متریال» با جزئیات بیشتر) پوشش داده نشد —
  ما فقط یک boolean داریم.

---

## MOD-FEAT-006 | ۱۴۰۵/۰۶/۰۸ | نمایش زنده‌ی سقف بیمه در فرم درمان (بازیابی) | **done ✓**

### مشکل
`MOD-FEAT-005` بیمه‌نامه و سقف را ذخیره می‌کرد ولی **هیچ اثری روی قیمت
نداشت**. منطق `splitCoverage` نوشته شده بود و هیچ‌جا صدا زده نمی‌شد —
یعنی دقیقاً «کد یتیم»، که ممنوعیت مطلق پروژه است.

### علت ریشه‌ای
دومین ماژول از پنج ماژول گم‌شده در revert اضطراری v1.147.0
(کامیت `544c1c3`).

### راه‌حل
بیمه‌نامه‌ها و ادعاهای بیمار **هنگام باز شدن فرم درمان** بارگذاری
می‌شوند، نه از قبل برای همه‌ی بیماران — این صفحه می‌تواند صدها ویزیت
داشته باشد.

سپس دو جا نمایش داده می‌شود:
1. **بنر زنده در خود فرم** — سهم بیمه، سهم بیمار، مانده سقف پس از این
   درمان. رنگ کهربایی وقتی سقف گاز می‌گیرد.
2. **دو ردیف اضافه در پنجره‌ی تأیید** — سهم واقعی بیمار در لحظه‌ی ثبت
   دیده می‌شود، نه بعد از اینکه ادعا کم برگشت.

وقتی هیچ بیمه‌نامه‌ای اعمال نمی‌شود، **هیچ بنری نشان داده نمی‌شود** —
نه بنری با عدد صفر که گمراه‌کننده است.

### ⚠️ نقصی که در کد بازیابی‌شده پیدا شد
کد اصلی تقسیم را با `new Date()` حساب می‌کرد — یعنی **تاریخ امروز**،
نه تاریخ ویزیت.

اثر واقعی: منشی درمان‌های هفته‌ی پیش را امروز وارد می‌کند. اگر
بیمه‌نامه بین آن دو تاریخ منقضی شده باشد، نرم‌افزار سهم بیمه را صفر
حساب می‌کند در حالی که روز درمان معتبر بوده — یا برعکس، بیمه‌نامه‌ای
که تازه شروع شده به ویزیت قبلی‌اش پوشش می‌دهد.

این **محاسبه‌ی مالی غیردقیق** است و ممنوعیت مطلق پروژه.

اصلاح: `encounters.find(...)` که همان چند خط پایین‌تر از قبل وجود
داشت، حالا `encounter_date` را می‌دهد. fallback به امروز فقط وقتی
ویزیت پیدا نشود — همان تاریخی که رکورد با آن ساخته می‌شود.

این با `MOD-FEAT-008` اشتباه گرفته نشود: آن یکی **محاسبه‌ی مجدد
اختیاری** درمان‌های قبلی را اضافه می‌کند، که کار جداگانه‌ای است.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.153.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 15 passed | Tests 258 passed (258)
✓ vite build — موفق
```
۲۵۰ ← **۲۵۸ تست** (۶ تست بازیابی‌شده + ۲ تست جدید که ثابت می‌کنند
تقسیم به تاریخ ویزیت وابسته است، نه تاریخ امروز).

### تأیید بصری از رقیب
`COMP-151` و `COMP-178` همین رفتار را در minadent.ir نشان دادند —
بنر قرمز «به دلیل محدودیت سقف تعهد، تمام یا بخشی از هزینه به‌صورت آزاد
محاسبه می‌گردد». متن هشدار ما تقریباً همان است.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** بنر کهربایی و دو ردیف پنجره‌ی تأیید را
  باید مهدی ببیند.
- `fetchInsuranceClaims()` همه‌ی ادعاهای کلینیک را می‌گیرد و سمت کلاینت
  فیلتر می‌کند. برای مقیاس فعلی بی‌اهمیت است؛ با رشد باید کوئری
  به بیمار محدود شود.
- `MOD-FEAT-007` هنوز بازیابی نشده: سهم بیمه **محاسبه و نمایش داده
  می‌شود ولی روی درمان ذخیره نمی‌شود**. یعنی نقل‌قولی که به بیمار داده
  شد جایی ثبت نمی‌ماند. نیاز به migration 021.
- بیمه‌ی تکمیلی (`COMP-155`) پشتیبانی نمی‌شود — فقط یک لایه بیمه.

---

## MOD-FEAT-005 | ۱۴۰۵/۰۶/۰۸ | سقف بیمه‌ی هر بیمار (بازیابی) | **done ✓**

### مشکل
`insurance_companies` فقط شرکت بیمه را نگه می‌داشت. اینکه **این بیمار**
چه تعهدی دارد — شماره بیمه‌نامه، بازه‌ی اعتبار، درصد پوشش، و مهم‌تر از
همه **سقف تعهد** — هیچ جایی مدل نشده بود.

### علت ریشه‌ای
این ماژول یک بار ساخته و تست شده بود (کامیت `56c8d4a`)، ولی محتوایش با
revert اضطراری v1.147.0 از main برداشته شد و برنگشت. تا امروز فرض بر
این بود که داریمش. با خواندن کد معلوم شد نداریم: `CHANGELOG` فقط
`MOD-FEAT-001/002/010` را داشت و `Treatment` هیچ فیلد بیمه‌ای نداشت.

### راه‌حل
`src/lib/insurance.ts` بازیابی شد: `splitCoverage`، `usedCeiling`،
`remainingCeiling`، اعتبارسنجی بیمه‌نامه و انتخاب بهترین بیمه‌ی فعال.
**مصرف سقف از `insurance_claims` مشتق می‌شود، هرگز ذخیره نمی‌شود** —
یک شمارنده‌ی ذخیره‌شده با واقعیت drift می‌کند.

### ⚠️ دو نقص در همان کد بازیابی‌شده
| نقص | چه اتفاقی می‌افتاد | اصلاح |
|---|---|---|
| **تب تکراری** | کامیت اصلی یک تب دوم با **همان کلید `insurance`** اضافه می‌کرد. نتیجه: دو تب برای یک مقصد (نقض ممنوعیت مطلق) و هر دو پنل هم‌زمان رندر می‌شدند | یک تب، بدنه‌ی مشترک: خلاصه‌ی قدیمی + پنل جدید |
| **نسخه‌ی Dexie اشتباه** | کد اصلی `version(12)` بود، چون روی شاخه‌ای نوشته شده بود که `version(11)` را به `tooth_notes` داده بود. آن نسخه هرگز منتشر نشد. اگر ۱۲ می‌ماند، بین کد و دیتابیس هر مرورگر نصب‌شده شکاف می‌افتاد | `version(11)` |

### ترتیب اجرا — درس همان قطعی
**اول migration، بعد کد.** `patient_policies` فقط *پس از* اعمال
migration 020 روی سرور زنده به `TABLE_NAMES` اضافه شد. برعکسش دقیقاً
همان باگی است که یک بار کل سینک را شکست.

### نتیجه‌ی تست واقعی
migration 020 روی Supabase زنده اعمال شد. کوئری تأیید:
```
columns 13 · indexes 4 · rls_enabled true
policies 3 (SELECT, INSERT, UPDATE) · open_policies 0
```
بدون سیاست DELETE — بیمه‌ی منقضی روی پرونده می‌ماند چون درمان‌های
گذشته با آن قیمت‌گذاری شده‌اند.

کوئری با همان شکلی که `pullTable()` می‌زند: **۰ ردیف، بدون خطا** —
یعنی سینک این جدول را می‌بیند و نمی‌شکند.

```
npm run verify
✓ tsc --noEmit — بدون خطا
✓ Test Files 15 passed | Tests 250 passed (250)
✓ vite build — موفق
```
۲۱۱ ← **۲۵۰ تست** (۳۹ تست بازیابی‌شده‌ی insurance).

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** تب بیمه و فرم بیمه‌نامه را باید مهدی ببیند.
- `MOD-FEAT-006/007/008` هنوز بازیابی نشده‌اند: سقف در **فرم درمان**
  دیده نمی‌شود و سهم بیمه روی درمان ذخیره نمی‌شود. یعنی الان
  بیمه‌نامه ثبت می‌شود ولی هنوز روی قیمت درمان اثر ندارد.
- `renderInsurance()` قدیمی هنوز دو فیلد متنی را نشان می‌دهد که در تب
  «مشخصات» هم هستند. عمداً حذف نشد تا داده‌ی کلینیک‌هایی که فقط همان
  فیلدها را پر کرده‌اند از دست نرود؛ ولی این یک تکرار است و باید در
  MOD-FEAT-006 تعیین تکلیف شود.

---

## MOD-FEAT-010 | ۱۴۰۵/۰۶/۰۸ | جستجوی سراسری با Ctrl+K (AUD-006) | **done ✓**

### مشکل
برای پیدا کردن یک بیمار باید اول به صفحه‌ی بیماران می‌رفتی، بعد جستجو
می‌کردی. هر ماژول جستجوی خودش را داشت و هیچ راهی برای «فقط اسمش را
بنویس و پرونده‌اش باز شود» وجود نداشت.

### علت ریشه‌ای
`AICommandBar` بلد بود **برود** («برو به بیماران») ولی بلد نبود
**پیدا کند**. نیمه‌ی دوم هرگز نوشته نشده بود.

نکته‌ی مهم: این کار یک بار در `feat/global-search` ساخته شده بود، ولی
هنگام revert اضطراری v1.147.0 محتوایش از main برداشته شد. کامیت
`81e06f9` در تاریخچه ماند اما کد در درخت کاری نبود — یعنی سند
`START-HERE.md` که آن را «شاخه‌ی معلق» می‌نامید، دقیق نبود.

### راه‌حل
`src/lib/globalSearch.ts` جدید، با استفاده‌ی مجدد از `fuzzyScore` موجود
به‌جای افزودن یک وابستگی جستجو. **همان `AICommandBar` ارتقا داده شد،
نه یک نوار جستجوی دوم** — دو مسیر برای یک مقصد ممنوع است.

- `Ctrl/Cmd+K` روی `window` بسته شد تا از هر صفحه‌ای کار کند
- ایندکس روی **باز شدن** نوار ساخته می‌شود، نه روی mount
- وزن‌دهی به نوع رکورد (بیمار ۱.۴ ← لابراتوار ۰.۸۵)
- پیشوند محدودکننده: `بیمار: احمدی`
- دستور شناخته‌شده هنوز برنده است: «بیمار جدید اضافه کن» یک دستور است،
  نه جستجوی بیماری به نام «جدید»

### دو نقصی که هنگام بازخوانی همین کد پیدا شد
| نقص | چه اتفاقی می‌افتاد | اصلاح |
|---|---|---|
| **پرش هایلایت** | لیست گروه‌بندی‌شده نمایش داده می‌شد ولی کلید ↓ روی آرایه‌ی مرتب‌شده‌ی خام حرکت می‌کرد. هر جا نتیجه‌ای ضعیف‌تر هم‌نوع نتیجه‌ای قوی‌تر بود، این دو ترتیب فرق می‌کردند و هایلایت بین بخش‌ها می‌پرید | تابع `flattenGroups` — نمایش و کیبورد هر دو از یک ترتیب می‌خوانند |
| **ایندکس کهنه** | ایندکس فقط یک بار در هر session ساخته می‌شد. بیماری که ۵ دقیقه پیش ثبت شده بود تا بارگذاری مجدد صفحه در جستجو پیدا نمی‌شد | ساخت مجدد در هر بار باز شدن؛ لیست قبلی حین ساخت روی صفحه می‌ماند |

ضمناً `results` و `noMatch` از state خارج و مشتق (derived) شدند. علت:
اگر کاربر قبل از آماده شدن ایندکس تایپ می‌کرد، «چیزی پیدا نشد» روی
صفحه گیر می‌کرد چون هیچ‌چیز رتبه‌بندی را دوباره اجرا نمی‌کرد.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache already at v1.150.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 14 passed | Tests 211 passed (211)
✓ vite build — موفق در ۱۶.۵ ثانیه
```
۱۸۴ تست پایه ← **۲۱۱ تست** (۲۴ تست بازیابی‌شده + ۳ تست جدید برای
`flattenGroups`، شامل یک fixture که واقعاً ترتیب خام و ترتیب نمایش را
از هم جدا می‌کند).

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** من نمی‌توانم روی گوشی لمس کنم. چیدمان
  لیست نتایج، ترتیب RTL و رفتار `Ctrl+K` روی دستگاه واقعی باید توسط
  مهدی دیده شود.
- ایندکس همه‌ی رکوردها را به حافظه می‌خواند. برای کلینیک فعلی
  (۲ بیمار) بی‌اهمیت است؛ در مقیاس رقیب (۱۴٬۴۳۰ بیمار) ساخت ایندکس
  در هر بار باز شدن احتمالاً باید به یک ایندکس افزایشی تبدیل شود.
- `payment` در `KIND_LABELS` تعریف شده ولی هنوز در ایندکس پر نمی‌شود —
  عمدی است تا نتایج مالی بعداً با احتیاط اضافه شوند، نه کد یتیم:
  نوع اتحادیه‌ای (union) وجود دارد تا افزودنش بدون برچسب ممکن نباشد.

---

## موارد باز (از ممیزی AUD)

| شناسه | شرح | وضعیت |
|---|---|---|
| AUD-003 | صفر تست خودکار | **done ✓** (MOD-TEST-001/2/3) |
| AUD-004 | پیامک: هیچ سرویس واقعی وصل نیست (نیاز به کلید API) | pending |
| AUD-005 | پوش نوتیفیکیشن وجود ندارد | pending |
| AUD-006 | جستجوی سراسری (Ctrl+K) نیست | **done ✓** (MOD-FEAT-010) |
| AUD-007 | خروجی PDF رسمی نیست (فقط CSV) | pending |
| AUD-008 | دسترس‌پذیری ناقص (۵۴ aria-label) | pending |
| AUD-010 | طرح درمان جامع | **done ✓** (MOD-FEAT-001 + 002) |
| AUD-011 | تعطیلات اشتباه | **done ✓** (MOD-DATA-001) |
