# CHANGELOG — مینادنت

قالب هر رکورد: `شناسه | تاریخ | شرح | وضعیت`
وضعیت‌ها: `done` (تست‌شده) · `pending` (شروع نشده) · `needs-review` (نیاز به بررسی)

---

## MOD-SEC-001 | ۱۴۰۵/۰۶/۰۳ | رفع بنیادی RLS | **done ✓**

### مشکل
۳۹ از ۴۳ سیاست امنیتی روی `USING (true)` بودند. RLS فقط ظاهراً روشن بود.
هرکس کلید `anon` را داشت (در هر PWA سمت کلاینت قابل استخراج) می‌توانست
کل پرونده‌ی بیماران را بخواند یا حذف کند.

### علت ریشه‌ای
هیچ ستونی جدول `staff` را به `auth.users` وصل نمی‌کرد، پس نوشتن هیچ
سیاست معناداری ممکن نبود — به همین دلیل همه روی `true` رها شده بودند.

### راه‌حل (۴ migration)
| Migration | کار |
|---|---|
| `20260825000001` | افزودن `staff.user_id` + توابع `current_clinic_id()` و `current_staff_role()` با `SECURITY DEFINER` و `search_path` قفل‌شده |
| `20260825000002` | آزمایش روی `patients` (فقط یک جدول، برای کاهش ریسک) |
| `20260825000003` | تعمیم خودکار به همه‌ی جداول دارای `clinic_id` |
| `20260825000004` | `notification_logs`، `sms_logs` (بدون `clinic_id`) و `online_booking_requests` |

### نتایج تست واقعی (روی دیتابیس زنده)
| سناریو | قبل | بعد | نتیجه |
|---|---|---|---|
| کاربر ناشناس ← خواندن بیماران | ۲ رکورد | **۰** | ✅ |
| کاربر ناشناس ← خواندن پرداخت/درمان/ایمپلنت/پیامک | همه | **۰** | ✅ |
| کاربر ناشناس ← درج بیمار | موفق | **رد شد** | ✅ |
| کاربر لاگین‌کرده ← خواندن ۸ جدول اصلی | همه | **همه (بدون تغییر)** | ✅ |
| نوبت‌دهی عمومی (بدون لاگین) ← درج | موفق | **موفق** | ✅ |

**آمار نهایی:** ۳۷ جدول با جداسازی کلینیکی، ۲ جدول محدود به کاربر
لاگین‌کرده، ۱ جدول با درج عمومی عمدی. صفر سیاست باز باقی مانده.

### ریسک باقی‌مانده
- `notification_logs` و `sms_logs` ستون `clinic_id` ندارند، پس جداسازی
  کلینیکی روی آن‌ها ممکن نیست؛ فعلاً به کاربر لاگین‌کرده محدود شده‌اند.
  در معماری چندکلینیکی آینده باید `clinic_id` اضافه شود.
- تفکیک دسترسی **بر اساس نقش** (مثلاً دستیار نتواند حقوق پرسنل را ببیند)
  هنوز فقط سمت کلاینت (`permissions.ts`) است، نه سطح دیتابیس.
  تابع `current_staff_role()` برای این کار آماده شده ولی هنوز استفاده نشده.

---

## MOD-TEST-001/002/003 | ۱۴۰۵/۰۶/۰۳ | زیرساخت تست خودکار | **done ✓**

### مشکل (AUD-003)
صفر تست خودکار در ۲۸٬۳۰۰ خط کد — علت مستقیم رگرسیون‌های مکرر
(فروریختن چیدمان کارت‌ها، دکمه‌های تکراری، سرریز هدر).

### تحلیل انتخاب ابزار
| گزینه | سرعت | نگهداری | سازگاری | نتیجه |
|---|---|---|---|---|
| **Vitest** | بالا (موتور Vite) | همان `vite.config` | بومی Vite 5 + TS 5 | ✅ انتخاب شد |
| Jest | کند (ts-jest) | پیکربندی موازی | اصطکاک ESM | رد |
| `node:test` | بالا | کامپایل دستی TS | alias ها را نمی‌فهمد | رد |

**دلیل انتخاب:** از همان پیکربندی بیلد واقعی استفاده می‌کند، پس هیچ
واگرایی بین محیط تست و تولید ایجاد نمی‌شود.

### پوشش
| شناسه | فایل | تعداد | تمرکز |
|---|---|---|---|
| MOD-TEST-001 | `finance.test.ts` | ۱۳ | درمان لغوشده، پرداخت در انتظار، ایمپلنت، تفکیک بیماران، null |
| MOD-TEST-002 | `persianDate.test.ts` | ۱۴ | تبدیل رفت‌وبرگشت، کبیسه، ۳۰ اسفند، ارقام فارسی، باگ نیمه‌شب |
| MOD-TEST-003 | `permissions.test.ts` | ۹ | مرز نقش‌ها، fail-closed، دور زدن با لینک مستقیم |

### نتیجه‌ی اجرای واقعی
```
✓ finance.test.ts      (13 tests)
✓ persianDate.test.ts  (14 tests)
✓ permissions.test.ts  (9 tests)
Test Files  3 passed (3)  |  Tests  36 passed (36)
```
تأیید شد که کد تست وارد بیلد تولیدی نمی‌شود.

### دستور جدید
`npm run verify` = تایپ‌چک ← تست ← بیلد (دروازه‌ی واحد قبل از هر commit)

### ریسک باقی‌مانده
- `sync.ts` (۲۲۲ خط) هنوز تست ندارد؛ نیاز به mock کردن Dexie و Supabase دارد.
- هیچ تست UI/کامپوننتی وجود ندارد — رگرسیون‌های چیدمانی همچنان
  فقط با چشم قابل تشخیص‌اند.

---

## MOD-UI-001 | ۱۴۰۵/۰۶/۰۳ | سیستم متریال iOS 27 | **done ✓**

### جستجو (مرحله ۱ استاندارد)
واقعیت iOS 27 با جستجوی وب تأیید شد، نه حدس:
- **iOS 26** (شهریور ۱۴۰۴): زبان طراحی «Liquid Glass» — شفاف و عمق‌دار
- **iOS 27** (WWDC خرداد ۱۴۰۵، انتشار شهریور): اپل عمداً **عقب‌نشینی کرد** —
  یک «نوار تنظیم شفافیت» اضافه کرد چون شفافیت زیاد خوانایی را خراب می‌کرد،
  به‌علاوه آیکون‌های لایه‌دارتر و واضح‌تر.

**نتیجه‌گیری:** پیاده‌سازی صادقانه‌ی iOS 27 یعنی شیشه‌ی زیبا **به‌همراه
کنترل واقعی کاربر** — نه شیشه‌ی بیشتر. برای نرم‌افزار پزشکی این ایمنی
است، نه سلیقه: مبلغ یا شماره‌ی دندانِ ناخوانا یعنی خطای واقعی.

### تحلیل (مرحله ۲)
| گزینه | نتیجه |
|---|---|
| **توکن CSS + کنترل زنده** | ✅ انتخاب شد — یک منبع حقیقت، کل اپ بدون دست زدن به ۷۳ فایل |
| فقط Tailwind config | رد — کنترل زنده بدون بیلد مجدد ممکن نیست |
| کتابخانه‌ی UI | رد — مهاجرت ۷۳ فایل، درگیری با ساختار فعلی |

### پیاده‌سازی (مرحله ۳)
- `src/lib/materials.ts` — ۴ سطح (مات / ملایم / استاندارد / شیشه‌ای کامل)
- مقادیر ثابت پراکنده (blur: 40/20/60/50px) حذف و به سه متغیر ریشه تبدیل شد:
  `--glass-blur` · `--glass-opacity` · `--glass-saturate`
- همه‌ی کلاس‌ها (`glass`, `glass-dark`, `glass-teal`, `glass-ultra`,
  `glass-specular`, `tab-bar`) از همین توکن‌ها مشتق می‌شوند
- نوار پایین عمداً در هر سطح کدرتر است (روی محتوای متغیر قرار می‌گیرد)
- هایلایت لبه با نزدیک شدن به حالت مات محو می‌شود (وگرنه شبیه خط اضافی می‌شود)
- **دسترس‌پذیری:** `prefers-reduced-transparency` هم در CSS و هم در JS
  رعایت شده و بر انتخاب کاربر **اولویت دارد** (مطابق راهنمای اپل)
- تب جدید «ظاهر و شفافیت» در تنظیمات با اعمال آنی

### تست واقعی (مرحله ۵)
```
✓ materials.test.ts (13 tests)
Test Files  4 passed (4)  |  Tests  49 passed (49)
```
تست کلیدی: **تضمین ریاضی ترتیب سطوح** — اگر کسی مقادیر را دستکاری کند
و «مات» شفاف‌تر از «شیشه‌ای» شود، تست می‌شکند.
همچنین: مقدار ذخیره‌شده‌ی خراب، نبود `matchMedia`، و سطح نامعتبر
هیچ‌کدام نباید رابط کاربری را بشکنند — هر سه تست شد.

### ریسک باقی‌مانده
- تأیید بصری روی گوشی واقعی لازم است؛ من فقط کد و بیلد را می‌بینم.
- تایپوگرافی و فاصله‌گذاری (بخش دیگری از زبان طراحی) هنوز توکن‌محور
  نشده — فعلاً فقط لایه‌ی متریال.
- آیکون‌های لایه‌دارتر iOS 27 اعمال نشده (نیاز به کار طراحی گرافیکی).

---

## MOD-UI-002 | ۱۴۰۵/۰۶/۰۳ | تایپوگرافی سیال و مقیاس معنایی | **done ✓**

### جستجو (مرحله ۱)
اجماع طراحی ۲۰۲۶، جستجوشده نه حدس:
- اندازه‌های ثابت px **منسوخ**‌اند → `clamp()` سیال
- **نسبت** تُکِن می‌شود نه اندازه (تغییر یک عدد = هماهنگی کل هرم)
- جمله‌ی `rem` در clamp **اجباری** است، وگرنه زوم مرورگر می‌شکند
- line-height نسبتی: ۱.۴–۱.۶ متن، ۱.۱–۱.۲۵ تیتر
- طول خط ۶۰–۷۵ کاراکتر (رایج‌ترین شکست خوانایی وب)

### کشف کلیدی حین بررسی کد
Tailwind از `rem` استفاده می‌کند و `input/textarea/select` روی `16px`
**ثابت px** قفل است (ضدزوم iOS). یعنی سیال کردن **اندازه‌ی ریشه**
کل ۷۳ فایل را هم‌زمان و هماهنگ مقیاس می‌دهد، بدون دست زدن به هیچ‌کدام،
و ورودی‌ها چون px هستند درست دست‌نخورده می‌مانند.

### تحلیل (مرحله ۲)
| گزینه | نتیجه |
|---|---|
| **ریشه‌ی سیال + توکن نسبتی** | ✅ صفر تغییر در ۷۳ فایل، زوم سالم |
| بازنویسی همه‌ی `text-*` | رد — پرریسک |
| media query ثابت | رد — «یادگار گذشته» طبق منابع ۲۰۲۶ |

### پیاده‌سازی (مرحله ۳)
- `src/lib/typography.ts` — نسبت «سوم بزرگ» (۱.۲۵)، ۷ توکن معنایی
- ریشه: `clamp(0.9375rem, 0.8958rem + 0.2083vw, 1.0625rem)` — ۱۵→۱۷px
- بازه عمداً محافظه‌کارانه: چون در همه‌ی padding و gap ضرب می‌شود
- **مخصوص فارسی:** line-height متن ۱.۶۵ (توصیه‌ی لاتین ۱.۴ است) —
  خط فارسی به‌خاطر زیرنویس و کشیدگی (ی، ج، ع) فضای بیشتری می‌خواهد
- شبکه‌ی فاصله‌گذاری ۴ واحدی + کلاس `.prose-measure` با `68ch`

### تست واقعی (مرحله ۵)
```
✓ typography.test.ts (14 tests)
Test Files  6 passed (6)  |  Tests  72 passed (72)
```
تست‌های کلیدی: **ترتیب اکید نزولی توکن‌ها** (اگر caption از body
بزرگ‌تر شود می‌شکند)، **الزام جمله‌ی rem** در clamp (محافظ زوم)،
**آستانه‌ی خوانایی** ریزترین متن، و **حداقل ۱.۶ برای فارسی**.

مقدار `clamp` در CSS با اجرای واقعی فرمول ماژول راستی‌آزمایی شد و
اختلاف جزئی اصلاح شد — دو منبع حقیقت برای یک عدد، منشأ واگرایی خاموش است.

### ریسک باقی‌مانده
- تأیید بصری روی گوشی لازم است (مقیاس ریشه روی فاصله‌ها هم اثر دارد).
- کلاس‌های `.text-display` تا `.text-micro` ساخته شده ولی هنوز در
  کامپوننت‌ها جایگزین `text-*` تیلویند نشده — پذیرش تدریجی.

---

## MOD-UI-003 | ۱۴۰۵/۰۶/۰۳ | سیستم حرکت و رفع باگ پرش | **done ✓**

### جستجو (مرحله ۱)
اصول حرکت ۲۰۲۶، جستجوشده:
- توکن معنایی به‌جای عدد خام؛ ۴ پله کافی است (instant/fast/base/slow)
- **خروج سریع‌تر و بدون کشش از ورود** — خروج کند مثل کندی سیستم حس می‌شود
- فقط `transform` و `opacity` روی GPU؛ بقیه باعث بازمحاسبه‌ی چیدمان
- بازخورد فشردن **زیر ۱۰۰ms** وگرنه لمس بی‌جواب حس می‌شود
- الگوی **ضریب‌مدت**: صفر کردن یک متغیر = رعایت خودکار کاهش حرکت
- تأخیر پلکانی باید **افت** کند وگرنه فهرست بلند کند حس می‌شود

### بررسی کد موجود — یافته‌ی مثبت و یک باگ واقعی
✅ **مثبت:** بلوک سراسری `prefers-reduced-motion` روی `*` از قبل وجود
داشت. پایه‌ی دسترس‌پذیری سالم بود — تغییرش ندادم.

🔴 **باگ واقعی:** نشانگر تب پایین `width` را انیمیت می‌کرد (خط ۸۰۸).
`width` خاصیت چیدمانی است، یعنی هر فریم کل نوار تب دوباره چیده
می‌شد — علت کلاسیک پرش، دقیقاً روی عنصری که کاربر بیشترین لمس را
با آن دارد.

### پیاده‌سازی (مرحله ۳)
- `src/lib/motion.ts` — ۴ پله، ۴ منحنی، ریاضیات مسافت و پلکان
- توکن‌های CSS با **الگوی ضریب‌مدت**: همه‌ی مدت‌ها در
  `--duration-scalar` ضرب می‌شوند، که در حالت کاهش حرکت صفر می‌شود.
  یعنی رعایت دسترس‌پذیری **ساختاری** است، نه چیزی که هر انیمیشن
  جدید باید یادش باشد انتخاب کند.
- مدت خروج ۰.۷ برابر ورود، با منحنی بدون کشش
- **رفع باگ:** نشانگر تب از `width` به `transform: scaleX()` تبدیل شد —
  از نظر بصری یکسان، ولی روی GPU ترکیب می‌شود نه بازچیدمان هر فریم

### تست واقعی (مرحله ۵)
```
✓ motion.test.ts (17 tests)
Test Files  7 passed (7)  |  Tests  89 passed (89)
```
تست‌های کلیدی: **آستانه‌ی ۱۰۰ms** برای فشردن، **عدم کشش در منحنی
خروج** (کشش یعنی عنصرِ در حال رفتن برمی‌گردد)، **زیرخطی بودن رشد
مسافت** (مسافت ۴ برابر نباید مدت ۴ برابر بدهد)، **افت تأخیر پلکانی**
(آیتم پنجاهم نباید ۲ ثانیه دیر بیاید)، و **تشخیص خواص ناامن GPU**.

تأیید شد که هیچ انیمیشن روی خاصیت چیدمانی در کل فایل باقی نمانده.

### ریسک باقی‌مانده
- انیمیشن‌های موجود (کارت، دکمه) هنوز مقادیر ثابت خودشان را دارند و
  به توکن‌ها مهاجرت نکرده‌اند — پذیرش تدریجی، بدون ریسک رگرسیون.
- روانی واقعی حرکت فقط روی دستگاه واقعی قابل قضاوت است.

---

## MOD-UI-003 | ۱۴۰۵/۰۶/۰۳ | سیستم حرکت — اتصال کد یتیم | **done ✓**

### جستجو (مرحله ۱)
اجماع ۲۰۲۶، جستجوشده:
- `prefers-reduced-motion` در صدر فهرست «حتماً باید رفع شود»
- توکن معنایی به‌جای عدد خام
- **خروج سریع‌تر از ورود** — خروج کند حس تأخیر می‌دهد نه ظرافت
- فقط `transform`/`opacity` — نه width/height/top/left (بازچینش هر فریم)
- مدت‌زمان با مسافت رشد کند، ولی **زیرخطی**

### دو کد یتیم پیدا شد (نقض مستقیم استاندارد خودمان)
۱. **توکن‌های CSS مدت‌زمان** تعریف شده بودند ولی فقط **۱ قانون** از
   ۳۶ مورد مصرفشان می‌کرد. یعنی `--duration-scalar: 0` که قرار بود
   با یک خط کل حرکت را برای کاهش‌حرکت خاموش کند، عملاً روی ۹۷٪
   انیمیشن‌ها **بی‌اثر** بود.
۲. **ماژول `motion.ts`** کامل و تست‌شده بود ولی **هیچ فایلی از آن
   استفاده نمی‌کرد**.

### راه‌حل
- همه‌ی ۲۵+ مدت‌زمان ثابت به توکن تبدیل شد: **از ۱ مصرف به ۳۶**،
  صفر مقدار ثابت باقی ماند. حالا `prefers-reduced-motion` واقعاً
  ساختاری است، نه اسمی.
- `staggerDelay()` در Dashboard جایگزین فرمول دستی `index * 60` شد.
  آن فرمول خطی بود: فهرست ۳۰ تایی ۱.۸ ثانیه طول می‌کشید تا کامل
  ظاهر شود. نسخه‌ی زیرخطی این مشکل را ندارد و تست هم دارد.

### تست واقعی (مرحله ۵)
```
✓ motion.test.ts (17 tests)
Test Files  7 passed (7)  |  Tests  89 passed (89)
```
تست‌های کلیدی: خروج همیشه سریع‌تر از ورود، رشد زیرخطی مدت با مسافت،
ویژگی‌های چیدمان به‌عنوان ناامن رد می‌شوند، منحنی خروج بدون کشش.

حین اتصال، یک خطای واقعی گرفته شد (کامنت JSX داخل ویژگی‌های تگ)
که تایپ‌چک آن را قبل از commit گرفت.

### ریسک باقی‌مانده
- تأیید بصری روی گوشی لازم است؛ نگاشت مدت‌زمان‌ها (مثلاً ۰.۲۸s →
  `base`) قضاوت من بوده و ممکن است جایی کمی سریع/کند حس شود.
- `durationForTravel` و `exitDuration` هنوز در کد زنده مصرف نشده‌اند
  — تست‌شده‌اند ولی هنوز یتیم‌اند.

---

## MOD-UI-004 | ۱۴۰۵/۰۶/۰۳ | کنتراست WCAG 2.2 و فوکوس | **done ✓**

### جستجو (مرحله ۱)
- **WCAG 2.2 AA معیار قانونی است** (ADA آمریکا، EAA اروپا از خرداد ۱۴۰۵)
  آستانه: ۴.۵:۱ متن معمولی، ۳:۱ متن بزرگ و اجزای رابط
- **APCA** دقیق‌تر است ولی هنوز الزام قانونی نیست؛ منابع صراحتاً
  می‌گویند فعلاً جایگزین نشود → عمداً WCAG 2.2 پیاده شد نه APCA
- **معیار ۲.۴.۱۱ (ظاهر فوکوس)** تازه در WCAG 2.2 — «بسیاری از
  استایل‌های پیش‌فرض مرورگر را رد می‌کند»

### 🚨 دو شکست واقعی کشف شد (نه تئوری)
تست خودکار روی **پالت واقعی همین برنامه** اجرا شد:

| ترکیب | نسبت واقعی | لازم | نتیجه |
|---|---|---|---|
| متن سفید روی `primary-600` | **۳.۷۴:۱** | ۴.۵:۱ | ❌ **مهم‌ترین دکمه‌های برنامه** |
| متن سفید روی `primary-500` | **۲.۴۹:۱** | ۳:۱ | ❌ |

**۲. هیچ استایل فوکوسی وجود نداشت.** نه ناکافی — اصلاً نبود.
Tailwind preflight پیش‌فرض مرورگر را حذف می‌کند و جایگزینی اضافه
نشده بود. یعنی کاربر کیبورد اصلاً نمی‌دید کجاست — مانع کامل.

### راه‌حل
- دکمه‌ی اصلی و شماره‌های ویزارد: `primary-600` → **`primary-700`**
  (۵.۴۷:۱ ✅). هاور به ۸۰۰ رفت تا تغییر وضعیت محسوس بماند.
- حلقه‌ی فوکوس با `:focus-visible` اضافه شد (نه `:focus` — کاربر ماوس
  با کلیک حلقه نمی‌گیرد، کاربر کیبورد همیشه می‌گیرد)
- حلقه‌ی سفید بیرونی تا روی سطح تیره/سبزآبی هم دیده شود
- `forced-colors` برای حالت کنتراست بالای ویندوز

### تست واقعی (مرحله ۵)
```
✓ contrast.test.ts (21 tests)
Test Files  8 passed (8)  |  Tests  111 passed (111)
```
شکست‌های کشف‌شده صریحاً به‌عنوان تست ثبت شدند (`primary-600` برای
متن سفید کافی نیست) تا کسی دوباره به‌اشتباه از آن‌ها استفاده نکند.
از این پس هر تغییر رنگی که خوانایی را بشکند، **بیلد را می‌شکند**.

### ریسک باقی‌مانده
- ممیزی روی توکن‌های پالت انجام شد، نه روی هر ترکیب واقعی در ۷۳ فایل.
  ترکیب‌های موردی (مثلاً متن رنگی روی پس‌زمینه‌ی شیشه‌ای) پوشش داده
  نشده‌اند — نیاز به axe-core برای پوشش کامل.
- تأیید بصری تغییر رنگ دکمه‌ها روی گوشی لازم است.

---

## MOD-DATA-001 | ۱۴۰۵/۰۶/۰۳ | تعطیلات قمری محاسبه‌شده (AUD-011) | **done ✓**

### مشکل
۵۱ ورودی تعطیلات برای ۱۴۰۳/۱۴۰۴/۱۴۰۶/۱۴۰۷ همان ماه/روز ۱۴۰۵ را
تکرار می‌کردند. تعطیلات قمری هر سال شمسی ~۱۱ روز عقب می‌روند، پس
**ریاضی‌وار تضمین‌شده غلط** بودند. این داده مستقیم در نوبت‌دهی
دیده می‌شود: تاریخ غلط = نوبت در روز تعطیل یا بستن مطب در روز کاری.

### تحلیل (مرحله ۲)
| گزینه | نگهداری | نتیجه |
|---|---|---|
| هاردکد دستی هر سال | ❌ همین باگ سال بعد برمی‌گردد | رد |
| **محاسبه از تقویم قمری با `Intl`** | ✅ همه‌ی سال‌ها خودکار، بدون وابستگی | ✅ انتخاب |
| کتابخانه‌ی جدید | وابستگی اضافه | رد |

### پیاده‌سازی
- `src/lib/lunarHolidays.ts` — ۱۷ تعطیلی با تاریخ ثابت قمری
  (عاشورا همیشه ۱۰ محرم است) از طریق `Intl` با تقویم ام‌القری
- ۵۱ ورودی غلط **حذف شد**؛ ۱۱ ورودی تأییدشده‌ی ۱۴۰۵ حفظ شد
- تاریخ‌های محاسبه‌شده با پسوند **«(تقریبی)»** نمایش داده می‌شوند

**تصمیم مهندسی:** حذف داده‌ی غلط، نه نگه‌داشتنش. تعطیلی «قطعی‌نما
ولی غلط» بدتر از «تقریبی صادقانه» است، چون پرسنل به آن اعتماد می‌کنند.

**محدودیت صادقانه:** ایران تقویم قمری را با **رؤیت هلال** تعیین
می‌کند نه محاسبه؛ ام‌القری معمولاً ۰ تا ۱ روز فرق دارد. به همین
دلیل «تقریبی» برچسب خورده.

### 🚨 باگ دوم که تست یکپارچگی کشف کرد
`getHoliday` از `replace('/', '-')` استفاده می‌کرد که فقط **اولین**
اسلش را عوض می‌کند. پس «1406/05/01» می‌شد «1406-05/01» و هیچ کلیدی
مطابقت نمی‌کرد — یعنی تابع برای **هر تاریخ با فرمت اسلش بی‌صدا
null برمی‌گرداند**، حتی برای نوروز.

این باگ **از قبل وجود داشت** و تست‌های واحد نگرفته بودندش، چون با
فرمت خط‌تیره صدا زده شده بودند. فقط اسکن یک سال کامل آن را آشکار کرد.

**نتیجه: از ۰ تعطیلی به ۲۶ تعطیلی در سال ۱۴۰۶.**

### تست واقعی
```
✓ lunarHolidays.test.ts (15 tests)
Test Files  9 passed (9)  |  Tests  126 passed (126)
```
تست کلیدی: اثبات جابه‌جایی سالانه (عاشورا در دو سال متوالی تاریخ
متفاوت دارد) — دقیقاً همان چیزی که باگ اصلی نقض می‌کرد.

### ریسک باقی‌مانده
- تاریخ‌های محاسبه‌شده ±۱ روز با اعلام رسمی ایران فرق دارند.
- تعطیلات ملی غیرقمری (مثل ۲۲ بهمن) در جدول ثابت هستند و تأیید شدند،
  ولی تعطیلات مناسبتی موردی (اعلام دولت) قابل محاسبه نیستند.

---

## MOD-FEAT-001 | ۱۴۰۵/۰۶/۰۳ | نمای پیشرفت طرح درمان (AUD-010) | **done ✓**

### درخواست اصلی
«بیمار یک بار می‌آید، ۴۰ تا کار دارد. همه را یک‌جا ثبت کنیم و
جلسه‌به‌جلسه تیک بزنیم و بدانیم چه کاری انجام شده، چه کاری مانده.»

### کشف مهم حین بررسی کد (مرحله ۱)
زیرساخت داده **از قبل موجود بود**: درمان‌ها وضعیت
برنامه‌ریزی‌شده/در حال انجام/تکمیل‌شده دارند. کمبود واقعی **نمایش**
بود — پرونده‌ی بیمار فقط یک فهرست صاف زمانی نشان می‌داد. نمی‌شد در
یک نگاه فهمید چند درصد تمام شده، چقدر هزینه مانده، یا روی کدام
دندان چه چیزی باقی است.

پس مسئله ساختن انبار داده‌ی جدید نبود، آشکار کردن چیزی بود که
از قبل ذخیره می‌شد.

### پیاده‌سازی
- `src/lib/treatmentPlan.ts` — منطق جدا از UI برای قابل تست بودن
- نوار پیشرفت + سه عدد: انجام‌شده / باقی‌مانده / کل طرح
- **گروه‌بندی بر اساس دندان** — واحد کار دندانپزشک، نه ردیف فهرست
- دندان‌های ناتمام **اول** مرتب می‌شوند؛ گروه «عمومی» آخر
- **تیک یک‌ضربه‌ای:** برنامه‌ریزی‌شده ← در حال انجام ← تکمیل‌شده
  بدون باز کردن فرم، برای ثبت سریع حین جلسه
- `role="progressbar"` با `aria-valuenow` برای صفحه‌خوان

### تصمیم‌های طراحی و دلیلشان
- **درصد بر اساس تعداد نه ارزش ریالی:** «۸ از ۱۰ کار» برای بیمار
  معنادارتر از «۷۳٪ ارزش» است، و یک ایمپلنت گران نباید ده ترمیم
  کوچک را در نمودار محو کند.
- **تکمیل‌شده با ضربه‌ی دوباره تغییر نمی‌کند:** برگرداندن باید
  تصمیم آگاهانه باشد نه نتیجه‌ی لمس تصادفی روی گوشی.

### تست واقعی
```
✓ treatmentPlan.test.ts (19 tests)
Test Files  10 passed (10)  |  Tests  145 passed (145)
```
تست کلیدی: **درمان لغوشده نباید شمرده شود** — چون طبق سیاست پروژه
حذف دائمی نداریم، اگر شمرده شود بیمار برای همیشه «کار ناتمام»
خواهد داشت و درصد هرگز به ۱۰۰ نمی‌رسد.

حین پیاده‌سازی، تایپ‌چک سه خطای واقعی گرفت (بقایای کد قدیمی پس از
جایگزینی، براکت تکراری، نام اشتباه تابع بارگذاری) — همه قبل از
commit رفع شدند.

### ریسک باقی‌مانده
- ثبت ۴۰ کار همچنان یعنی ۴۰ بار باز کردن ویزارد درمان. نمای پیشرفت
  ساخته شد ولی **ورود دسته‌ای** هنوز نه — این بخش دوم درخواست است
  و کار جداگانه‌ای می‌خواهد.
- `treatments.phase_id` وجود ندارد، پس درمان‌ها به فازهای طرح درمان
  وصل نمی‌شوند؛ گروه‌بندی فعلاً بر اساس دندان است نه فاز.
- تأیید بصری روی گوشی لازم است.

---

## MOD-FEAT-002 | ۱۴۰۵/۰۶/۰۳ | ورود دسته‌ای طرح درمان | **done ✓**

### مشکل (نیمه‌ی دوم AUD-010)
نمای پیشرفت (MOD-FEAT-001) ردیابی ۴۰ کار را ممکن کرد، ولی **ثبت**
اولیه همچنان یعنی ۴۰ بار باز کردن ویزارد ۴ مرحله‌ای — یعنی همان
مرحله‌ای که کاربر گفت «بیمار یک بار می‌آید» عملاً غیرعملی بود.

### تحلیل (مرحله ۲)
| گزینه | سرعت ۴۰ کار | خطاپذیری | نتیجه |
|---|---|---|---|
| ویزارد فعلی × ۴۰ | ~۱۰ دقیقه | زیاد (خستگی) | رد |
| **سبد: دندان ← رویه ← افزودن** | ~۲ دقیقه | کم | ✅ انتخاب |
| وارد کردن از فایل | سریع ولی نامناسب بالین | زیاد | رد |

**دلیل انتخاب:** دندانپزشک همان‌طور فکر می‌کند — دندان، بعد کار.

### پیاده‌سازی
- `src/lib/bulkTreatment.ts` — منطق جدا از UI برای تست‌پذیری
- انتخاب دندان از چارت پالمر + رویه با قیمت پیش‌فرض
- جمع کل **زنده** + شمارش دندان‌های درگیر
- ویرایش تعداد/قیمت/تخفیف هر قلم داخل سبد
- ذخیره‌ی **ترتیبی** نه موازی: هر ثبت یک عملیات سینک صف می‌کند و
  حفظ ترتیب یعنی صف آفلاین به همان ترتیب ورود پخش می‌شود
- تکراری **هشدار می‌دهد نه مسدود**: دو ترمیم روی یک دندان در دو
  سطح واقعی است، ولی دوبار زدن اتفاقی هم رایج

### 🚨 یافته از دیتابیس زنده
کوئری واقعی نشان داد **هیچ‌کدام از ۴ رویه‌ی موجود `default_price`
ندارند**. بدون اطلاع‌رسانی، سبد قیمت‌ها را صفر پر می‌کرد،
اعتبارسنجی جلوی ثبت را می‌گرفت، و کاربر بدون فهمیدن دلیل گیر می‌کرد.
هشدار صریح در بالای مودال اضافه شد.

### تست واقعی
```
✓ bulkTreatment.test.ts (24 tests)
Test Files  11 passed (11)  |  Tests  169 passed (169)
```
تست‌های کلیدی مالی: **تخفیف بزرگ‌تر از مبلغ عدد منفی نمی‌سازد**
(وگرنه جمع کل سبد کم می‌شد و کلینیک ضرر می‌کرد)، تخفیف از جمع کل
کم می‌شود نه از قیمت واحد، و سبد ۴۰ تایی درست جمع می‌زند.

### ریسک باقی‌مانده
- تأیید بصری روی گوشی لازم است — سبد طولانی روی صفحه‌ی کوچک
  ممکن است نیاز به بهبود پیمایش داشته باشد.
- اقلام با وضعیت «برنامه‌ریزی‌شده» ثبت می‌شوند؛ اگر کاری همان جلسه
  انجام شده باشد باید جداگانه تیک بخورد.

---

## MOD-SEC-002 | ۱۴۰۵/۰۶/۰۸ | قفل جدول بایگانی قدیمی (AUD-002) | **done ✓**

### مشکل
جدول `minadent_entity_records` روی RLS روشن بود ولی **صفر سیاست**
داشت — یعنی برای همه مسدود، بدون اینکه معلوم باشد عمدی است یا فراموش‌شده.

### بررسی واقعی
- **۴۰ ردیف داده** دارد (پس حذف بی‌فکرانه اشتباه بود)
- محتوا: Treatment ۹، Transaction ۸، Staff ۸، Patient ۷، Appointment ۵، Doctor ۳
- آخرین نوشتن: ۳۰ مرداد ۱۴۰۵
- **هیچ فایلی در `src/` به آن ارجاع نمی‌دهد** (بررسی با grep)

**تشخیص:** بازمانده‌ی معماری اولیه — همه‌چیز در یک جدول JSON عمومی.
بعداً به جداول اختصاصی مهاجرت شد و این رها ماند.

### تصمیم
**حذف نشد** (سیاست پروژه حذف دائمی را ممنوع کرده، و این داده ممکن
است برای بازیابی سابقه لازم شود). در عوض:
- `COMMENT` صریح روی جدول: بایگانی، فقط‌خواندنی، کد جدید استفاده نکند
- سیاست `SELECT` برای کاربر لاگین‌کرده
- **هیچ سیاست INSERT/UPDATE/DELETE ساخته نشد** — عمدی
- `REVOKE` مجوزهای نوشتن در سطح Postgres

**دو لایه‌ی مستقل:** سیاست RLS تعیین می‌کند «کدام ردیف‌ها»، ولی GRANT
تعیین می‌کند «آیا اصلاً دسترسی هست». حین تست کشف شد که فقط سیاست
کافی نیست — خطای `permission denied` گرفتیم و مجوز پایه هم لازم بود.

### تست واقعی
| سناریو | نتیجه |
|---|---|
| کاربر لاگین‌کرده ← خواندن | **۴۰ ردیف** ✅ |
| کاربر لاگین‌کرده ← درج | **permission denied** ✅ |

### ریسک باقی‌مانده
اگر روزی این داده‌ی قدیمی واقعاً بی‌فایده تشخیص داده شد، حذفش باید
تصمیم آگاهانه‌ی مهدی باشد نه پاک‌سازی خودکار.

---

## MOD-FEAT-013 | ۱۴۰۵/۰۶/۰۸ | وضعیت دندان با شکل، نه فقط رنگ (COMP-19/20/22/106) | **done ✓**

### مشکل
چارت هر دندانِ موجود را با **همان طرح آناتومیک** می‌کشید و فقط رنگ
پرکردنش را عوض می‌کرد. رنگ نمی‌تواند بگوید «این ایمپلنت است»: ایمپلنت
یک دندان با ته‌رنگ دیگر نیست، یک پیچ رزوه‌دار در استخوان است.

دندانپزشکی که یک قوس را اسکن می‌کند، **شکل را قبل از رنگ می‌خواند** —
و خواندن قوس در یک نگاه، کل کارِ چارت است.

### راه‌حل
`src/lib/toothVisual.ts` — تصمیم «چطور کشیده شود» جدا از «چه رنگی
باشد»، به‌صورت توابع خالص و تست‌شده.

**سه شکل بنیادی:**
- `implant` → پیچ با abutment، گردن، بدنه‌ی مخروطی و **رزوه**
- `absent` → سوکت خالی
- `tooth` → طرح آناتومیک موجود

**دو نشانه‌ی روی دندان:**
- **ریشه‌ی پرشده** برای عصب‌کشی — ضخامت و کدری ریشه بالا می‌رود
- **تاج توپر** برای روکش/ونیر/بریج — تاج طبیعی دیگر آن چیزی نیست که
  می‌بینی

### تصمیم‌های بالینی که ارزش کار در آن‌هاست

**۱. `post` و `pin` هم ریشه‌ی پرشده می‌گیرند.**
پست و پین لنگرهایی هستند که **داخل کانال** سیمان می‌شوند — یعنی کانال
قبلش درمان شده. کشیدن ریشه‌ی توخالی که یک پست را نگه داشته، چیزی را
نشان می‌دهد که وجود ندارد.

**۲. دندان شیری هرگز پرمولر نیست.**
منطق قدیمی `number % 10 >= 4` بود، پس `54` را پرمولر حساب می‌کرد. اما
پرمولر شیری وجود ندارد — ۵۴ مولر اول شیری است. روی چارت کودک، تعداد
ریشه‌ی اشتباه می‌کشید.

**۳. روکش و ونیر و بریج یکی حساب می‌شوند.**
در جنس و در صورتحساب فرق دارند، ولی روی چارت هر سه یک معنی دارند.

### حذف مسیر دوم
`isUpper`، `isMolar`، `isPremolar` و `isCanine` به‌صورت درون‌خطی با
`number % 10` در خود کامپوننت حساب می‌شدند — یعنی **منبع دوم حقیقت**،
که ممنوعیت «دو مسیر برای یک مقصد» است. حالا همه از `toothVisual`
می‌آیند. باگ دندان شیری از همین دوگانگی آمده بود.

### دو چیزی که در حین کار درست کردم
| مشکل | اصلاح |
|---|---|
| تست «جامعیت» جامع نبود — فهرست شرایط را از حافظه نوشتم و `post` و `pin` جا افتاد | فهرست به `Record<ToothCondition, true>` تبدیل شد تا **کامپایلر** تضمین کند، نه حافظه‌ی من |
| `rootCount` و `isPrimaryTooth` نوشته و تست شدند ولی هیچ مصرف‌کننده‌ای نداشتند | **حذف شدند.** کد یتیم ممنوعیت مطلق است؛ وقتی هندسه‌ی ریشه واقعاً آناتومیک شد برمی‌گردند |

### دسترس‌پذیری
هر SVG حالا `role="img"` و `aria-label` دارد که وضعیت را می‌گوید —
«دندان ۱۶ — ایمپلنت». قبلاً صفحه‌خوان فقط یک عدد لخت می‌گرفت.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.157.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 19 passed | Tests 357 passed (357)
✓ vite build — موفق
```
۳۴۰ ← **۳۵۷ تست**.

بررسی کد یتیم: هر ۶ تابع باقی‌مانده در کامپوننت‌ها صدا زده می‌شوند.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده — و این مورد بیش از بقیه به آن نیاز دارد.**
  شکل پیچ در اندازه‌ی ۴۸ پیکسل روی گوشی باید دیده شود. اگر رزوه‌ها
  بهم بریزند یا تاج توپر خیلی تیره باشد، باید تنظیم شود.
- تعداد ریشه هنوز آناتومیک نیست: مولر بالا سه ریشه دارد ولی چارت دو تا
  می‌کشد. `rootCount` نوشته و تست شده بود ولی چون هندسه هنوز از آن
  استفاده نمی‌کرد حذف شد — کار جداگانه‌ای است.
- `COMP-103` (گلیف انتخاب سطح زیر هر دندان) ساخته نشد.
- `COMP-104` (چند رنگ حاشیه هم‌زمان با معانی مختلف) ساخته نشد.

---

## MOD-FEAT-012 | ۱۴۰۵/۰۶/۰۸ | درصد اشغال، میانگین انتظار و بیماران در مطب (COMP-133/134/135) | **done ✓**

### مشکل
صفحه‌ی نوبت‌دهی **ردیف می‌شمرد**: امروز / در انتظار / تکمیل‌شده.
شمردن ردیف حجم را می‌گوید، نه اینکه روز پر است یا نه. هشت نوبت برای
چهار یونیت یک روز خلوت است و برای یک پزشک نیم‌شیفت یک روز پرشده.

### راه‌حل
`src/lib/dayMetrics.ts` — کاملاً خالص، بدون `Date.now()` و بدون DOM.
«اکنون» به‌عنوان ورودی پاس داده می‌شود تا همان روز در تست در هر لحظه‌ای
قابل ارزیابی باشد.

**migration لازم نداشت.**

### تصمیم‌هایی که ارزش کار در آن‌هاست

**۱. ظرفیت با دقیقه سنجیده می‌شود، نه با تعداد اسلات.**
نوبت‌ها هم‌اندازه نیستند. سه ایمپلنت می‌تواند روزی را پر کند که شمارش
اسلات آن را «یک‌سوم پر» می‌نامد. تست این را پین می‌کند.

**۲. درصد اجازه دارد از ۱۰۰ رد شود.**
Overbooking واقعاً اتفاق می‌افتد. محدود کردن به ۱۰۰ دقیقاً همان روزی
را پنهان می‌کند که مدیر باید ببیند. بالای ۱۰۰ قرمز می‌شود.

**۳. زمان استراحت از ظرفیت کم می‌شود.**
اگر ناهار جزو ظرفیت حساب شود، هر کلینیکی همیشه کم‌کار به نظر می‌رسد —
و عددی که همیشه پایین است، عددی است که کسی به آن عمل نمی‌کند.

**۴. میانگین انتظار وقتی کسی منتظر نیست `null` است، نه صفر.**
نوبت ساعت ۱۵:۰۰ وقتی ساعت ۱۴:۳۰ است انتظار نیست، آینده است. کارت «—»
نشان می‌دهد، نه یک صفرِ آرامش‌بخشِ بی‌معنا.

**۵. «تأیید شده» یعنی بیمار زنگ زده، نه اینکه در ساختمان است.**
پس در شمارش «در مطب» حساب نمی‌شود.

**۶. زمان نامعتبر `null` برمی‌گرداند، نه `NaN`.**
یک `NaN` بی‌سروصدا در هر میانگینی که لمسش کند پخش می‌شود.

**۷. نوبت با زمان خراب صفر دقیقه حساب نمی‌شود** — پیش‌فرض می‌گیرد.
وگرنه یک ردیف معیوب روز را خلوت‌تر از واقعیت نشان می‌دهد.

### حالت خالی گویا
اگر برنامه‌ی کاری پزشکان برای امروز ثبت نشده باشد، کارت به‌جای «۰٪»
می‌نویسد «برنامه‌ی کاری پزشکان برای امروز ثبت نشده» — عدد نیست چون
شیفتی تعریف نشده، نه چون روز خالی است. همان ممنوعیت «پیام کلی».

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.156.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 18 passed | Tests 340 passed (340)
✓ vite build — موفق
```
۳۱۴ ← **۳۴۰ تست** (۲۶ تست جدید).

بررسی کد یتیم: هر ۹ تابع `dayMetrics` یا از `summariseDay` صدا زده
می‌شوند یا مستقیماً از صفحه — هیچ‌کدام معلق نیست.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** چهار کارت جدید روی موبایل دو ستون می‌شوند؛
  چیدمانشان را باید مهدی ببیند.
- ظرفیت از `doctor_schedules` می‌آید. اگر کلینیک شیفت‌ها را وارد نکرده
  باشد، کارت اشغال عدد نمی‌دهد — عمدی، ولی یعنی این قابلیت به پر کردن
  تنظیمات وابسته است.
- مرخصی و تعطیلی پزشک لحاظ نمی‌شود؛ فقط برنامه‌ی هفتگی. `COMP-136`
  (نمایش ناموجودی پزشک) هنوز ساخته نشده.
- اعداد در لحظه‌ی رندر حساب می‌شوند و خودکار تازه نمی‌شوند. `COMP-174`
  نشان داد رقیب بازه‌ی بروزرسانی خودکار دارد.

---

## MOD-FEAT-011 | ۱۴۰۵/۰۶/۰۸ | هشدار شناور بالینی و بدهکار (COMP-33/34) | **done ✓**

### مشکل
حساسیت دارویی، بیماری زمینه‌ای و داروی مصرفی بیمار در تب «سوابق پزشکی»
دفن شده بودند — یعنی فقط اگر کسی عمداً می‌رفت آن تب را باز می‌کرد.
دندانپزشکی که نداند بیمار وارفارین می‌خورد یا به پنی‌سیلین حساس است،
مشکل واقعی دارد. بدهی هم همین‌طور: در تب پرداخت‌ها بود، نه جلوی چشم.

### علت ریشه‌ای
داده‌ها از قبل وجود داشتند (`allergies`، `medical_conditions`،
`medical_history`، `medications`) ولی هیچ‌جا **مزاحم** نمی‌شدند.
اطلاعاتی که برای دیدنش باید بگردی، در لحظه‌ی درمان دیده نمی‌شود.

### راه‌حل
`src/lib/patientAlerts.ts` — منطق خالص و تست‌پذیر — به‌علاوه‌ی
`PatientAlerts.tsx` که فقط ظاهر را تصمیم می‌گیرد.

**migration لازم نداشت.** هر چهار فیلد از قبل روی `patients` بودند.

### 🔴 تصمیم اصلی: فیلدهای «ندارد» نباید کارت قرمز بسازند
کلینیک‌ها فیلدهای اجباری‌نما را با «ندارد» پر می‌کنند، نه خالی
می‌گذارند. یک کارت قرمز که می‌گوید «حساسیت: ندارد» **بدتر از نبودن
هشدار است**: پرسنل یاد می‌گیرند کارت قرمز معمولاً نویز است و خواندنش
را رها می‌کنند — و آن یک باری که نوشته «پنی‌سیلین» هم با بقیه بسته
می‌شود.

پس یک واژگان از مقادیر «هیچ‌چیز برای گزارش» ساخته شد، با نرمال‌سازی
حروف عربی/فارسی قبل از جستجو. فیلد نیمه‌ویرایش‌شده («دیابت، ندارد»)
هم فقط «دیابت» را نشان می‌دهد.

### 🔴 ترتیب شدت با تست قفل شد
```
۰ حساسیت    — می‌تواند ظرف چند دقیقه بکشد
۱ بیماری     — تغییر می‌دهد چه کاری روی صندلی امن است
۲ دارو       — عمدتاً ریسک خونریزی؛ مهم ولی کندتر
۳ بدهی       — پول. هرگز بالای یک واقعیت بالینی نه
```
اگر کارت بدهکار بالای کارت ضدانعقاد بیفتد، یک خطای ایمنی بیمار است.
پس ترتیب با تست پین شد، نه به ترتیب رندر JSX سپرده شد.

رنگ هم همین را می‌گوید: بالینی قرمز، مالی کهربایی — بدون خواندن متن
معلوم است «این می‌تواند به بیمار آسیب بزند» یا «این به کلینیک هزینه
دارد».

### تصمیم‌های دیگر
- **`medical_conditions` و `medical_history` با هم خوانده می‌شوند.**
  کلینیک‌ها سر اینکه کدام فیلد را پر کنند توافق ندارند؛ خواندن فقط
  فیلد مرتب یعنی از دست دادن هشدار روی نیمی از پرونده‌ها. تکراری‌ها
  حذف می‌شوند.
- **بستن هشدار موقتی است.** به ازای بیمار و به ازای هر بار باز کردن
  پرونده — هرگز ذخیره نمی‌شود. حساسیتی که ماه پیش تأیید شد باید دفعه‌ی
  بعد دوباره مزاحم شود، وگرنه اصلاً چرا کارت؟
- **مانده‌ی منفی هشدار نیست** — یعنی کلینیک به بیمار بدهکار است.
- سقف اعتبار اگر رد شده باشد، خط دوم اضافه می‌کند.

### نتیجه‌ی تست واقعی
تست اول **رد شد** و یک خلأ واقعی را نشان داد: «نداري» با املای عربی
نرمال می‌شد ولی در واژگان نبود، پس یک کارت قرمز اشتباه می‌ساخت.
واژگان اصلاح شد (`نداری`، `مشکلی ندارد`، `بدون سابقه` و چند مورد دیگر).

```
npm run verify
✓ sw.js cache bumped to v1.155.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 17 passed | Tests 314 passed (314)
✓ vite build — موفق
```
۲۹۰ ← **۳۱۴ تست** (۲۴ تست جدید).

### تأیید بصری از رقیب
`COMP-33`/`COMP-34`/`COMP-191`: کارت قرمز شناور، دکمه‌ی × برای بستن،
چند هشدار روی هم پشته — همان الگویی که پیاده شد.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** موقعیت کارت‌ها روی موبایل (بالا-چپ، عرض
  محدود) را باید مهدی ببیند. اگر با هدر تداخل داشت، `top-20` باید
  تنظیم شود.
- واژگان «ندارد» فهرست ثابت است. اگر کلینیکی عبارت دیگری بنویسد، یک
  کارت اشتباه ساخته می‌شود. فهرست باید با دیدن داده‌ی واقعی رشد کند.
- چیپ‌های سربرگ (`COMP-78`) عمداً قابل بستن **نیستند**. کارت شناور
  می‌تواند بسته شود؛ چیپ تا پایان ویزیت سر جایش می‌ماند. اگر روی
  پرونده‌های شلوغ سربرگ را بهم بریزد، باید محدود شود.

**یادداشت روند:** اولین بار `alertChips()` نوشته و تست شد ولی هیچ‌جا
صدا زده نمی‌شد. آن را در «ریسک باقی‌مانده» نوشتم که «عمدی است» — که
غلط بود: **کد یتیم ممنوعیت مطلق است و مستندکردنش مجازش نمی‌کند.**
قبل از کامیت به سربرگ وصل شد.

---

## MOD-FEAT-009 | ۱۴۰۵/۰۶/۰۸ | مکان قفسه، زنگ پیگیری و صف تحویل لابراتوار (بازیابی) | **done ✓**

### مشکل
کار لابراتوار **دیجیتالی** ردیابی می‌شد ولی **فیزیکی** نه. وقتی کار از
لابراتوار برمی‌گشت، هیچ‌کس نمی‌دانست روی کدام قفسه است — پرسنل جعبه‌ها
را دستی باز می‌کردند. و زنگ پیگیری وجود نداشت: نبودِ کار در روز موعد
معلوم می‌شد، نه چند روز قبلش که هنوز می‌شد کاری کرد.

### علت ریشه‌ای
سومین ماژول از پنج ماژول گم‌شده در revert اضطراری v1.147.0
(کامیت `c47a4c1`).

### راه‌حل
هفت ستون جدید روی `lab_orders` + `src/lib/labShelf.ts`.

**تصمیم‌های طراحی که عمدی‌اند:**
- **آدرس قفسه سه‌تکه است، نه یک فیلد آزاد.** قفسه / شماره / فضا. یک
  فیلد متنی آزاد به دست‌خط‌های ناهماهنگ تبدیل می‌شود.
- **همه یا هیچ.** «قفسه A» بدون شماره یعنی باز کردن همه‌ی جعبه‌های A —
  بدتر از هیچ. هم در کد و هم با `check constraint` در دیتابیس.
- **`alarm_date` جدا از `deadline`.** زنگ پیگیری چند روز جلوتر است.
- **«تحویل شده» تا وقتی «اتمام کار» تیک نخورده غیرفعال است.** کار
  تمام‌نشده نمی‌تواند به بیمار برسد.
- **صف «آماده تحویل»** با نمایش آدرس قفسه، و علامت‌گذاری موردهایی که
  آدرس ندارند — همان‌ها که در جعبه گم می‌شوند.

### ⚠️ نقصی که در کد بازیابی‌شده پیدا شد
دکمه‌ی سریع «ثبت تحویل» در صف، فقط `{ delivered: true }` می‌فرستاد.

ولی `readyForDelivery()` سفارش‌هایی را هم برمی‌گرداند که
`work_done = false` دارند اما `status` آن‌ها `ready` یا `received` است —
یعنی **هر ردیفی که قبل از MOD-FEAT-009 ساخته شده**، چون مقدار پیش‌فرض
ستون `false` است.

دیتابیس `check (delivered = false or work_done = true)` دارد و این را
رد می‌کند.

**چرا این بدتر از یک خطای ساده است:** برنامه آفلاین‌اول است. نوشتن در
Dexie موفق می‌شود، به پرسنل گفته می‌شود «تحویل ثبت شد»، و خطا فقط
بعداً در صف سینک ظاهر می‌شود. یعنی کار تحویل‌شده در ظاهر ثبت شده ولی
روی سرور نیست.

اصلاح: تابع `deliveryPatch()` که `work_done` را هم اجباراً true می‌کند.
منطق در کدِ تست‌شده گذاشته شد، نه داخل یک `onClick` در JSX.

### نتیجه‌ی تست واقعی

**migration 022 روی Supabase زنده اعمال شد:**
```
new_cols 7 · new_constraints 2 · ready_idx 1 · rls_enabled true
```

**و محدودیت‌ها واقعاً تست شدند** (درج عمدی داده‌ی بد داخل بلاک
`do $$` با rollback):
```
partial_shelf_blocked          = true
delivery_before_work_blocked   = true
→ BOTH_CONSTRAINTS_ENFORCED_ROLLED_BACK
```

```
npm run verify
✓ sw.js cache bumped to v1.154.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 16 passed | Tests 290 passed (290)
✓ vite build — موفق
```
۲۵۸ ← **۲۹۰ تست** (۳۰ تست بازیابی‌شده + ۲ تست جدید برای `deliveryPatch`).

نکته: `lab_orders` از قبل در `TABLE_NAMES` و Dexie بود و ستون‌های جدید
ایندکس نمی‌خواهند، پس **نسخه‌ی Dexie بالا نرفت** — بررسی شد که هیچ
`.where()` روی ستون‌های جدید زده نمی‌شود.

### تأیید بصری از رقیب
`COMP-152` و `COMP-153`: همان سه فیلد قفسه و همان «تاریخ آلارم» جدا از
تاریخ تحویل در فرم لابراتوار minadent.ir دیده شد.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** فرم لابراتوار و کارت «آماده تحویل» را باید
  مهدی ببیند.
- **Dexie محدودیت ندارد.** اگر ترکیب نامعتبری آفلاین ساخته شود، فقط
  هنگام push رد می‌شود. `deliveryPatch()` مسیر اصلی را می‌بندد، ولی فرم
  دستی هنوز به اعتبارسنجی سمت کلاینت تکیه دارد. یک آینه‌ی کامل
  محدودیت‌ها در لایه‌ی سینک کار جداگانه‌ای است.
- `COMP-160` («وضعیت برگشت متریال» با جزئیات بیشتر) پوشش داده نشد —
  ما فقط یک boolean داریم.

---

## MOD-FEAT-006 | ۱۴۰۵/۰۶/۰۸ | نمایش زنده‌ی سقف بیمه در فرم درمان (بازیابی) | **done ✓**

### مشکل
`MOD-FEAT-005` بیمه‌نامه و سقف را ذخیره می‌کرد ولی **هیچ اثری روی قیمت
نداشت**. منطق `splitCoverage` نوشته شده بود و هیچ‌جا صدا زده نمی‌شد —
یعنی دقیقاً «کد یتیم»، که ممنوعیت مطلق پروژه است.

### علت ریشه‌ای
دومین ماژول از پنج ماژول گم‌شده در revert اضطراری v1.147.0
(کامیت `544c1c3`).

### راه‌حل
بیمه‌نامه‌ها و ادعاهای بیمار **هنگام باز شدن فرم درمان** بارگذاری
می‌شوند، نه از قبل برای همه‌ی بیماران — این صفحه می‌تواند صدها ویزیت
داشته باشد.

سپس دو جا نمایش داده می‌شود:
1. **بنر زنده در خود فرم** — سهم بیمه، سهم بیمار، مانده سقف پس از این
   درمان. رنگ کهربایی وقتی سقف گاز می‌گیرد.
2. **دو ردیف اضافه در پنجره‌ی تأیید** — سهم واقعی بیمار در لحظه‌ی ثبت
   دیده می‌شود، نه بعد از اینکه ادعا کم برگشت.

وقتی هیچ بیمه‌نامه‌ای اعمال نمی‌شود، **هیچ بنری نشان داده نمی‌شود** —
نه بنری با عدد صفر که گمراه‌کننده است.

### ⚠️ نقصی که در کد بازیابی‌شده پیدا شد
کد اصلی تقسیم را با `new Date()` حساب می‌کرد — یعنی **تاریخ امروز**،
نه تاریخ ویزیت.

اثر واقعی: منشی درمان‌های هفته‌ی پیش را امروز وارد می‌کند. اگر
بیمه‌نامه بین آن دو تاریخ منقضی شده باشد، نرم‌افزار سهم بیمه را صفر
حساب می‌کند در حالی که روز درمان معتبر بوده — یا برعکس، بیمه‌نامه‌ای
که تازه شروع شده به ویزیت قبلی‌اش پوشش می‌دهد.

این **محاسبه‌ی مالی غیردقیق** است و ممنوعیت مطلق پروژه.

اصلاح: `encounters.find(...)` که همان چند خط پایین‌تر از قبل وجود
داشت، حالا `encounter_date` را می‌دهد. fallback به امروز فقط وقتی
ویزیت پیدا نشود — همان تاریخی که رکورد با آن ساخته می‌شود.

این با `MOD-FEAT-008` اشتباه گرفته نشود: آن یکی **محاسبه‌ی مجدد
اختیاری** درمان‌های قبلی را اضافه می‌کند، که کار جداگانه‌ای است.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache bumped to v1.153.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 15 passed | Tests 258 passed (258)
✓ vite build — موفق
```
۲۵۰ ← **۲۵۸ تست** (۶ تست بازیابی‌شده + ۲ تست جدید که ثابت می‌کنند
تقسیم به تاریخ ویزیت وابسته است، نه تاریخ امروز).

### تأیید بصری از رقیب
`COMP-151` و `COMP-178` همین رفتار را در minadent.ir نشان دادند —
بنر قرمز «به دلیل محدودیت سقف تعهد، تمام یا بخشی از هزینه به‌صورت آزاد
محاسبه می‌گردد». متن هشدار ما تقریباً همان است.

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** بنر کهربایی و دو ردیف پنجره‌ی تأیید را
  باید مهدی ببیند.
- `fetchInsuranceClaims()` همه‌ی ادعاهای کلینیک را می‌گیرد و سمت کلاینت
  فیلتر می‌کند. برای مقیاس فعلی بی‌اهمیت است؛ با رشد باید کوئری
  به بیمار محدود شود.
- `MOD-FEAT-007` هنوز بازیابی نشده: سهم بیمه **محاسبه و نمایش داده
  می‌شود ولی روی درمان ذخیره نمی‌شود**. یعنی نقل‌قولی که به بیمار داده
  شد جایی ثبت نمی‌ماند. نیاز به migration 021.
- بیمه‌ی تکمیلی (`COMP-155`) پشتیبانی نمی‌شود — فقط یک لایه بیمه.

---

## MOD-FEAT-005 | ۱۴۰۵/۰۶/۰۸ | سقف بیمه‌ی هر بیمار (بازیابی) | **done ✓**

### مشکل
`insurance_companies` فقط شرکت بیمه را نگه می‌داشت. اینکه **این بیمار**
چه تعهدی دارد — شماره بیمه‌نامه، بازه‌ی اعتبار، درصد پوشش، و مهم‌تر از
همه **سقف تعهد** — هیچ جایی مدل نشده بود.

### علت ریشه‌ای
این ماژول یک بار ساخته و تست شده بود (کامیت `56c8d4a`)، ولی محتوایش با
revert اضطراری v1.147.0 از main برداشته شد و برنگشت. تا امروز فرض بر
این بود که داریمش. با خواندن کد معلوم شد نداریم: `CHANGELOG` فقط
`MOD-FEAT-001/002/010` را داشت و `Treatment` هیچ فیلد بیمه‌ای نداشت.

### راه‌حل
`src/lib/insurance.ts` بازیابی شد: `splitCoverage`، `usedCeiling`،
`remainingCeiling`، اعتبارسنجی بیمه‌نامه و انتخاب بهترین بیمه‌ی فعال.
**مصرف سقف از `insurance_claims` مشتق می‌شود، هرگز ذخیره نمی‌شود** —
یک شمارنده‌ی ذخیره‌شده با واقعیت drift می‌کند.

### ⚠️ دو نقص در همان کد بازیابی‌شده
| نقص | چه اتفاقی می‌افتاد | اصلاح |
|---|---|---|
| **تب تکراری** | کامیت اصلی یک تب دوم با **همان کلید `insurance`** اضافه می‌کرد. نتیجه: دو تب برای یک مقصد (نقض ممنوعیت مطلق) و هر دو پنل هم‌زمان رندر می‌شدند | یک تب، بدنه‌ی مشترک: خلاصه‌ی قدیمی + پنل جدید |
| **نسخه‌ی Dexie اشتباه** | کد اصلی `version(12)` بود، چون روی شاخه‌ای نوشته شده بود که `version(11)` را به `tooth_notes` داده بود. آن نسخه هرگز منتشر نشد. اگر ۱۲ می‌ماند، بین کد و دیتابیس هر مرورگر نصب‌شده شکاف می‌افتاد | `version(11)` |

### ترتیب اجرا — درس همان قطعی
**اول migration، بعد کد.** `patient_policies` فقط *پس از* اعمال
migration 020 روی سرور زنده به `TABLE_NAMES` اضافه شد. برعکسش دقیقاً
همان باگی است که یک بار کل سینک را شکست.

### نتیجه‌ی تست واقعی
migration 020 روی Supabase زنده اعمال شد. کوئری تأیید:
```
columns 13 · indexes 4 · rls_enabled true
policies 3 (SELECT, INSERT, UPDATE) · open_policies 0
```
بدون سیاست DELETE — بیمه‌ی منقضی روی پرونده می‌ماند چون درمان‌های
گذشته با آن قیمت‌گذاری شده‌اند.

کوئری با همان شکلی که `pullTable()` می‌زند: **۰ ردیف، بدون خطا** —
یعنی سینک این جدول را می‌بیند و نمی‌شکند.

```
npm run verify
✓ tsc --noEmit — بدون خطا
✓ Test Files 15 passed | Tests 250 passed (250)
✓ vite build — موفق
```
۲۱۱ ← **۲۵۰ تست** (۳۹ تست بازیابی‌شده‌ی insurance).

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** تب بیمه و فرم بیمه‌نامه را باید مهدی ببیند.
- `MOD-FEAT-006/007/008` هنوز بازیابی نشده‌اند: سقف در **فرم درمان**
  دیده نمی‌شود و سهم بیمه روی درمان ذخیره نمی‌شود. یعنی الان
  بیمه‌نامه ثبت می‌شود ولی هنوز روی قیمت درمان اثر ندارد.
- `renderInsurance()` قدیمی هنوز دو فیلد متنی را نشان می‌دهد که در تب
  «مشخصات» هم هستند. عمداً حذف نشد تا داده‌ی کلینیک‌هایی که فقط همان
  فیلدها را پر کرده‌اند از دست نرود؛ ولی این یک تکرار است و باید در
  MOD-FEAT-006 تعیین تکلیف شود.

---

## MOD-FEAT-010 | ۱۴۰۵/۰۶/۰۸ | جستجوی سراسری با Ctrl+K (AUD-006) | **done ✓**

### مشکل
برای پیدا کردن یک بیمار باید اول به صفحه‌ی بیماران می‌رفتی، بعد جستجو
می‌کردی. هر ماژول جستجوی خودش را داشت و هیچ راهی برای «فقط اسمش را
بنویس و پرونده‌اش باز شود» وجود نداشت.

### علت ریشه‌ای
`AICommandBar` بلد بود **برود** («برو به بیماران») ولی بلد نبود
**پیدا کند**. نیمه‌ی دوم هرگز نوشته نشده بود.

نکته‌ی مهم: این کار یک بار در `feat/global-search` ساخته شده بود، ولی
هنگام revert اضطراری v1.147.0 محتوایش از main برداشته شد. کامیت
`81e06f9` در تاریخچه ماند اما کد در درخت کاری نبود — یعنی سند
`START-HERE.md` که آن را «شاخه‌ی معلق» می‌نامید، دقیق نبود.

### راه‌حل
`src/lib/globalSearch.ts` جدید، با استفاده‌ی مجدد از `fuzzyScore` موجود
به‌جای افزودن یک وابستگی جستجو. **همان `AICommandBar` ارتقا داده شد،
نه یک نوار جستجوی دوم** — دو مسیر برای یک مقصد ممنوع است.

- `Ctrl/Cmd+K` روی `window` بسته شد تا از هر صفحه‌ای کار کند
- ایندکس روی **باز شدن** نوار ساخته می‌شود، نه روی mount
- وزن‌دهی به نوع رکورد (بیمار ۱.۴ ← لابراتوار ۰.۸۵)
- پیشوند محدودکننده: `بیمار: احمدی`
- دستور شناخته‌شده هنوز برنده است: «بیمار جدید اضافه کن» یک دستور است،
  نه جستجوی بیماری به نام «جدید»

### دو نقصی که هنگام بازخوانی همین کد پیدا شد
| نقص | چه اتفاقی می‌افتاد | اصلاح |
|---|---|---|
| **پرش هایلایت** | لیست گروه‌بندی‌شده نمایش داده می‌شد ولی کلید ↓ روی آرایه‌ی مرتب‌شده‌ی خام حرکت می‌کرد. هر جا نتیجه‌ای ضعیف‌تر هم‌نوع نتیجه‌ای قوی‌تر بود، این دو ترتیب فرق می‌کردند و هایلایت بین بخش‌ها می‌پرید | تابع `flattenGroups` — نمایش و کیبورد هر دو از یک ترتیب می‌خوانند |
| **ایندکس کهنه** | ایندکس فقط یک بار در هر session ساخته می‌شد. بیماری که ۵ دقیقه پیش ثبت شده بود تا بارگذاری مجدد صفحه در جستجو پیدا نمی‌شد | ساخت مجدد در هر بار باز شدن؛ لیست قبلی حین ساخت روی صفحه می‌ماند |

ضمناً `results` و `noMatch` از state خارج و مشتق (derived) شدند. علت:
اگر کاربر قبل از آماده شدن ایندکس تایپ می‌کرد، «چیزی پیدا نشد» روی
صفحه گیر می‌کرد چون هیچ‌چیز رتبه‌بندی را دوباره اجرا نمی‌کرد.

### نتیجه‌ی تست واقعی
```
npm run verify
✓ sw.js cache already at v1.150.0
✓ tsc --noEmit — بدون خطا
✓ Test Files 14 passed | Tests 211 passed (211)
✓ vite build — موفق در ۱۶.۵ ثانیه
```
۱۸۴ تست پایه ← **۲۱۱ تست** (۲۴ تست بازیابی‌شده + ۳ تست جدید برای
`flattenGroups`، شامل یک fixture که واقعاً ترتیب خام و ترتیب نمایش را
از هم جدا می‌کند).

### ریسک باقی‌مانده
- **تأیید بصری انجام نشده.** من نمی‌توانم روی گوشی لمس کنم. چیدمان
  لیست نتایج، ترتیب RTL و رفتار `Ctrl+K` روی دستگاه واقعی باید توسط
  مهدی دیده شود.
- ایندکس همه‌ی رکوردها را به حافظه می‌خواند. برای کلینیک فعلی
  (۲ بیمار) بی‌اهمیت است؛ در مقیاس رقیب (۱۴٬۴۳۰ بیمار) ساخت ایندکس
  در هر بار باز شدن احتمالاً باید به یک ایندکس افزایشی تبدیل شود.
- `payment` در `KIND_LABELS` تعریف شده ولی هنوز در ایندکس پر نمی‌شود —
  عمدی است تا نتایج مالی بعداً با احتیاط اضافه شوند، نه کد یتیم:
  نوع اتحادیه‌ای (union) وجود دارد تا افزودنش بدون برچسب ممکن نباشد.

---

## موارد باز (از ممیزی AUD)

| شناسه | شرح | وضعیت |
|---|---|---|
| AUD-003 | صفر تست خودکار | **done ✓** (MOD-TEST-001/2/3) |
| AUD-004 | پیامک: هیچ سرویس واقعی وصل نیست (نیاز به کلید API) | pending |
| AUD-005 | پوش نوتیفیکیشن وجود ندارد | pending |
| AUD-006 | جستجوی سراسری (Ctrl+K) نیست | **done ✓** (MOD-FEAT-010) |
| AUD-007 | خروجی PDF رسمی نیست (فقط CSV) | pending |
| AUD-008 | دسترس‌پذیری ناقص (۵۴ aria-label) | pending |
| AUD-010 | طرح درمان جامع | **done ✓** (MOD-FEAT-001 + 002) |
| AUD-011 | تعطیلات اشتباه | **done ✓** (MOD-DATA-001) |
