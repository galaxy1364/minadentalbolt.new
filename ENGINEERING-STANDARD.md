# 🔒 استاندارد مهندسی مینادنت — قفل‌شده

> این سند **قانون ثابت** پروژه است. هر تغییری در مینادنت باید از این
> مسیر عبور کند. خروج از این مسیر مجاز نیست.
> **آخرین به‌روزرسانی:** ۱۴۰۵/۰۶/۰۳

---

## چرخه‌ی اجباری ۵ مرحله‌ای

هیچ کدی بدون طی کردن هر ۵ مرحله نوشته نمی‌شود:

### ۱️⃣ جستجو (قبل از فکر کردن)
- جستجوی وب برای بهترین شیوه‌ی روز، نه حافظه‌ی قدیمی
- بررسی رقبا (مینادنت، لبخند، دندان‌چی) برای اطمینان از برابری یا برتری
- **خواندن کد واقعی موجود** — نه فرض کردن اینکه چطور کار می‌کند

### ۲️⃣ تحلیل عمیق (قبل از کدنویسی)
حداقل **۳ گزینه‌ی فنی** با جدول مقایسه:

| معیار | توضیح |
|---|---|
| Performance | سرعت اجرا، حجم باندل |
| Maintainability | چقدر بعداً قابل فهم و تغییر است |
| سازگاری با استک | React 18 + Vite 5 + TS 5 + Supabase + Dexie |
| پشتیبانی بلندمدت | آیا ۳ سال دیگر هم زنده است |

سپس: انتخاب + **یک خط دلیل**. گزینه‌های منسوخ (deprecated) اصلاً نوشته نمی‌شوند.

### ۳️⃣ پیاده‌سازی
- TypeScript strict — بدون `any` بی‌دلیل
- RTL و فارسی در تمام UI (جهت متن، آیکون، چیدمان)
- کامنت انگلیسی برای منطق پیچیده: **چرا** این‌طور نوشته شده، نه **چه** کاری می‌کند
- **تکرار ممنوع**: قبل از ساخت هر دکمه/جریان جدید، جستجو که آیا مسیر
  مشابهی از قبل هست — دو راه برای یک مقصد یعنی باگ

### ۴️⃣ مستندسازی شماره‌دار
هر ماژول شناسه می‌گیرد و در `CHANGELOG.md` ثبت می‌شود:

```
MOD-<حوزه>-<شماره> | تاریخ | شرح | وضعیت
حوزه‌ها: SEC (امنیت) · TEST · UI · DATA · PERF · FEAT
وضعیت: done ✓ | pending | needs-review
```

هر رکورد باید داشته باشد: مشکل، علت ریشه‌ای، راه‌حل، **نتیجه‌ی تست
واقعی**، و **ریسک باقی‌مانده**.

### ۵️⃣ تست اجباری (بدون استثنا)
```bash
npm run verify   # typecheck → tests → build
```
- برای منطق خالص: تست Vitest بنویس و **اجرا کن**
- برای تغییر دیتابیس: کوئری واقعی روی Supabase بزن و نتیجه را نشان بده
- **هرگز ننویس «باید کار کند»** — یا اجرا شده و نتیجه دیده شده، یا نشده
- اگر خطا داد: قبل از رفتن به بخش بعد، حل شود

---

## ⛔ ممنوعیت‌های مطلق

| ممنوع | دلیل |
|---|---|
| حذف دائمی هر رکورد | سیاست پروژه: فقط soft-delete / تغییر وضعیت |
| فیلد اجباری که فقط هشدار می‌دهد | باید واقعاً جلوی ادامه را بگیرد |
| کد یتیم (نوشته‌شده ولی صدا زده نشده) | بارها در این پروژه باگ ساخته |
| دو مسیر برای یک کار | منشأ سردرگمی و باگ تکراری |
| `USING (true)` در RLS | حفره‌ی امنیتی — همیشه `current_clinic_id()` |
| ادعای تست بدون اجرا | نقض مستقیم این استاندارد |
| محاسبه‌ی مالی غیردقیق | اعداد کلینیک باید ریاضی‌وار درست باشند |

---

## 🎯 معیار برتری بر رقبا

بر اساس بررسی واقعی (۱۴۰۵/۰۶): رقبای اصلی مینادنت.ir (۳۰۰۰+ مرکز)
و لبخند این‌ها را دارند. مینادنت ما باید **حداقل برابر، و در ستون
آخر برتر** باشد:

| قابلیت | رقبا | ما | وضعیت |
|---|---|---|---|
| چارت گرافیکی دندان | ✅ | ✅ | برابر |
| چارت پریودنتال تخصصی | ✅ | ❌ | **کمبود** |
| مدیریت لابراتوار | ✅ | ✅ | برابر |
| حسابداری + چک | ✅ | ✅ | برابر |
| نوبت‌دهی + تقویم تعطیلات | ✅ | ⚠️ | تعطیلات ناقص (AUD-011) |
| پیامک | ✅ | ❌ | **کمبود** (AUD-004) |
| اپ موبایل | ✅ | ✅ PWA | برابر |
| مدیریت صف + فراخوان صوتی | ✅ | ❌ | **کمبود** |
| کیوسک پذیرش | ✅ | ❌ | خارج از دامنه فعلی |
| CRM / باشگاه مراجعین | ✅ | ❌ | **کمبود** |
| **آفلاین‌اول واقعی** | ❌ | ✅ | **🏆 برتری ما** |
| **RLS سطح دیتابیس** | نامعلوم | ✅ | **🏆 برتری ما** |
| **تست خودکار** | نامعلوم | ✅ ۳۶ تست | **🏆 برتری ما** |
| **عدم حذف دائمی** | نامعلوم | ✅ ۱۵+ نوع رکورد | **🏆 برتری ما** |

**استراتژی:** کمبودها را ببندیم، برتری‌ها را عمیق‌تر کنیم.

---

## 📋 صف اولویت (به‌روزرسانی بعد از هر مرحله)

| # | شناسه | شرح | وضعیت |
|---|---|---|---|
| ۱ | MOD-SEC-001 | رفع بنیادی RLS | ✅ done |
| ۲ | MOD-TEST-001/2/3 | تست خودکار (۳۶ تست) | ✅ done |
| ۳ | AUD-011 | تعطیلات ۱۴۰۳/۰۴/۰۶/۰۷ | ⏭️ بعدی |
| ۴ | AUD-010 | صفحه‌ی جامع طرح درمان چندجلسه‌ای | pending |
| ۵ | AUD-006 | جستجوی سراسری (Ctrl+K) | pending |
| ۶ | AUD-007 | خروجی PDF رسمی | pending |
| ۷ | AUD-008 | دسترس‌پذیری (aria-label) | pending |
| ۸ | AUD-004/005 | پیامک و پوش (نیاز به کلید API) | blocked |

---

## 🚫 آنچه هرگز ادعا نمی‌شود

صداقت بخشی از این استاندارد است:

- **تست تعاملی روی گوشی**: من فقط کد می‌خوانم و بیلد می‌گیرم؛
  لمس واقعی صفحه از عهده‌ام خارج است. هر تغییر UI باید توسط شما دیده شود.
- **چارت سه‌بعدی قابل چرخش**: نیاز به کتابخانه‌ی گرافیک سه‌بعدی +
  مدل‌های واقعی دندان — پروژه‌ی چندهفته‌ای طراحی، نه کدنویسی.
- **کارهای چندهفته‌ای در یک پاسخ**: تحویل سطحی و پر از رگرسیون می‌شود.
  یک مورد، کامل و تست‌شده — بهتر از ده مورد نیمه‌کاره.
