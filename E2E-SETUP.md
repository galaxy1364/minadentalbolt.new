# تست با مرورگر واقعی — راهنمای راه‌اندازی

> این فایل برای دو مخاطب نوشته شده: **مهدی**، و **هر نمونه‌ی Claude که روی
> لپ‌تاپ باز می‌شود**. اگر Claude Code هستی و این فایل را می‌خوانی، بخش
> «برای Claude روی لپ‌تاپ» را کامل بخوان.

---

## چرا این وجود دارد

تا v1.188 پروژه ۷۲۳ تست داشت و همه سبز بودند. با همان ۷۲۳ تست سبز، دو باگ
واقعی منتشر شد که کاربر با چشمش پیدا کرد:

| باگ | چه بود | چرا تست نگرفتش |
|---|---|---|
| MOD-FIX-013 | **کل قوس دندانی آینه بود** — راست بیمار سمت راست صفحه افتاده بود | داده درست بود؛ فقط جهت چیدمان غلط بود. jsdom چیدمان ندارد |
| MOD-FIX-012 | قوس دندان نصف عرض صفحه گیر کرده بود | jsdom عرض را نمی‌سنجد |

هر دو از یک جنس‌اند: **تست‌های موجود چیزی را نمایش نمی‌دهند.** آن‌ها DOM
می‌سازند و می‌خوانند. مرورگر واقعی صفحه را می‌چیند — و فقط او می‌تواند
بگوید یک عنصر **کجای صفحه** است و **چقدر عرض** دارد.

---

## برای مهدی — سه دستور

در پوشه‌ی پروژه، ترمینال را باز کن و این سه خط را یکی‌یکی بزن:

```bash
npm ci
npm run e2e:install
npm run e2e
```

- خط اول: کتابخانه‌ها را نصب می‌کند (اگر قبلاً کرده‌ای، سریع رد می‌شود)
- خط دوم: **مرورگر واقعی Chromium** را دانلود می‌کند — فقط یک بار لازم است
- خط سوم: تست‌ها را اجرا می‌کند

اگر خواستی تست‌ها را **ببینی** که چطور کلیک می‌کنند:

```bash
npm run e2e:ui
```

### رمز لازم نیست (MOD-TEST-004)

قبلاً تست‌های روند مطب به `E2E_EMAIL` و `E2E_PASSWORD` نیاز داشتند و بدون
آن‌ها **رد** می‌شدند — یعنی در عمل هرگز اجرا نمی‌شدند.

حالا `e2e/fixtures/backend.ts` مرز شبکه‌ی سوپابیس را می‌بندد و یک کلینیک
آماده (پزشک، یونیت، دو رویه‌ی قیمت‌دار، لابراتوار) را از همان مسیر sync
واقعی داخل Dexie می‌ریزد. برنامه آفلاین‌اول است و داده‌اش را از Dexie
می‌خواند، پس **همه‌ی کد بعد از آن مرز، کد واقعی است**: React، مسیریابی،
چیدمان، چارت.

پس این سه دستور، بدون هیچ تنظیم اضافه‌ای، کل سوئیت را اجرا می‌کنند.

⚠️ **این جای تست با سرور واقعی را نمی‌گیرد.** RLS، مهاجرت‌ها و همگام‌سازی
واقعی این‌طور سنجیده نمی‌شوند و هنوز فقط با کوئری روی محیط زنده قابل
تأییدند.

---

## برای Claude روی لپ‌تاپ

اگر تازه این پروژه را باز کرده‌ای:

1. **اول `START-HERE.md` را بخوان، بعد `ENGINEERING-STANDARD.md`، بعد
   `COMPETITOR-AUDIT-minadent.md`.** استاندارد مهندسی این پروژه
   غیرقابل‌مذاکره است — به‌ویژه: هیچ حذف دائمی، هیچ کد یتیم، هیچ ادعای
   «تست شد» بدون اجرای واقعی.

2. **این مخزن است و نه هیچ پروژه‌ی هم‌نام دیگری:**
   `github.com/galaxy1364/minadentalbolt.new`
   پوشه‌های دیگری با نام مشابه روی همین لپ‌تاپ وجود دارند. اگر مطمئن
   نیستی در کدامی، `git remote -v` بزن و تأیید کن. **در پوشه‌ی اشتباه
   چیزی ننویس.**

3. **قبل از هر ادعای «انجام شد»:**
   ```bash
   npm run verify        # typecheck + ۷۲۳ تست واحد + build
   npm run e2e           # مرورگر واقعی
   ```

4. **کاری که از این نمونه ساخته است و از نسخه‌ی چت نیست:**
   - مرورگر واقعی باز کن و اسکرین‌شات بگیر — و **خودت نگاهش کن**
   - برنامه را مثل یک منشی واقعی استفاده کن: بیمار بساز، نوبت بده،
     ویزیت باز کن، از چارت دندان بزن، درمان ثبت کن، سفارش لابراتوار
     بده، پرداخت ثبت کن، رسید چاپ کن
   - هر چیزی که تنگ، بریده، آینه، تکراری یا نامفهوم بود بنویس

5. **هر باگ چیدمانی که پیدا کردی، اول یک تست Playwright برایش بنویس که
   شکست بخورد، بعد رفعش کن.** دو باگ اخیر دقیقاً به این دلیل منتشر شدند
   که هیچ تستی نمی‌توانست ببیندشان.

---

## چه چیزی هست و چه چیزی نیست

**`e2e/smoke.spec.ts`** — بدون هیچ تنظیمی:
- برنامه بالا می‌آید و **فرم ورود واقعی** دیده می‌شود. قبلاً این ادعا
  `getByText(/مینادنت|ورود|رمز عبور/)` بود که روی صفحه‌ی خطای «پیکربندی
  سرور ناقص است» هم سبز می‌شد — چون در جمله‌ی «بنابراین **ورود** به حساب
  ممکن نیست» می‌افتد. حالا دنبال جعبه‌ی رمز و دکمه‌ی ورود می‌گردد.
- هیچ خطای کنسولی هنگام بارگذاری نیست (یک بار با متغیر محیطی غایب سفید شد).
  خطاهای مسیر «آفلاین» فیلتر می‌شوند: برنامه عمداً بدون سرور کار می‌کند، و
  قرمز کردن تست به‌خاطر آن یعنی قرمز شدن در CI — دقیقاً جایی که برایش
  نوشته شده.
- **هیچ عنصری از عرض گوشی بیرون نمی‌زند** ← همان باگ MOD-FIX-012

**`e2e/clinic-flow.spec.ts`** — این هم بدون رمز:
- **قوس آینه نیست** — دندان ۱۱ (راست بیمار) از دندان ۲۱ چپ‌تر است ← MOD-FIX-013
- هر ۳۲ دندان دائمی رندر می‌شوند، هر چهار ربع ← MOD-FIX-012
- **وضعیت و سطح دندان بعد از ذخیره باقی می‌ماند** ← MOD-FIX-016
- **نوار «نگه دارید» از راست پر می‌شود، نه از چپ** ← MOD-FIX-015
- هفت صفحه‌ی اصلی سرریز افقی ندارند

«صفحه‌ی چاپ راه خروج دارد» (MOD-FIX-011) از این‌جا برداشته شد چون
`src/lib/printDocument.test.ts` همان را کامل‌تر و بدون مرورگر می‌سنجد —
از جمله اینکه هیچ صفحه‌ای سند چاپی دستی نمی‌سازد.

**آنچه هنوز پوشش ندارد:** نوبت‌دهی تا آخر، سفارش لابراتوار، پرداخت و رسید،
رفتار آفلاین، و همگام‌سازی واقعی. این‌ها کار بعدی است.

---

## در CI

⚠️ **این فایل هنوز در مخزن نیست.** توکنی که از نسخه‌ی چت استفاده شد
دسترسی `workflow` نداشت و گیت‌هاب اجازه‌ی ساختش را نداد. فایلش آماده است و
باید دستی اضافه شود: `.github/workflows/e2e.yml`

بعد از اضافه شدن، روی هر push به `main` یک مرورگر واقعی بالا
می‌آورد. اسکرین‌شات و trace هر شکستی به‌عنوان artifact ذخیره می‌شود.

**هیچ secret ای لازم نیست.** بعد از MOD-TEST-004 کل سوئیت — از جمله
تست‌های روند مطب — بدون `E2E_EMAIL`، `E2E_PASSWORD` یا کلید سوپابیس اجرا
می‌شود. `playwright.config.ts` خودش یک کلید جانشین به سرور توسعه می‌دهد و
`e2e/fixtures/backend.ts` مرز شبکه را می‌بندد.

### اگر `npm run e2e:install` شکست خورد

در محیط‌هایی که شبکه‌شان محدود است (مثل کانتینر Claude Code on the web)،
`cdn.playwright.dev` بسته است و دانلود مرورگر با ۴۰۳ رد می‌شود. اگر
Chromium از قبل روی سیستم هست، با اشاره به همان می‌شود سوئیت را اجرا کرد:

```bash
PLAYWRIGHT_BROWSERS_PATH=/path/to/browsers npm run e2e
```

روی لپ‌تاپ مهدی این مشکل وجود ندارد و همان `npm run e2e:install` کافی است.
