# بریف تحویل به Claude Code

> **این فایل را کامل بخوان، بعد کار را شروع کن.**
> تاریخ نگارش: ۱۴۰۵/۰۶/۱۰ · نسخه در زمان نگارش: **v1.189.0** · ۷۲۳ تست واحد
>
> نویسنده: نسخه‌ی چت Claude، که کد را می‌نویسد و تست واحد اجرا می‌کند ولی
> **هیچ‌وقت برنامه را نمی‌بیند**. تو می‌توانی ببینی. کل دلیل وجود این فایل
> همین است.

---

## ۰. اول از همه — مخزن درست

```
github.com/galaxy1364/minadentalbolt.new
```

روی این لپ‌تاپ پوشه‌های دیگری با نام مشابه «مینادنت» وجود دارد که ربطی
به این پروژه ندارند. **قبل از نوشتن هر خط کد:**

```bash
git remote -v      # باید minadentalbolt.new باشد
git log --oneline -1
```

اگر چیز دیگری دیدی، **در آن پوشه چیزی ننویس** و از مهدی بپرس.

---

## ۱. سه فایلی که باید بخوانی

به همین ترتیب:

1. `START-HERE.md` — وضعیت پروژه، نقشه‌ی راه، و **فهرست تست دستی**
2. `ENGINEERING-STANDARD.md` — استاندارد غیرقابل‌مذاکره (چرخه‌ی ۵ مرحله‌ای،
   جدول ممنوعیت‌ها، و بخش «آنچه هرگز ادعا نمی‌شود»)
3. `COMPETITOR-AUDIT-minadent.md` — سنجش با رقیب؛ معیار «حداقل برابر»
4. `E2E-SETUP.md` — راه‌اندازی تست مرورگر واقعی

بعد `CHANGELOG.md` را از بالا تا `MOD-FIX-006` بخوان. آن‌ها کارهای همین
هفته‌اند و هر کدام یک بخش «ریسک باقی‌مانده» دارد که **فهرست کار توست**.

---

## ۲. قوانین مطلق — قابل مذاکره نیستند

- **هیچ حذف دائمی.** فقط `deactivate` یا `status: 'cancelled'`.
  `src/lib/api.ts` صفر فراخوانی حذف دارد و باید همین بماند.
- **هیچ کد یتیم.** چیزی که نوشته شود ولی صدا زده نشود، حذف می‌شود.
  امروز سه باگ از همین یک الگو آمد (پایین ببین).
- **هیچ دو مسیر برای یک مقصد.** اگر منطقی در دو جا کپی شده، بکشش بیرون.
- **هیچ `USING(true)` در RLS.** همیشه با `current_clinic_id()`.
- **هیچ ادعای «تست شد» بدون اجرای واقعی.**
- **هیچ ریاضی مالی غیردقیق.** هر تابع پول باید خالص و تست‌شده باشد.
- **هر تغییر UI به تأیید بصری مهدی نیاز دارد.** تو می‌توانی اسکرین‌شات
  بگیری و نگاه کنی — این را جدی بگیر، ولی جای تأیید او را نمی‌گیرد.

### چرخه‌ی اجباری هر تغییر
```bash
npm run verify     # sync-sw + tsc --noEmit + 723 تست واحد + build
npm run e2e        # مرورگر واقعی
```
و یک رکورد در `CHANGELOG.md` با شناسه‌ی `MOD-<AREA>-<NUM>` شامل: مشکل،
علت ریشه‌ای، گزینه‌های بررسی‌شده، **نتیجه‌ی تست واقعی**، و **ریسک
باقی‌مانده**.

---

## ۳. الگویی که این هفته سه بار باگ ساخت

منطق داخل یک کامپوننت تعریف می‌شد، پس هیچ‌جای دیگری به آن دسترسی نداشت،
پس یک نسخه‌ی موازی ساخته می‌شد که با اصلی از هم می‌افتاد:

| مورد | چه چیزی داخل کامپوننت بود | نتیجه |
|---|---|---|
| MOD-FIX-006 | ردیف‌های قوس پالمر | خط وسط دو تا شد، در دندان شیری صفر |
| MOD-FEAT-023 | برچسب پالمر | چارت `UR1` می‌گفت، پرداخت «دندان ۱۱» |
| MOD-FEAT-024 | تصویر دندان | فرم‌ها انتخابگر عددی جدا داشتند |

**نشانه‌اش:** اگر یک تست مجبور شود کد را کپی کند تا بتواند تستش کند، آن
کد جای اشتباهی است.

---

## ۴. دو باگی که با ۷۲۳ تست سبز منتشر شدند

مهدی هر دو را **با چشمش** پیدا کرد. اینها معیار توست:

**MOD-FIX-013 — کل قوس دندانی آینه بود.** داده درست بود (`UR8…UR1 | UL1…UL8`)
ولی ظرف `dir="rtl"` را از پوسته ارث می‌برد و flex در RTL آیتم اول را سمت
راست می‌گذارد. کل دهان برعکس. jsdom چیدمان ندارد، پس هیچ تستی ندیدش.

**MOD-FIX-012 — قوس نصف عرض گیر کرده بود.** داخل `grid-cols-2` کنار فیلد
«رنگ». jsdom عرض نمی‌سنجد.

**قرارداد جهانی که باید بدانی:** چارت دندانی طوری کشیده می‌شود که انگار
روبه‌روی بیمار ایستاده‌ای. **راستِ بیمار در سمت چپِ صفحه.** راست و چپ
همیشه مالِ بیمار است، نه بیننده.

---

## ۵. کارهای باز — به ترتیب اولویت

### ترتیب کلی کار — این را قبل از بخش‌های بعدی بخوان

مهدی این را درست پرسید: «اگر تست بگیریم و بعد قرار باشد جابه‌جا کنیم،
دوباره‌کاری می‌شود.» **حق با اوست** — برای تست‌های چیدمان.

```
فاز ۱ — دود روشن شود          npm run e2e (فقط smoke)
فاز ۲ — کار بصری و روند       اسکرین‌شات، بدون تست ریز
فاز ۳ — قفل کردن ثابت‌ها       بعد از اینکه صفحه تثبیت شد
```

فاز ۲ بلندترین است و شامل بیشتر ۱۳ مورد بخش «بازیافت» می‌شود. در طول آن
فقط تست دود و ثابت‌های موجود را سبز نگه دار؛ تست چیدمان جدید ننویس.

**استثنا:** هر باگی که قبلاً منتشر شده و دوباره برگشته، همان لحظه تست
می‌خواهد.

### 🔴 اول: تست‌های E2E را واقعاً اجرا کن
`e2e/smoke.spec.ts` و `e2e/clinic-flow.spec.ts` نوشته شده‌اند ولی
**هرگز اجرا نشده‌اند** — در محیط نویسنده دانلود مرورگر مسدود بود.
انتخابگرها از روی کد حدس زده شده‌اند، نه از روی DOM واقعی. اولین اجرا
تقریباً حتماً اصلاح می‌خواهد.

```bash
npm ci && npm run e2e:install && npm run e2e
```

### 🔴 دوم: فایل CI را اضافه کن
`.github/workflows/e2e.yml` ساخته شد ولی **push نشد** — توکن استفاده‌شده
دسترسی `workflow` نداشت. متنش در `E2E-SETUP.md` توضیح داده شده؛ از مهدی
بگیر و اضافه کن.

### 🔴 سوم: بررسی داده‌ی آینه‌شده
تا v1.188 قوس آینه بود. رکوردهای زیر **ممکن است دندانشان برعکس ثبت شده
باشد** اگر کاربر به موقعیت روی صفحه نگاه کرده باشد نه به برچسب:

| جدول | تعداد رکورد با دندان |
|---|---|
| `treatments` | ۷ |
| `lab_orders` | ۲ |
| `implant_cases` | ۱ |

**خودکار عوضشان نکن.** فهرستشان را به مهدی بده و بگذار او بگوید کدام
اشتباه است. (داده‌ی فعلی تستی است، ولی همین عادت باید بماند.)

### بازیافت از دو گفتگوی پرشده — ۱۸ مورد، همه بررسی‌شده

مهدی فهرست موارد ثبت‌نشده را از دو گفتگوی قبلی بیرون کشید. **هر مورد در
برابر کد v1.193 چک شد** — چون بعضی‌شان در فاصله رفع شده بودند.

#### ✅ رفع‌شده یا باگ نبوده — کاری لازم نیست
| مورد | نتیجه |
|---|---|
| شمارنده‌ی خالی «○» روی چهار کارت وضعیت | **باگ نبود.** `DentalChart:719` شمارش می‌کند و با رقم فارسی چاپ می‌کند؛ «۰» یعنی واقعاً صفر دندان آن وضعیت را داشت |
| `tooth_notes` در فهرست سینک | **برداشته شده.** جدول در دیتابیس نیست و در `db.ts` فقط یک کامنت تاریخی مانده. سینک دیگر نمی‌شکند |
| علامت‌های کوادرانت، مسیر ویزیت→چارت | در گفتگوهای بعدی رفع شد |

#### 🔴 باز — و تأیید شد که هنوز در کدند

**۱. دکمه‌ی سطل قرمز روی ردیف درمان و ویزیت** — `Treatments.tsx:995` و
`:1208`. هر دو `status: 'cancelled'` می‌زنند (درست است) ولی آیکون
`Trash2` قرمز نشان می‌دهند. **این جامانده‌ی خودِ MOD-UI-011 است** — آنجا
`Implants`، `PersonalFinance` و `Prescriptions` اصلاح شدند ولی
`Treatments` از قلم افتاد. `Ban` یا `Archive` باید بگذاری.

**۲. تاریخ با رقم لاتین در جدول ویزیت‌ها** — `Treatments.tsx:987`،
`toJalaliString()` که رقم فارسی نمی‌زند. همان ناهماهنگی که در
MOD-FEAT-023 برای دندان رفع شد ولی برای تاریخ نه. ۱۲۷ محل استفاده دارد،
پس `toJalaliStringPretty` را فارسی کن (نمایشی است) و `toJalaliString` را
در محل‌های نمایشی بپیچ.

**۳. انتخاب بیمار در «شروع سریع» یک `Select` بومی است** —
`Treatments.tsx:1504`. جستجوگر غنی با چیپ بدهکار در نوبت‌دهی ساخته شده
ولی اینجا نیامد. **«دو مسیر برای یک انتخاب»** — نقض مستقیم استاندارد.

**۴. تداخل درمان روی یک دندان** — «سه سطح» و «بیلدآپ» هم‌زمان روی دندان
۱۱ ثبت شدند. هیچ هشداری نبود. رویه‌های هم‌پوشان بالینی باید تشخیص داده
شوند. **نیازمند تعریف با مهدی:** کدام جفت‌ها ناسازگارند.

**۵. دندان‌های علامت‌خورده در ویزیت به فرم درمان پیشنهاد نمی‌شوند** —
MOD-FEAT-021 فقط **یک** دندان را از چارت می‌آورد. اگر در ویزیت روی چند
دندان علامت گذاشته شده، فرم باید همان‌ها را پیشنهاد دهد.

**۶. قوس افقی بریده می‌شود** — روی عرض گوشی حدود ۹ دندان از ۱۶ دیده
می‌شود. اسکرول هست ولی نیمی از دهان بیرون دید است. رقیب دکمه‌ی
`UPPER / All` دارد. **تست Playwright برایش بنویس، بعد رفع کن.**

**۷. راهنمای رنگ ۱۳ چیپ در ۳ سطر** — فضای عمودی زیادی پیش از خود چارت
می‌گیرد.

**۸. ترتیب فهرست رویه‌ها بی‌قاعده است** — بیلدآپ ← دو سطح ← سه سطح ←
یک سطح. نه کد، نه الفبا.

**۹. دکمه‌ی شناور «جستجو و دستیار» محتوا را می‌پوشاند** — روی ردیف‌های
جدول می‌افتد. `Layout.tsx:260` نوار پایین `pb-safe` دارد ولی دکمه‌ی
شناور فاصله‌ی کافی ندارد.

**۱۰. ردیف کارت آمار از لبه بریده می‌شود** — کارت سوم نیمه‌بیرون.

**۱۱. برچسب دوزبانه‌ی سطوح** — رقیب «لینگوال / Lingual» می‌نویسد،
`surfaceGlyph.ts` فقط `L`.

**۱۲. کنترل زاویه‌ی چارت** و **۱۳. پشتیبانی قلم نوری** — هر دو در چارت
رقیب هستند و **هرگز در `COMPETITOR-AUDIT-minadent.md` ثبت نشدند**. اول
ثبتشان کن، بعد با مهدی تصمیم بگیرید ساخته شوند یا نه.

#### ⚠️ نیازمند اقدام مهدی، نه کد
- **سرویس ایمیل Supabase پیکربندی نشده** — «خطا در ارسال ایمیل بازیابی».
  یعنی **بازیابی رمز برای هیچ‌کس کار نمی‌کند** و دعوت پرسنل هم نه. سرویس
  پیش‌فرض Supabase محدودیت شدید دارد؛ باید SMTP واقعی وصل شود.
- **دو خطای ثبت‌شده در «⚠ ۲» نوار بالا** — `errorLog.ts` آن‌ها را
  **محلی** ذخیره می‌کند نه در Postgres، پس از بیرون قابل خواندن نیستند.
  مهدی باید تنظیمات ← گزارش خطاها را باز کند و متنشان را بدهد.

### زنجیره‌ی هوشمند رویه‌ها — خواسته‌ی اصلی مهدی
«اگر پزشک روی چارت عصب‌کشی زد، باید بخش‌های مخصوص عصب‌کشی و بعد پست و
بعد روکش خودکار باز شود، نه اینکه دوباره پوسیدگی و غیره پرسیده شود.»

این یک **نگاشت دانشی** است نه ترتیب فرم. قبل از کد زدن باید با مهدی
تعریف شود: هر رویه چه زنجیره‌ای دارد، کدام مرحله اجباری است، و کدام
فیلدها از مرحله‌ی قبل پر می‌شوند.

### گارد پرداخت تکراری
همین حالا **۱ پرداخت در انتظار** در دیتابیس هست که با فاصله‌ی ۲ دقیقه و
۳۷ ثانیه از یک پرداخت هم‌مبلغ و هم‌روز ثبت شده، بدون هیچ هشداری. هشدار
«تسویه‌حساب کامل» وجود دارد ولی این حالت را نمی‌گیرد. منطق خالص، تست‌پذیر.

### اقساط با ماه شمسی
`src/lib/installments.ts` تاریخ سررسید را با **ماه میلادی** جلو می‌برد.
طول ماه‌های شمسی فرق دارد، پس روزِ شمسیِ سررسید در طول سال چند روز
جابه‌جا می‌شود. به بیمار می‌گویی «اول هر ماه» و سررسیدها می‌شوند
۱، ۱، ۲، ۲، ۳، ۴… `jalaali-js` از قبل در پروژه هست.

### موارد کوچک‌تر
- **سطح دندان در سفارش لابراتوار ذخیره نمی‌شود** — `lab_orders` ستون سطح
  ندارد. به فرم می‌رسد، در دیتابیس نمی‌ماند.
- **وضعیت دندان‌ها در فرم‌ها نمایش داده نمی‌شود** — `ToothArchSelect`
  ورودی `conditions` دارد ولی سه فرم پرونده‌ی دندانی را بارگذاری نمی‌کنند،
  پس همه سالم دیده می‌شوند. در چارت درست است.
- **پیوند درمان و سفارش لابراتوار استنباطی است** — `lab_orders` ستون
  `treatment_id` ندارد. `src/lib/labHandoff.ts` با ویزیت+آزمایشگاه+دندان
  حدس می‌زند. migration لازم دارد.
- **سهم پزشک از `payments.doctor_id` محاسبه نمی‌شود** — داده از v1.181
  هست، گزارشش ساخته نشده.
- **تصاویر دندان آناتومیک نیستند** — تعداد ریشه‌ها واقعی نیست. مهدی
  می‌خواهد SVG حرفه‌ای جایگزین شود. نقطه‌ی تمیزش:
  `src/components/ToothGlyph.tsx` — یک فایل، و هم چارت هم هر سه فرم با هم
  عوض می‌شوند.

---

## ۶. کارهایی که فقط مهدی می‌تواند

اینها را **نساز و حدس نزن** — عدد جعلی در دیتابیس واقعی از خالی بودن
بدتر است:

| مورد | وضعیت امروز | چرا خودت نه |
|---|---|---|
| شیفت پزشکان | **۰ ردیف** | ساعت کاری را نمی‌شود حدس زد. تا وارد نشود، نوبت‌دهی و تداخل و ریل زمان کار نمی‌کند |
| قیمت پایه‌ی رویه‌ها | **۴ رویه بدون قیمت** (بیلدآپ، یک/دو/سه سطح) | تصمیم کسب‌وکار |
| ۳ ویزیت با اضافه‌پرداخت | جمعاً **۲۰,۰۰۰,۰۰۰ تومان** | تصمیم مالی: بازگشت پول، اصلاح لغو، یا جابه‌جایی پرداخت |
| باطل کردن توکن گیت‌هاب | **انجام نشده** | امنیت — توکن در متن گفتگو ظاهر شده |
| چرخاندن رمز دیتابیس | **انجام نشده** | امنیت |

---

## ۶.۵ اتصال به دیتابیس زنده

`.mcp.json` در ریشه‌ی مخزن، سرور MCP سوپابیس را تعریف می‌کند. اولین بار
که Claude Code باز شود، **درخواست تأیید** می‌دهد؛ تا تأیید نشود در
`claude mcp list` به‌صورت pending دیده می‌شود. هیچ رمزی در فایل نیست —
احراز هویت از طریق OAuth انجام می‌شود.

بعد از تأیید می‌توانی روی دیتابیس واقعی کوئری بزنی. **این را جدی بگیر:**
چند باگ مهم این هفته فقط با خواندن داده و لاگ زنده پیدا شدند، نه از روی
کد — از جمله علت واقعی «ثبت می‌شود ولی وارد نمی‌شود» که سه فرضیه‌ی غلط
داشت و لاگ سرور جوابش را داد.

قانون: **خواندن آزاد، نوشتن با احتیاط.** migration را اول اعمال کن، بعد
کدی که آن ستون را می‌نویسد منتشر کن — عکسش یک بار سینک را شکست.

## ۷. دسترسی‌ها

- **Supabase:** پروژه `gkxkihdibkmpryopbkkz` (حساب `mostafa.hasanvand@gmail.com`)
- **Vercel:** ورود با «Continue with GitHub»، حساب `galaxy1364` — نه ایمیل/رمز
- **کلینیک:** `a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11`
- **زنده:** https://minadentalbolt-new.vercel.app

⚠️ Supabase رایگان بعد از ۷ روز بی‌فعالیتی خودش را متوقف می‌کند — یک بار
باعث قطعی شد. workflow نگه‌داشتن فعال در `.github/workflows/` هست.

⚠️ اگر `VITE_SUPABASE_ANON_KEY` تنظیم نباشد، `createClient()` قبل از
mount شدن React خطا می‌دهد و صفحه **سفید** می‌شود. یک بار اتفاق افتاد.

---

## ۷.۵ فهرست تست دستی — نقطه‌ی شروع کار تو

`START-HERE.md` بخش «🧪 فهرست تست دستی» شش بخش دارد (الف تا و) که با
مهدی مرور می‌شد. **این همان روند «از پایه شروع کردن» است** و باید ادامه
پیدا کند — با این تفاوت که حالا تو خودت می‌توانی بیشترش را ببینی.

وضعیت هر بخش تا v1.191:

| بخش | وضعیت |
|---|---|
| **الف) ظاهر** | چارت و نوار پایین تا حدی دیده شد. **پس‌زمینه‌ی رنگی و گلیف سطح هنوز تأیید نشده‌اند** |
| **ب) نوبت‌دهی** | 🔴 **قابل تست نیست** — `doctor_schedules` صفر ردیف دارد. تا مهدی شیفت وارد نکند، ریل ساعت خالی و تداخل معنی ندارد |
| **ج) مالی** | ممیزی شد (v1.179). **اقساط شمسی هنوز باگ دارد** — سررسید با ماه میلادی جلو می‌رود. مورد «طرحی که ۳۱ام شروع شود» هنوز تست نشده |
| **د) حذف و بازگردانی** | برچسب‌ها اصلاح شدند (v1.173) ولی **مسیر بازگردانی هیچ‌کدام از ۱۳ مورد در بایگانی تست نشده** |
| **ه) روند کامل** | تایم‌لاین ساخته و بازسازی شد (v1.176 و v1.179). **رفتار آفلاین/آنلاین هرگز تست نشده** |
| **و) پیگیری** | تست نشده |

**قاعده‌ی مرور:** یکی‌یکی، نه همه با هم. هر مورد را انجام بده، اسکرین‌شات
بگیر، نتیجه را بنویس. اگر ایرادی بود، اول تست شکست‌خورده بنویس بعد رفع
کن.

## ۷.۶ چیزهایی که ساخته شدند ولی هرگز دیده نشدند

```bash
npm run unverified
```

امروز **۲۹ رکورد** برمی‌گرداند. اینها ادعای شکست نیستند — کارهایی‌اند که
منطقشان تست شده ولی هیچ‌کس نتیجه را روی دستگاه واقعی ندیده. **تو
می‌توانی ببینی؛ نویسنده‌ی آن رکوردها نمی‌توانست.**

این احتمالاً پربازده‌ترین کار اول توست: فهرست را بگیر، از تازه‌ترین شروع
کن، هرکدام را باز کن و نگاه کن. دو باگ آخری که مهدی پیدا کرد (قوس آینه و
قوس نصف‌عرض) هر دو در همین فهرست بودند و هفته‌ها منتظر ماندند.

وقتی موردی تأیید شد، خط «ریسک باقی‌مانده»اش را در `CHANGELOG.md` به‌روز
کن تا از فهرست خارج شود.

## ۸. کاری که از تو ساخته است و از نویسنده‌ی این فایل نبود

این مهم‌ترین بخش است. **برنامه را مثل یک منشی واقعی استفاده کن:**

بیمار بساز → نوبت بده → ویزیت باز کن → از چارت دندان بزن → درمان ثبت کن
→ سفارش لابراتوار بده → پرداخت ثبت کن → رسید چاپ کن → پرونده را چاپ کن.

**در هر مرحله اسکرین‌شات بگیر و خودت نگاهش کن.** دنبال این‌ها بگرد:
- چیزی که از عرض صفحه بیرون زده یا زیر نوار پایین پنهان شده
- سؤالی که دو بار پرسیده می‌شود
- عددی که در دو صفحه فرق دارد
- دکمه‌ای که کاری غیر از اسمش می‌کند
- صفحه‌ای که راه برگشت ندارد
- هر جایی که راست و چپ دندان مشکوک است

**و قاعده‌ی مهم:** هر باگ چیدمانی که پیدا کردی، **اول یک تست Playwright
بنویس که شکست بخورد**، بعد رفعش کن. دو باگ اخیر دقیقاً به این دلیل
منتشر شدند که هیچ تستی نمی‌توانست ببیندشان.

---

## ۹. یک چیز که نباید قول بدهی

«صد درصد قابل اطمینان» وجود ندارد. چیزی که ساختنی است: تست خودکار برای
رگرسیون، مرورگر واقعی برای چیدمان، و چشم مهدی برای کار واقعی مطب.

امروز ۹ باگ واقعی پیدا شد. **چهارتاشان را مهدی پیدا کرد، نه ابزارها.**
