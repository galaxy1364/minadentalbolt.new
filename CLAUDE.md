# MinaDent — قوانین پروژه

> این فایل را Claude Code در **هر جلسه خودکار** می‌خواند. عمداً کوتاه است.
> جزئیات در فایل‌های زیر است و باید در شروع کار خوانده شوند.

## قبل از هر کاری

```bash
git remote -v     # باید minadentalbolt.new باشد
```
روی این لپ‌تاپ پوشه‌های دیگری با نام مشابه «مینادنت» هست که ربطی به این
پروژه ندارند. در پوشه‌ی اشتباه چیزی ننویس.

بعد بخوان، به همین ترتیب:
1. `HANDOVER-TO-CLAUDE-CODE.md` — فهرست کار و اولویت‌ها
2. `START-HERE.md` — وضعیت پروژه + **فهرست تست دستی (بخش ۱۰)**
3. `ENGINEERING-STANDARD.md` — استاندارد کامل و غیرقابل‌مذاکره
4. `COMPETITOR-AUDIT-minadent.md` — سنجش با رقیب
5. `E2E-SETUP.md` — تست مرورگر واقعی

## ممنوعیت‌های مطلق

- **هیچ حذف دائمی.** فقط `deactivate` یا `status: 'cancelled'`.
  `src/lib/api.ts` صفر فراخوانی حذف دارد و باید همین بماند.
- **هیچ کد یتیم** — نوشته‌شده ولی صدازده‌نشده.
- **هیچ دو مسیر برای یک مقصد.** منطق کپی‌شده را بکش بیرون.
- **هیچ `USING(true)` در RLS** — همیشه `current_clinic_id()`.
- **هیچ ادعای «تست شد» بدون اجرای واقعی.**
- **هیچ ریاضی مالی غیردقیق** — هر تابع پول خالص و تست‌شده.
- **هیچ فیلد اجباری که فقط هشدار بدهد** — باید واقعاً جلوی ادامه را بگیرد.
- **هیچ عدد جعلی در دیتابیس واقعی** (قیمت، شیفت، سهم). اینها تصمیم
  کسب‌وکار مهدی‌اند، نه حدس تو.

## چرخه‌ی اجباری ۵ مرحله‌ای

۱. **جستجو پیش از فکر کردن** — وب برای شیوه‌ی روز، و **خواندن کد واقعی
   موجود**، نه فرض کردن اینکه چطور کار می‌کند.
۲. **مقایسه‌ی ۳+ گزینه** روی کارایی، نگهداشت‌پذیری، تناسب با استک و
   ماندگاری. یکی را با **یک خط دلیل** انتخاب کن. گزینه‌ی منسوخ رد شود.
۳. **پیاده‌سازی** — TS strict بدون `any` بی‌دلیل، RTL و فارسی کامل،
   کامنت انگلیسی که **چرا** را بگوید نه **چه**.
۴. **مستندسازی** در `CHANGELOG.md` با شناسه‌ی `MOD-<AREA>-<NUM>`:
   مشکل · علت ریشه‌ای · گزینه‌های بررسی‌شده · **نتیجه‌ی تست واقعی** ·
   **ریسک باقی‌مانده**.
۵. **تست اجباری**:
```bash
npm run verify    # sync-sw + tsc --noEmit + تست واحد + build
npm run e2e       # مرورگر واقعی
```
برای تغییر دیتابیس، کوئری واقعی روی Supabase بزن و نتیجه را نشان بده.
**هرگز ننویس «باید کار کند»** — یا اجرا شده و نتیجه دیده شده، یا نشده.

## آنچه هرگز ادعا نمی‌شود

- **تست تعاملی روی گوشیِ مهدی.** تو مرورگر داری، ولی گوشی او را نداری.
- **کارهای چندهفته‌ای در یک پاسخ.** یک مورد کامل و تست‌شده، نه ده مورد
  نصفه. اگر خواسته بزرگ بود، صادقانه بگو و تکه‌اش کن.
- **«صد درصد قابل اطمینان».** ریسک باقی‌مانده را در هر رکورد بنویس.

## الگویی که این هفته سه بار باگ ساخت

منطق داخل یک کامپوننت تعریف می‌شد، هیچ‌جای دیگری به آن دسترسی نداشت، پس
یک نسخه‌ی موازی ساخته می‌شد که با اصلی از هم می‌افتاد.

**نشانه‌ی تشخیص:** اگر تستی مجبور شود کد را کپی کند تا بتواند تستش کند،
آن کد جای اشتباهی است — بکشش به `src/lib/`.

## دو باگی که با ۷۲۳ تست سبز منتشر شدند

هر دو را مهدی با چشمش پیدا کرد، نه ابزارها:
- **قوس دندانی آینه بود** — داده درست بود، جهت چیدمان غلط. jsdom چیدمان ندارد.
- **قوس نصف عرض گیر کرده بود** — jsdom عرض نمی‌سنجد.

**پس:** هر باگ چیدمانی که پیدا کردی، **اول یک تست Playwright بنویس که
شکست بخورد**، بعد رفعش کن.

## قرارداد چارت دندانی

چارت طوری کشیده می‌شود که انگار روبه‌روی بیمار ایستاده‌ای:
**راستِ بیمار در سمت چپِ صفحه.** راست و چپ همیشه مالِ بیمار است، نه بیننده.

ذخیره‌سازی با FDI است، نمایش با پالمر (`src/lib/toothLabel.ts`).
هر جا دندان نمایش می‌دهی از `toothLabel()` استفاده کن، نه عدد خام.

## نکات محیطی

- Supabase رایگان بعد از ۷ روز بی‌فعالیتی متوقف می‌شود (workflow نگه‌داشتن
  فعال در `.github/workflows/`).
- اگر `VITE_SUPABASE_ANON_KEY` نباشد، `createClient()` قبل از mount شدن
  React خطا می‌دهد و صفحه **سفید** می‌شود. یک بار اتفاق افتاد.
- دو اجراکننده‌ی تست: Vitest برای واحد، Playwright برای E2E.
  `vite.config.ts` پوشه‌ی `e2e/` را از Vitest کنار می‌گذارد.

## زبان

کد و کامنت‌ها انگلیسی — کامنت باید **چرا** را توضیح دهد نه **چه**.
رابط کاربری و CHANGELOG فارسی، با RTL کامل و ارقام فارسی.
